# 貢献の手引き(CONTRIBUTING)

このリポジトリへの提案(Pull Request)や不具合の報告を歓迎します。読む前に、まずこのリポジトリの立ち位置を知っておいてください。

## このリポジトリの立ち位置

このソフトウェアの開発は、ここではない別の場所(非公開)で行われています。ここ(公開リポジトリ)には、その開発の成果が**リリースのたびにまとめて反映されます**。

そのため、このリポジトリのコミット履歴には**リリースだけが並びます**。日々の変更がどういう経緯で行われたか、途中の試行錯誤や議論は、このリポジトリの履歴からは見えません。「小さな commit を積み重ねてほしい」といった依頼には応えられない、という点をご了承ください。

## Pull Request を送る

提案(Pull Request)はいつでも歓迎します。送っていただいたあと、次の流れで扱います。

1. こちら側で内容を確かめます。
2. 問題がなければ、開発の本体(非公開の場所)にも同じ変更を取り込みます。
3. そのうえで、このリポジトリ側でも merge します。

このため、**merge されるまでに時間がかかることがあります**。すぐに反応が無くても、届いていないわけではありません。気長にお待ちください。

### 送る前に、手元で確かめてほしいこと

**このリポジトリでは自動テストを走らせていません。** Pull Request を出しても、緑・赤の判定は表示されません。代わりに、送る前に手元で次を実行し、テストが通ることを確かめてください。

```bash
bun install
bun test
```

(2026-09-04 に手元で打った実測では、441 ファイル・9455 件が通り、失敗 0 件・スキップ 1 件で終わります。**この数はリリースのたびに変わります**ので、大事なのは「失敗が 0 件であること」です。)

送っていただいた内容は、受け取った側(このリポジトリの管理者)が手元でテストを打って確認します。テストが通らない状態のままだと、確認に時間がかかったり、そのまま取り込めなかったりします。

### 言語

提案の説明文やコミットメッセージは、日本語で構いません。英語での説明も受け付けます。どちらでも構いませんので、書きやすい言語で書いてください。

### ライセンスについての同意

Pull Request を送った時点で、その内容が本リポジトリのライセンス(AGPLv3)のもとで配布されることに同意したものとして扱います。

## このソフトウェアが扱える範囲(全量)について

このソフトウェアが書けるものには、決まった全量があります。

- 定義できるもの(8種): `app` / `table` / `form` / `list_view` / `detail_view` / `workflow` / `function` / `report_view`
- 項目の型(9種): `text` / `long_text` / `number` / `boolean` / `date` / `select` / `reference` / `image` / `file`
- 定義を変える操作(17種): `add_table` / `add_field` / `add_view` / `update_view` / `remove_field` / `remove_table` /
  `change_table` / `change_field` / `remove_view` / `add_workflow` / `update_workflow` / `remove_workflow` /
  `add_function` / `update_function` / `remove_function` / `set_theme` / `set_roles`

**この一覧を増やす、あるいは種類を追加するような提案は、影響が大きいため慎重に扱います。** 採用を断ることがあります。実装に取りかかる前に、まず Issue で相談してください。相談なしに実装だけを送っていただいても、方向性が合わずお断りする可能性が高いので、ご注意ください。

### 受け付けにくい提案の例

次のような提案は、受け付けにくいものとして扱います。

- 上に挙げた全量(8種 / 9種 / 17種)を増やすもの
- 挙動の変更を伴わない、大量の書式変更だけを行うもの(改行の統一、インデントの付け替えなど)
- 新しい依存ライブラリを追加するもの

これらに当てはまりそうな提案をお考えの場合も、まず Issue で相談していただけると、無駄なく進められます。

## 不具合の報告

不具合を見つけたときは、Issue で報告してください。次の内容を書いていただけると、確認がスムーズです。

- 再現の手順(できれば番号付きで)
- 期待した結果
- 実際に起きた結果

Issue を立てる際は、テンプレートが用意されていますので、そちらに沿って書いてください。
