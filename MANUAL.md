# SmAIltalk マニュアル

書けるものと書けないものを引くための資料である。上から通して読む必要はない。
探している言葉が見つからなかったら、それは「まだ書き方を知らない」のではなく **書けない** ことが多い。
最後の「できないこと」を必ず一度は開いてほしい。

---

## このマニュアルの読み方

- **語彙は意図的に狭い。** ここに挙げた一覧に無いものは、言い方を変えても、道具を組み合わせても表せない。
- **一覧は実装から起こしてある。** 扱えるリソースの種類・フィールドの型・差分操作の3つは、
  カーネルが持つ配列そのものを列挙している。
- **数を覚える必要はない。** 覚えるべきは「名前がこの表に在るかどうか」だけである。

### このマニュアルで使う言葉

| 言葉 | 意味 |
|---|---|
| **アプリ** | 表・画面・自動処理をひとまとめにした単位。1つのアプリが1つのデータベースを持つ |
| **マニフェスト** | そのアプリの定義の全部。JSON である。画面もデータの形も自動処理もここに書いてある |
| **差分(パッチ)** | マニフェストへの変更をまとめたもの。定義を変える手段はこれだけである |
| **意図** | その差分を出すに至った会話の要約。差分には必ず添える。変更履歴にそのまま残る |
| **レコード(行)** | 表に入っている実際のデータ。差分ではなく、レコード用の口から出し入れする |

---

## 全体像

会話から動くものが出てくるまでの流れは、次のとおりである。

| 段 | 何が起きるか | 誰が |
|---|---|---|
| 1. 要望 | 「こういうものが欲しい」と言葉で伝える | あなた |
| 2. 差分 | 今のマニフェストを読み、意図つきの差分を組み立てる | 会話している AI |
| 3. 下見 | 適用したら何が起きるかを、何も変えずに実測する | プラットフォーム |
| 4. 承認 | 何が消えて何が残るかを見て、進めてよいか決める | あなた |
| 5. 適用 | 差分を当てる。当てる直前に自動でスナップショットを取る | プラットフォーム |

### 押さえておく性質

- **アプリごとのコードは生成されない。** 画面はマニフェストを毎回読んで解釈して描かれる。
  したがって **定義を変えたら、ビルドもサーバの再起動も要らず、ブラウザのリロードだけで新しい画面になる。**
- **データが入ったまま育つ。** 表に行が溜まったあとで項目を足しても、作り直しにはならない。
- **消える変更と消えない変更がある。** 破壊的な操作は実際に値を消す。詳しくは「差分操作」と
  「取り消しが効くもの・効かないもの」を読むこと。

### 部品どうしの関係

```
アプリ
 ├─ 表(table)                … データの形。項目と型を持つ
 │    └─ レコード             … 実際の行。差分ではなくレコード用の口で出し入れする
 ├─ 画面(form / list_view / detail_view / report_view)
 │    └─ どの表を、どう見せるか
 ├─ 役割(roles)             … 誰に何が見えるか・書けるか
 ├─ 自動処理(workflow)      … きっかけが起きたら決まった動作をする
 │    └─ 計算の島(function) … 自動処理から呼ばれたときだけ動く閉じた計算
 └─ 見た目(theme)           … アプリ全体の配色・書体・余白
```

---

## 動かすための設定

前提は2つだけである。

- ソフトウェア管理ツール mise が入っていること
- 会話に使う AI クライアント(Claude Code)が使えること

サーバを起動すると、次の1行が標準出力に出る。

```
smailtalk server: http://127.0.0.1:3000 (dataRoot=data, timeZone=Asia/Tokyo)
```

### 環境変数

| 変数 | 既定値 | 効くもの | 実際の挙動 |
|---|---|---|---|
| `ST_DATA_ROOT` | `data` | サーバと AI 側の口の両方 | 実データの置き場所。**相対パスなので、起動時のカレントディレクトリが違えば別の場所を指す。** 2つのプロセスはこのディレクトリを通じてだけ状態を共有する。食い違うと、アプリは作れているのに画面に何も出てこない(エラーは出ない) |
| `PORT` | `3000` | サーバ | 待ち受けるポート。**ホスト名は `127.0.0.1` に固定されていて変えられない** |
| `ST_PREVIEW_BASE_URL` | `http://127.0.0.1:3000` | AI 側の口 | 画面の URL を返すときの基底。**サーバの実際の待ち受け先と揃える責任は人にある。** ずれても警告は出ず、繋がらない URL が返るだけである |
| `ST_TIMEZONE` | `Asia/Tokyo` | サーバ | 時刻起動の自動処理の「毎日 何時何分」の解釈と、「今日もう動いたか」の日付境界。**不正な値ではサーバは起動しない**(黙って既定に落ちない)。**タイムゾーンはマニフェストには書けない** |
| `ST_MCP_ACTOR` | (無し) | AI 側の口 | **AI が誰として操作するかの名乗り。** 空だと道具がすべて断られる(起動そのものは成功する)。そのアプリに登録済みのログイン名でも利用者IDでも書ける。書き換えたら繋ぎ直すこと |

**フロントのビルドを飛ばすと画面が 404 になる。** アプリの画面はビルド済みの資産から配信されるためである。
逆に、ビルド済み資産があると **存在しないアプリの URL でも 200 が返る。**
アプリが在るかどうかの確認に画面の URL を使ってはいけない。定義を返す口を使うこと。

---

## 扱えるリソースの種類

**この一覧が全量である。**

| 種別 | 何か |
|---|---|
| `app` | アプリのルート。名前と、以下のすべてを束ねる |
| `table` | データの定義。項目と型を持つ |
| `form` | 行を作る・直すための入力画面 |
| `list_view` | 一覧画面。表示する列・並べ替え・絞り込み・検索・1ページの件数を持てる |
| `detail_view` | 1行の詳細画面。操作ボタンと、関連する行の一覧を持てる |
| `report_view` | 集計表。束ねるキーと集計を宣言して、群ごとの数を出す |
| `workflow` | 自動処理の定義。きっかけ・動作・実行履歴の書き先を持つ |
| `function` | 計算の島。閉じた場所で動く純粋な計算。自動処理から呼ばれたときだけ動く |

`form` / `list_view` / `detail_view` / `report_view` は「画面」であり、マニフェストでは
`views` という1つの配列に入って `type` で種別を指す(`kind` ではない)。

### 読み取り専用の内部の表

マニフェストには現れないが、読める表が2本ある。

| 表 | 中身 |
|---|---|
| `_apps` | アプリの一覧 |
| `_changelog` | 変更履歴。いつ・どんな意図で・何が当たったか |

`list_view` / `detail_view` の対象にすれば画面から見られる。**書き込み・列の追加・入力画面の作成はできない。**
`_changelog` に載るのは **アプリの作成と、差分の適用だけ** である。
行を直した・消したという操作は載らない。

---

## フィールドの型

**この一覧が全量である。**

| 型 | 何に使うか |
|---|---|
| `text` | 1行の文字列。名前・区分・短い覚え書き |
| `long_text` | 複数行の文字列。説明・本文・失敗の理由 |
| `number` | 数値。金額・個数 |
| `boolean` | 真偽値。チェックボックス |
| `date` | 日付・日時。値は ISO8601 形式の文字列で持つ |
| `select` | 選択肢つき。`options` に許可する値の配列を書く |
| `reference` | 他の表の1行への参照。`reference_table` に相手の表を書く |
| `image` | 画像ファイル。列にはファイルのIDが入る |
| `file` | 画像に限らない一般のファイル。列にはファイルのIDが入る |

### 型ごとの注意

- **`select` の選択肢は差分で書き換える。** 画面から増やすことはできない。
- **`reference` は1本の参照である。** 複数の相手を指す型は無い。
- **`image` と `file` は値の形が同じ**(どちらもファイルのIDを持つ)。違うのは次の3点だけである。
  - 大きさの上限。`image` は 5 MiB、`file` は 20,000,000 バイト。
  - 受け入れる種類。`file` には種類の制限が無い(HTML も SVG も zip も通る)。
  - 配信の形。`file` は **ログインしていない相手には配信されない**(`image` より狭い)。
    また、実体が画像だと判定できないものは必ずダウンロードとして返る。
- **画像の変換・リサイズ・サムネイル生成・CDN 配信は無い。** 使われなくなったファイルの自動削除も無い。

### 型と一緒に書ける項目の設定

| キー | 何を決めるか |
|---|---|
| `required` | 必須かどうか |
| `options` | `select` の許可値 |
| `reference_table` | `reference` の相手の表 |
| `unique` | 重複を禁じる。`true` / `false` のほか `"owner"`(持ち主ごとに一意)が書ける |
| `unit` | 数値などに添える単位の表示(8文字まで) |
| `emphasis` | 値ごとの強調。`neutral` / `info` / `caution` / `danger` のいずれかを値に割り当てる |
| `hide_when_empty` | 空のとき詳細画面から隠す |
| `reference_picker` | 相手の選ばせ方。`list` / `type_filter` / `search` |
| `reference_search_fields` | 相手を探すときに当たる列 |

### 名前が決まっている特別な項目

次の項目IDは、書くと意味を持つ。型は普通の型である。

| 項目ID | 効果 |
|---|---|
| `st_owner` | その行の持ち主。**書いた表では、自分の行しか見えず・書けなくなる** |
| `st_public` | 立てた行だけ、ログインしていない相手にも読める |
| `st_undeletable` | 立てた行は消せない |
| `st_no_direct_create` | その表に直接行を作らせない |

**`st_owner` と役割の付与は同時に効く。** 併用すると、付与があっても他人の行は1件も読めない。
また `st_owner` を書いた表では、詳細画面が「閲覧のみ」になり、編集ボタンが出ないことがある。

---

## 差分操作

**この一覧が全量である。** 危険度で群に分かれる。

### 足すだけの操作(定義もデータも失わない)

| 操作 | 何をするか |
|---|---|
| `add_table` | 表の定義を1つ足す(項目が1件以上必要) |
| `add_field` | 既存の表に項目を1つ足す |
| `add_view` | 画面の定義を1つ足す |
| `update_view` | 既存の画面の表示設定を差し替える |

### データを失わない、定義だけの操作

| 操作 | 何をするか |
|---|---|
| `remove_view` | 画面の定義を1つ取り除く。表・項目・行には触れない。`add_view` で作り直せる |
| `add_workflow` | 自動処理を1本足す。**当てたあとも動き続ける** |
| `update_workflow` | 自動処理の定義を **丸ごと置き換える**(部分更新ではない) |
| `remove_workflow` | 自動処理の定義を取り除く。**既に書かれた行も実行履歴も残る**(消えるのは「これから動くこと」だけ) |
| `add_function` | 計算の島を1つ足す。呼ばれるまで動かない |
| `update_function` | 計算の島の定義を **丸ごと置き換える** |
| `remove_function` | 計算の島の定義を取り除く。**その島が既に書いた行は残る** |
| `set_theme` | アプリの見た目を **丸ごと** 差し替える。表・項目・画面・行には触れない |
| `set_roles` | 役割の一覧を **丸ごと** 差し替える。表・項目・画面・行には触れない |

### 壊す操作 —— 実際に定義とデータを変える

**次の4つだけが破壊的である。**

| 操作 | 何が失われるか |
|---|---|
| `remove_field` | 項目が、入力済みの値ごと消える |
| `remove_table` | 表が、入っている行ごと消える |
| `change_table` | 表の識別子(名前替え)・表示名・代表の項目・探せる列・行アクセス権の宣言を変える |
| `change_field` | 項目の識別子・表示名・型ほかを変える。**型を変えると既存の全行が書き換わる** |

**この4つを含む差分は、いきなり当ててはいけない。** 次の順で進めること。

1. **下見する。** 当てるつもりの差分をそのまま渡し、何が起きるか(適用後の定義・影響を受ける行数・
   断られるならその理由)を実測する。本体は何も変わらないので、失敗しても失うものは無い。
2. **結果を見せて同意を得る。** とくに影響を受ける行数と、消える項目・書き換わる項目の名前。
3. **当てる。**
4. **確かめる。** 定義と行を読み直し、下見の予測と食い違っていたらそれも伝える。
5. **問題があれば取り消す。** ただし取り消しは万能ではない(後述)。

「確認しました」というフラグ引数は用意していない。**フラグを付ければ安全になる、という誤解を生むからである。**

### 差分そのものの形

差分は次の3つを必ず持つ。

| キー | 中身 |
|---|---|
| `diff_id` | その差分の識別子 |
| `intent` | **その変更に至った会話の要約。必須である。** 変更履歴にそのまま残り、あとから訂正する手段は無い |
| `operations` | 操作の配列 |

操作1つに書けるキーは `op` / `table` / `field` / `view` / `changes` / `workflow` / `function` /
`theme` / `roles` で閉じている。ここに無い名前を書くと差分ごと断られる。

### 実地で繰り返し外されるところ

- **同じキー名でも中身の形が違う。** `add_table` の `table` は **表の定義そのもの** だが、
  `add_field` / `remove_field` / `remove_table` / `change_table` / `change_field` の `table` は
  **表のIDの文字列** である。`field` / `view` にも同じ非対称がある。
- **自動処理と計算の島はさらに違う。** `add_workflow` / `update_workflow` の `workflow`
  (および `add_function` / `update_function` の `function`)は定義そのものだが、
  `remove_workflow` の `workflow` と `remove_function` の `function` は `{ "id": "..." }` という
  **オブジェクト** である(文字列を書くと断られる)。
- **並べ替えは配列である**(`[{ "field": ..., "order": "asc" }, ...]`)。キーが1本でも配列で書く。
- **操作は前から順に畳み込まれる。** あとの操作は前の操作の結果に対して書く。
- **画面から参照されている表・項目は、参照を外してからでないと消せない。**
  項目を消す前に `update_view` で列・絞り込み・並べ替えから外す。
  表を消す前には、その表を指す画面を `remove_view` で消す(同じ差分に並べてよい)。
- **`update_view` で差し替えられるキーは決まっている。** 対象の表や画面の種別は変えられず、
  関連する行の一覧も差し替えられない。変えたいときは `remove_view` してから `add_view` で作り直す
  (このとき行は失われない)。
- **`set_theme` / `set_roles` は全置換である。** 1つ変えるにも全部を書く。
  とくに `set_roles` は、書かなかった役割の宣言ごと消える。**書く前に今の役割を読み、
  在るものを全部書き写してから変更を加えること。**

---

## 画面の種類と書き方

### 入力画面(`form`)

行を作る・直すための画面。`fields` に並べる項目を書く。

| キー | 何を決めるか |
|---|---|
| `fields` | 並べる項目 |
| `field_groups` | 項目をまとまりに分けて見出しを付ける |
| `modal` | 小窓として開くかどうか |
| `after_save` | 保存したあとに移る画面 |
| `reference_pickers` | 参照項目ごとに選ばせ方を上書きする |
| `preset_label_placement` | 項目名と値の向き。`inline` / `stacked` |
| `preset_field_columns` | 項目の段組数。`1` / `2` |
| `preset_density` | 詰まり具合。`comfortable` / `compact` |

### 一覧画面(`list_view`)

| キー | 何を決めるか |
|---|---|
| `columns` | 出す列 |
| `sort` | 並べ替え(配列。先頭が第1キー) |
| `filter` | 絞り込み。`and` / `or` / `not` で組み立て、葉は `equals` / `contains` / `gte` / `lte` / `in` |
| `search_fields` | 検索の入力欄を出し、当てる列を決める(1本以上8本まで。`text` と `long_text` だけ) |
| `page_size` | 1ページの件数。`10` / `20` / `50` / `100` のいずれか。**それ以外の数は書けない** |
| `sum_field` | 一覧の下に合計を出す列 |
| `actions` | 一覧から他の画面へ渡る操作ボタン |
| `menu_listed` | メニューに並べるかどうか |
| `preset_column_align` | 列ごとの文字寄せ。`left` / `center` / `right` |
| `preset_column_width` | 列ごとの幅の段階。`narrow` / `standard` / `wide` |
| `preset_pager_position` | 件数表示とページ送りの位置。`top` / `bottom` / `both` |
| `preset_list_shape` | 器の形。`table` / `card` |
| `preset_image_size` | 画像の大きさ。`thumbnail` / `medium` / `original` |
| `preset_text_preview` | 長文の切り詰め。`short` / `standard` / `long` / `full` |
| `preset_density` | 詰まり具合。`comfortable` / `compact` |

**検索は部分一致だけである。** 前方一致・正規表現・あいまい検索・複数語の AND・語の分割は無い。
打った語はマニフェストに保存されない。画面の絞り込みは外れず、検索の条件と AND で結ばれる。

**今何ページ目かはマニフェストに入らない。** ページ位置は読み取りの引数であって、宣言ではない。

### 詳細画面(`detail_view`)

| キー | 何を決めるか |
|---|---|
| `fields` | 出す項目 |
| `field_groups` | 項目のまとまりと見出し |
| `related` | 関連する行の一覧。相手の表・辿る参照項目・出す列を書く。**1ホップだけである** |
| `actions` | 操作ボタン |
| `after_save` | **値の書換ボタン(`actions` の `set`)の書込が成立したあとに移る画面。** 詳細画面に「保存」という出来事は無いので、`run`(自動処理の起動)の後は移らない。**画面に1本で、ボタンごとには書けない** |
| `after_delete` | **削除が成立したあとに移る画面。** 指せるのは一覧画面(`list_view`)か集計表(`report_view`)だけで、詳細画面・入力画面を指した差分は拒否される(消した行を開こうとするため)。書かなければ今日どおり、同じ表の一覧 → 無ければ画面一覧へ戻る。**確認のしかたもボタンの文言も指定できない** |
| `preset_label_placement` / `preset_field_columns` / `preset_image_size` / `preset_text_preview` / `preset_density` | 見せ方 |

### 一続きの流れ(`flow`)

一覧画面・入力画面・詳細画面に置ける。**「カート → お届け先 → 確認 → 完了」のように、
順番の決まった一続きの流れの何段目かを、画面が1本だけ宣言する。**

| キー | 何を決めるか |
|---|---|
| `id` | 流れの名前。**同じ名前を書いた画面どうしが1つの流れになる**(画面のIDではない。複数の画面が同じ名前を持つのが普通である) |
| `step` | その流れの中の位置。**1から始まる整数**。同じ流れの中で位置が重なっている差分、位置が抜けている差分、1から始まっていない差分は拒否される |
| `kind` | 段の種類。`input`(入力の段)か `confirm`(確認の段)の**2つだけ**。`confirm` を書けるのは詳細画面だけである |

**集計表(`report_view`)には書けない。** 集計表は流れの段になれない。

**書かなければ今日どおりである。** 保存の後の行き先は `after_save` の宣言か、書いていなければ既定の規約に倒れる。

**段の名前・アイコン・進み具合はここでは1つも指定できない。** 段のあいだで未確定の入力を持ち回すこともできず、
別のアプリの画面や外部のURLを段にすることもできない。

**段を宣言すると、書かなくても次の2つが自動で出る。** **どちらも出し方・文言・位置を宣言する口は1つも無い。**

- **いま何段目 / 全部で何段。** `3段目 / 全5段` の形で、段を宣言した3種別すべてに出る。
  **出るのは2つの数だけで、段の名前・種類・進み具合の割合は1文字も出ない。押せない**(他の段へ飛べない)。
  **段を宣言していない画面には1ピクセルも出ない。**
- **「戻る」。** **段を宣言した入力画面(`form`)にだけ出る**(一覧画面と詳細画面には出ない)。
  **【2026-08-21 訂正。上の1行は1文字も消していない】** **その字面は今日は偽である。**
  **「戻る」は、段を宣言した画面には種別を問わず出る** —— 一覧画面にも、詳細画面にも、
  入力画面にも、それぞれ1つ出る。**今日の正はこの訂正のほうである。**
  **出ないのは「段を宣言していない画面」と「重ねて出す画面」の2つだけである。**
  押すと**履歴を1つ戻る。戻り先を宣言する口は1つも無い。**
  **重ねて出す画面(`modal: true`)には出ない**(閉じる操作が既に同じことをしている)。
  **段1でも出る**(そのときの行き先は流れの外である)。
  **URLを直接開いた場合は履歴に前の項目が無く、押しても何も起きない。**

**画面の名前に段番号を手で書かないこと。** 同じ数が2箇所に並び、位置を入れ替えたときに名前だけがずれる。
**そのずれで赤くなる検査は1本も無い。**

**いま何段目かは、同じ流れの画面を位置の順に並べたときの順位である**(欠番の残る古い定義では `step` の値とずれる)。
**全部で何段かは見る人によって変わる** —— **ログインしている人には、その人に開けない段も総数に入る。**
**ログインしていない人には、見せると決めた段だけが総数に入る**ので、同じ流れが小さい数に見える。

### 操作ボタン(`actions`)

詳細画面と一覧画面に置ける。1つのボタンに書けるのは次のとおり。

| キー | 何を決めるか |
|---|---|
| `id` | ボタンの識別子。**役割の規則から名指しするにはこれが要る** |
| `name` | 表示名 |
| `form` / `view` | 押したときに開く画面 |
| `prefill` | 開いた先の項目に、今の行の値を入れておく |
| `set` | 開いた先の項目に、決まった値を入れておく |
| `visible_when` | 出す条件(葉1つ分の条件式) |
| `run` | 押したときに動かす自動処理 |

### 逃げ道(任意の CSS)

選べる値の外の見た目にしたいときの行き先である。**AI は CSS の本文をまったく書けない。**

- **本文を書けるのは持ち主だけ** である。ブラウザの専用画面から、資産名・CSS 本文・
  当ててよい画面の集合を入れて発行する。**当ててよい画面を書かない発行・空の集合・全許可は断られる。**
- AI にできるのは **申請** と、発行済み資産への **参照**(資産名と内容のダイジェスト)を画面に書くことだけ。
- 未発行・作用域の外・実体が無いときは、黙って効かずに済ませず、画面に理由が出て遮断される。
- **持ち主が資産を失効させると、取り消しても戻らない。** 参照は戻るが実体が無いので遮断される。
- 逃げ道はアプリの画面の内側にだけ当たる。プラットフォームの外枠には漏れない。
- **要件ドキュメントには1文も載らない。** 逸脱の集計にも数えられない。

---

## 見た目(テーマ)

アプリ全体の見た目は `set_theme` で **丸ごと** 差し替える。部分更新の操作は無いので、
1つ変えるにも 25 のスロット全部を書く。

スロットの内訳は、配色(`--color-text` など)・書体(`--font-family-base`)・
文字寸(`--font-size-note` / `--font-size-secondary`)・行間・
余白の目盛り(`--space-1` から `--space-6`)・枠線の太さ・角丸・影・
フォーカスリングの色と太さ・詳細画面の項目名の幅・ログイン画面の最大幅である。

- **読めない配色は断られる。** 本文 4.5:1 / 非テキスト 3:1 のコントラスト検査を通らないと、
  差分全体が断られる(部分適用は無い)。
- **この製品の既定の配色は、それ自身の検査を通らない。** 既定の区切り線の色が基準に届かないので、
  **「今の見た目をそのままテーマとして持ち出す」ことはできない。**
- **当たらない範囲がある。** ボタンや入力欄の文字色と地色はブラウザの既定のまま、
  プラットフォームの外枠・読み込み中・エラー画面にも当たらない。
- **画面ごとの配色は無い。** テーマはアプリ単位である。値によって色を変える条件付き書式も無い。
- **取り込めるのはスロットの値だけである。** URL を渡しても取りに行かないし、ファイルも読まない。
  CSS のテキストを読み解く口は無い。
- **テーマは要件ドキュメントに1文も載らない。**

日常の言葉での微調整(「もっと余白を広く」など)は、あらかじめ決まった有限の対応表でだけ受け付ける。
対応表は 7つの軸(余白・文字寸・行間・枠線の太さ・角丸・影・フォーカスリングの太さ)で、
**表に無い言い方は断られる。** 配色は対応表が1つも持たないので、色は人と決めることになる。

---

## 役割と見える範囲

### 役割の宣言

`set_roles` で `app.roles` を丸ごと書く。1件以上12件まで。

| キー | 中身 |
|---|---|
| `id` | 役割の識別子。小文字英数と `_` の1〜32文字 |
| `name` | 表示名(任意。書かないと識別子がそのまま画面に出る) |
| `rules` | その役割に許す規則の配列(1本以上) |

- **`owner` / `editor` / `viewer` の3つは宣言から落とせない。** 落とすと差分ごと断られる。
- **`anonymous`(ログインしていない相手)も予約された名前である。**
- **`owner` は、アプリ自体を書き換える規則と、役割を書き換える規則の2本を持ち続けなければならない。**
- 同じ識別子を2回書けない。
- **宣言を「無い状態」へ戻す手段は無い。** 空の配列は断られる。

### 規則の形

| キー | 中身 |
|---|---|
| `target` | 対象。`table` / `field` / `view` / `action` / `app` / `role` |
| `table` / `field` / `view` / `action` | 対象の名指し。`target` によって要るものが決まる |
| `can` | 許す動詞。`read` / `write` / `delete` |
| `when` | 条件(表と項目の規則にだけ書ける) |

対象ごとに書ける動詞は決まっている。

| 対象 | 書ける動詞 | 名指しに要るもの |
|---|---|---|
| 表 | `read` / `write` / `delete` | 表 |
| 項目 | `read` / `write` | 表と項目 |
| 画面 | `read` だけ | 画面 |
| ボタン | `read` だけ | 画面とボタンの `id` |
| アプリ自体 | `write` だけ | 名指し不要 |
| 役割 | `write` だけ | 名指し不要 |

**条件(`when`)は `and` / `or` / `not` で組み立てる。** 葉に書けるのは次の形である。

- 項目の値が定数と等しい(`equals`)
- 項目の値が、いま要求している人と等しい(`equals_current_user`)
- 項目が空である(`is_empty`)

**画面とボタンの規則に条件は書けない。**
**ログインしていない相手の規則は狭い** —— 項目を対象にできず、動詞は `read` だけである。

### 既定は閉じている

- **規則から名指しされていない表・画面・ボタンには、誰も届かない。**
  行は SQLite の中に残ったまま、一覧が0件になり、作成は 403 になる。**持ち主も例外ではない。**
- **項目だけは今日も全許可である。**
- `add_table` / `add_view` は、畳み込みの瞬間に `owner` / `editor` / `viewer` へ
  その対象の規則を自動で足す。**規則を持たない対象を作りたいなら、`set_roles` を同じ差分の最後に置く**
  (全置換なので、自動で足された規則ごと上書きされる)。
- **届かなくなった行を拾う口が1本だけある。** 運営者専用の読み取り専用の一覧である。
  **見えるだけで、直すにも消すにも `set_roles` で規則を書き戻すしかない。**

### 最も踏みやすい代償

**表に対する `read` の規則を1本書くと、その役割の人にはその表の他人の行が見える。**
持ち主ごとの分離(`st_owner`)の壁を越える。**運営ではない役割にうっかり書くと、その人に全員分の行が見える。**
書く前に、誰に何が見えるようになるのかを人に説明して確認すること。
なお、開くのは読み取りだけである。見えている他人の行への更新・削除は通らない。

### ログインしていない相手

- 画面の規則で `anonymous` に `read` を与えた画面だけが、ログイン前の一覧に並ぶ。
- **どの規則からも名指しされていない画面は、ログインしていない相手にも並ぶ。** 隠したい画面は
  1画面ずつ規則で名指しすること。
- ログイン前でも読める口が1本だけある。返るのは **アプリ名・ログイン前に見せると決めた画面の識別子と表示名・配色** だけで、
  項目名も表も自動処理も画面の中身も返らない。**この口だけでは画面は描けない。**

### 最初の1人

**アプリを作った時点では、まだ誰も登録されていない。**
最初にブラウザから登録した人が持ち主(`owner`)になる。以降に登録した人は閲覧者から始まる。
したがって、AI が先に登録を済ませてしまうと、本来の持ち主が管理者になれない。
**空のアプリの器だけを AI が作り、登録は人が行い、その人として AI を繋ぎ直してから中身を作る** のが正しい順である。

### 会員登録が終わった直後に開く画面

ブラウザから自分で会員登録を済ませた直後、画面が1枚だけ自動で開くことがある。

- 開く候補になるのは、**`st_owner` を宣言した表**(= 個人ごとに行を持つ表)**の入力画面(`form`)のうち、
  その立場が規則で読めるもの**だけである。
- **候補がちょうど1本のときだけ、その画面が開く。**
- **候補が2本以上あるときは自動で開かない。** 登録の直後に出る案内に、候補が全部並ぶ。
  **どれを先に出すかも、並ぶ順序も、定義に書く口が1つも無い。**
- **候補が0本のときは何も起きない。**
- **画面一覧に出さない設定(`menu_listed: false`)にした入力画面でも、候補になる。**
  候補の選び方は掲載の設定を見ていないので、**一覧から隠した画面が、登録の直後の着地先になることがある。**
- **一度きりである。** そのあと自分で別の画面へ移っても、引き戻されることはない。
- **アプリ1本だけを配って動かす版**(定義を育てる画面を持たない版)**では、この案内そのものが出ない。**
  **自動で開くところだけが効く。**
- **行き先をアプリの定義で名指しする書き方は1つも無い。** 上の選び方はプラットフォーム側に固定されている。

開くのは入力画面であって、**保存を押さなければ行は1件も作られない。**
**その人の行が既にあるかどうかは見ていない** —— 同じ表に行が二重に作られることを止める仕組みは無い。

**2026-08-22 訂正。上の箇条と本文を1行も消していない。** 上の3点は今日の正ではなくなった。

- **開く候補の条件が増えた。** 上の1点目の「だけである」は、今日は成り立たない。
  今日の候補は、次の5つを**全部**満たしたものだけである。
  1. 入力画面(`form`)であること。
  2. その画面が使う表が `st_owner` を宣言していること(= 個人ごとに行を持つ表)。
  3. その立場が、規則でその画面を読めること。
  4. **その立場が、規則でその表に行を新しく作れること。** 閲覧者(`viewer`)や
     ログインしていない相手は、ここで落ちる。**見ているのは、入力画面の保存ボタンを
     先回りして消すのと同じ判定である** —— 遮断そのものは今日もサーバ側が行う。
  5. **その表を1件だけ読んでみて、その人から見えている行が1件も無いこと。**
     読み取りが**断られた**(権限が無いと返ってきた)候補も、ここで落ちる。
     それ以外の失敗(通信できない・見つからない・サーバの不調)では落とさない ——
     読めなかった候補は残す。
- **5点目は「その人の行があるか」ではなく「その人から見えている行があるか」である。**
  持ち主の欄が空の行(全員に見せている行)は誰にでも見えるので、
  **自分の行が1件も無くても候補が落ちる。**
  逆に、行ごとのアクセス権などで自分の行が見えていないときは、行があっても落ちない。
- **断られて落ちるのは、その画面を名指しして読んだときだけである。**
  表の一覧そのものは、その立場に読み取りの規則が1本も無くても断られず、0件を返す。
- **候補が落ちた理由は、画面に1文字も出ない。** 「もう登録済みです」のような文面は1つも無い。
- **配って動かす版でも、この案内が出る。** 上の「この案内そのものが出ない」は、今日は偽である。
  定義を育てる版と同じ案内を、同じ位置(画面の上端の帯の直後)に描く。
  候補が0本のときに何も描かないのは、今日も両方の版で同じである。
- **最後の1文は、前半だけが偽である。**
  「その人の行が既にあるかどうかは見ていない」は、今日は成り立たない(上の5点目)。
  **後半の「同じ表に行が二重に作られることを止める仕組みは無い」は、今日も真である** ——
  保存の口には何も足していない。止めているのは案内と自動で開くところだけであって、
  自分で入力画面を開けば、2件目も3件目も今日どおり保存できる。
- **2026-08-22 再訂正(この節の末尾に足す。上の4点目を1文字も消していない)。**
  上の4点目「その立場が、規則でその表に行を新しく作れること。」は、**言い方が正確でない。**
  **その表そのものに「書いてよい」と決める規則は、ここでは1度も読まれていない。**
  実際に見ているのは、次の2つだけである。
  1. **その立場が、閲覧者(`viewer`)でも、ログインしていない相手でもないこと。**
     これは立場の名前だけで決まり、表の宣言を1つも見ていない。
  2. **その表に行を作るボタンの規則が、その立場を拒んでいないこと。**
     **その表を書き先にするボタンが1本も無ければ、この関門は素通りする。**
  **落ちる相手(閲覧者・ログインしていない相手)は4点目に書いたとおりで、1つも変わらない。**
  **「入力画面の保存ボタンを先回りして消すのと同じ判定である」も、今日どおり真である。**
  遮断そのものを行うのが今日もサーバ側であることも、変わっていない。

---

## 行ごとのアクセス権

役割の規則が「面」で決めるのに対し、こちらは行1件ごとの「点」で決める。
表の定義に `access_control` を書いた表にだけ効く。

| キー | 中身 |
|---|---|
| `enabled` | 有効かどうか |
| `permissions` | 権限名の一覧(1件以上8件まで)。名前ごとに読む・書く・消すを決める |
| `creator_permission` | 行を作った人に自動で入る権限名 |
| `grant` | 付与を記録する表と、その中のどの列が何を表すか |
| `members` | 利用者の表と、ログインしている人を指す列 |
| `groups` | グループの表 |
| `inherit_from` | 親から引き継ぐときに辿る参照項目(2本まで) |

- **グループはプラットフォームの機能ではない。** アプリが普通の表として作り、宣言で名指しする。
  **規約から外れた作り方をすれば効かず、グループの入れ子は書けない。**
- **権限名に `owner` / `editor` / `viewer` / `anonymous` は使えない。**
- **宣言した表では、運営の役割も付与を迂回しない。** 付与が1件も無い行は持ち主からも見えない。
- **親に付けた付与を子・孫へ引き継げる。** 辿るのは5段まで。上限に当たった要求は
  黙って打ち切らずに断られる。
- **利用者の表に自分の行が無い人は、その表に行を1件も作れない。** 最初の1行は人が足す。
  そこに書くのは **ログイン名ではなく利用者ID** である。取り違えると、まったく同じ断り方をされる。
- **面と点は OR である。** 片方が許せば通る。**閉じたいときは両方で閉じること。**
- **持ち主ごとの分離(`st_owner`)と付与は AND である。** 併用すると、付与があっても他人の行は読めない。

---

## 自動処理(ワークフロー)

定義を1本足すと、**当てたあとも動き続ける。**
`id` / `name` / `trigger` / `actions` / `history_table` の5つがすべて必須である。

### きっかけ(`trigger`)

| `type` | 必須キー | いつ動くか |
|---|---|---|
| `on_create` | `table` | その表に行が作られたとき |
| `on_update` | `table` | その表の行が更新されたとき |
| `schedule` | `at`(`{ "hour": 0〜23, "minute": 0〜59 }`) | 毎日その時刻に1回 |
| `manual` | `table` | 画面のボタン(`run`)が押されたとき |

- **時刻起動には `table` を書いてもよい。** 書くとその表の行を1件ずつ処理する。
  書かなければ、行を対象にせず1回だけ動く(このときトリガー元の行が無いので、行の値を参照できない)。
- **1回の発火で処理できるのは 1000 行までである。** 超えるとその発火は1行も処理せずに打ち切られ、
  履歴に失敗が残る。**次回には持ち越さない。**
- **経過時間の述語 `older_than`** を、`table` を書いた時刻起動にだけ書ける。
  形は `{ "field": <date 型の項目>, "days": 1〜3650 }` の平たい1形で、
  その日付から `days` 日以上前の行だけが対象になる(境界は「以上」)。
  **これは時刻専用の1形であって、一般の大小比較ではない。**
- **cron 式・曜日・月・「5分ごと」は書けない。** 書けるのは時と分だけである。
- **削除・画面表示・エラー・外部からの受信をきっかけにはできない。**
- **内部の表(`_apps` / `_changelog`)はきっかけに指定できない。**

### 動作(`actions`。1件以上必要)

| `action` | 必須キー | 何をするか |
|---|---|---|
| `create_record` | `table` / `values` | 指定した表に1行作る |
| `update_record` | `table` / `values` / `target` | `target` が指す行を更新する |
| `call_external` | `connection` ほか | 外部の宛先へ送る。**持ち主が発行した接続の範囲でだけ起こり得る** |
| `ai_transform` | `capability` ほか | 機能の中で AI を呼び、結果を書き戻す |
| `run_function` | `function` ほか | 計算の島を動かす |

**削除する動作・通知する動作・メールを送る動作は無い。** 外へ出すのは接続の下での送信だけである。

### 動作に付ける条件(`when`)

条件を満たしたときだけその動作が走る。**ワークフロー全体ではなく動作ごとのガードである。**
**形は `{ "field": …, "equals": … }` の等値だけ** で、大小比較・論理結合・`else`・入れ子・
関数呼び出しは1つも書けない。
2つの条件の AND は、述語を結合するのではなく、きっかけ側で行を絞り、動作側で `when` を書いて2層に分ける。

### 書ける値

`values` と `target` に書けるのは、次のどちらか一方だけである。

1. **リテラル文字列**($ で始まらない文字列。ただし波括弧を含むものは差分ごと断られる)
2. **`"$record.<項目ID>"`**(および `"$record._id"`)

| 書きたいこと | 書けるか |
|---|---|
| トリガー元の行の値を写す | **書ける** |
| 二重波括弧のような差し込み記法 | **書けない。** 置換されないどころか、差分ごと断られる |
| 固定文字列と行の値をつなげる(「新しい申し込み: <名前>」) | **書けない。** 値の全体がリテラルか参照かのどちらかである |
| 演算・条件式・関数呼び出し | **1つも無い** |
| カウンタを増やす | **書けない**(値を計算する手段が無い) |

**書き込み先の型にも制約がある。**

| 型 | 書けるか |
|---|---|
| `text` / `long_text` | **書ける。自動処理に書かせたい列はこの型にしておくこと** |
| `date` | 書ける。ただし ISO8601 形式の文字列だけ |
| `select` | 書ける。ただし `options` に一致する文字列だけ |
| `boolean` / `number` | **書けない** |

`date` と `select` は **形がずれても差分は通り、動いたときに書き込みで弾かれる。**
失敗は履歴の `error` 列にだけ残る。

`update_record` の `target` に書けるのは、トリガー元の行自身(`"$record._id"`)・
参照項目が指す相手の行(`"$record.<reference 項目>"`)・特定の UUID の3つである。
参照型でない項目を `target` に書くと、黙らずに失敗する。
**実行時にその相手の行が実在するかは保証されない**(検査は差分を当てるときだけ)。

### 誰として動くか

`act_as` に `"$record.<項目>"` の形で書くと、その行が指す人として動く。
**行の持ち主を持つ表に書き込む自動処理では、これが無いと書き込みが通らないことがある。**
読めていても書けないという形で出る。

### 実行履歴の表は自分で作る

**履歴の表は自動では作られない。** `add_workflow` と **同じ差分の中で `add_table` して** 用意する
(存在しない表を指した差分は全体が断られる)。次の5列が要る。

| 項目ID | 型 | 入る値 |
|---|---|---|
| `ran_at` | `date` | 動いた時刻 |
| `workflow` | `text` | 動いた自動処理の識別子 |
| `trigger_type` | `text` | きっかけの種別 |
| `status` | `text` | `success` または `failure` |
| `error` | `long_text` | 失敗した動作の説明(成功時は空) |

- **`status` と `trigger_type` に `select` 型を使ってはいけない。**
  許可値の外は弾かれるが、**その失敗を書く先がまた同じ列** なので、失敗の記録自体がどこにも残らなくなる。
- 検査は「ちょうどこの5列」ではなく「少なくともこの5列が正しい型で在ること」である。列は足してよい。
- **実行1回につき1行**書く(動作1つにつき1行ではない)。

### 暴走を止める仕組み

| 仕組み | 挙動 |
|---|---|
| 連鎖の深さの上限(20段) | 21段目は動かず、失敗の行が履歴に残る |
| 同じ行の再発火の抑止 | 同じ行は1回の操作で2度発火しない。抑止された発火も失敗として残る |
| 履歴の表は発火させない | 履歴の表への書き込みはどの自動処理も起こさない。**したがって履歴の表をきっかけの表に持つ自動処理は、差分としては通り、永久に動かない**(エラーにならず静かに動かない) |

### 時刻起動の「今日もう動いたか」

**判定は履歴の表を読んで行われる。他のどこにも記録は無い。** ここから次が出る。

1. **履歴の行を消すと、その日のうちに二重に動く。**
2. 履歴の表の `workflow` 列と `ran_at` 列が欠けていると動かない。
3. サーバが止まっている間に時刻が過ぎた場合、**同じ日のうちに起動すれば遅れて動く。
   日をまたいだ分は失われ、取り戻さない** —— 「毎日1回まで」であって「必ず毎日1回」ではない。
4. **判定は成否を見ない。** 失敗した発火も、上限で打ち切られた発火も「動いた」である。
   したがって処理しきれなかった行は、その日のうちには再試行されない。

---

## 計算の島(`function`)

条件式や決まった演算では書けない計算を閉じ込めるための、唯一の逃げ道である。

- **本物の隔離された場所で動く。** 外部通信も、ファイルの読み書きも、環境変数も無い。外に一切触れない。
- **単体では何もしない。** 自動処理の `run_function` から呼ばれたときだけ動く。
  **島を直接叩く差分操作も、止める・再開する語彙も無い。**
- 資源の上限は、実行時間(既定 1000 ミリ秒)・メモリ(既定 64 MiB)・入力の大きさである。

### 入力(`input`)

1つだけ書く形に加えて、**最大5つまで並べた配列** が書ける。入力の元は `table` / `view` / `record` である。

- **突き合わせ(join)はカーネルが行わない。** 渡るのは各表の全行で、
  どの行がどの行に対応するかは島の JavaScript が書く。
- **参照を1ホップも辿らない。** 相手の「行だけ」を狙って渡すことはできず、相手の表を丸ごと渡す。
- **配列は名前の無い順序である。** 並べ替えると島は黙って壊れる。

### 出力(`output`)

書き方は2つあり、**排他** である。

1. **`output_table` へ書く。** その表を **全置換** する(既存の全行を消してから入れる)。
   出力が列の型に合わなければ、**何も書かずに失敗の理由を残す。**
2. **`write_back: "$record"`** でトリガー元の行へ書き戻す。

出力の形は、項目の一覧(`fields`)を宣言するか、`ops` を立てて更新操作の配列を返すかのどちらかである。

**時刻起動では、書き戻しも AI の呼び出しも断られる。**

---

## 集計表(`report_view`)

「どの項目で束ねて、何を数えるか」を宣言として書く画面である。

| キー | 中身 |
|---|---|
| `group_by` | 束ねるキー(1〜3本)。`field` に項目IDか、行そのものを表す `_id` を書く。日付は `granularity` に `day` / `week` / `month` を書いて束ねられる |
| `aggregates` | 集計(1〜3本)。`type` は `sum` または `count`。**平均・最小・最大・中央値は書けない** |
| `join` | 突き合わせる相手(最大4件)。実在する `reference` 項目1本(`via`)でだけ結ぶ |
| `filter` | 絞り込み。**起点の表にだけ掛かる** |
| `sort` | 群の並べ替え。束ねるキーか集計のどれかを選ぶ |
| `chart` | グラフ。`bar` または `line`。省略すると棒 |

### 上限と、その当たり方

| 上限 | 実数 | いつ止まるか |
|---|---:|---|
| 結合できる表(起点を含む) | 5 | 差分を当てるとき |
| 参照の鎖の段数 | 3 | 差分を当てるとき |
| 群にまとめる前に読む行(表ごと) | 10,000 | 集計を求めたとき |
| 作る群の数 | 10,000 | 集計を求めたとき |

- **どれもその値ちょうどは通り、超えたときだけ断られる。**
- **上限に当たると、部分的な結果は返らない。** 本当に0件のときは群0・合計0が返るので、区別はつく。
- **後ろ2つは差分を当てるときに検査されない。** 差分は通り、**行が増えた日に画面が開かなくなる。**
- **上限は宣言から変えられない。** マニフェストからも読めない。

### 集計表の限界

- **結合の条件式・射影・計算式は書けない。** 結合先の列で絞り込むこともできない。
- **一対多の結合が1本でも入ると、起点の行はその子の数だけ数えられる。**
- **相手が無い行は落ちない**(売れていない商品が 0 で並ぶ)。
- **母集団には、その画面を開いた人が読める行だけが入る。** したがって **見る人によって数が変わる。**
  ただし **結合先の表は素通しで全件読まれる。**
- **行そのもの(`_id`)で束ねた群の見出しには、生の UUID が出る**(名前ではない)。
- **集計表の結果を他の画面から参照することはできず、自動処理のきっかけにもできない。**
- **グラフは集計表の中だけに出る。** 常設の領域には出せず、ダッシュボードにはならない。
  円・散布図・積み上げ・面は書けず、どの集計を描くかも選べない。

**集計を書く道は2つある。** 宣言の道(この画面)と、島で数えて `output_table` に書き、一覧で見せる道である。
**正は宣言の道である。** 島の道を選ぶのは、結果を他の画面から参照したいとき・
自動処理のきっかけにしたいとき・見る人によらず同じ数を出したいときの3つに限る。
**2つの道が同じデータに違う数を返すのは正常である。**

---

## 外部とのやりとり

外に出す・外から受けるものは、すべて **持ち主が発行した許可(capability)の下でだけ** 起こる。
AI にできるのは **申請** までで、**発行できるのは持ち主のブラウザ操作だけ** である。
この非対称は説明文ではなく、発行する経路が存在しないことで固定されている。

| 種別 | AI ができること | 持ち主だけができること |
|---|---|---|
| 外部への送信(接続) | 申請 | 発行。宛先の範囲を決める |
| 機能の中での AI 呼び出し | 申請 | 発行。提供元・モデル・1日の上限を決める |
| 外部からの受信口 | 申請 | 発行 |
| 逃げ道の CSS 資産 | 申請(本文は渡せない) | 発行。本文と当ててよい画面を決める |

- **秘密の値は保管しない。** 取得元の参照だけを持ち、使うときに解決して、どこにも残さない。
- **接続が無い送信・範囲の外への送信は、実行の層で遮断される。** 1文字も送られない。
- **機能の中の AI 呼び出しは、既定では発行されていないので呼べない。** 1日の上限と連鎖の停止があり、
  上限超と連鎖停止は使用量の記録に残る。出力が書き戻し先の型や選択肢に合わなければ、決めておいた既定値が入る。
- **受信の署名検証は書き込みの直前に行われる。** 合わなければ何も書かずに断る。
  通れば宛先の表に1行作る。同じ通知が二度来たら重複は吸収するが、
  **欠落と順序の逆転は補償しない。**
- **受信口には、再発行・失効・一覧の口が無い。** 発行はできるが、止める手段が製品に無い。

---

## AI から使う口(MCP のツール一覧)

AI が呼ぶものなので、人が直接使うことは通常ない。何ができるかの見取り図として載せる。

### 読むだけの道具

| 道具 | 用途 | 注意 |
|---|---|---|
| `list_apps` | アプリの一覧 | 他の道具に渡す識別子はここで取る |
| `get_manifest` | 今の定義の全部 | 差分を組む前に必ず現状確認に使う。内部の表の列定義も一緒に返る |
| `get_changelog` | 変更履歴 | 古い順に返る |
| `get_preview_url` | ブラウザで開ける URL | 基底は環境変数であり、**サーバの実際の待ち受け先を確認していない** |
| `list_records` | 表の行 | 件数は常に表全体の件数である。集計の宣言は1つも見ない |
| `preview_undo` | 取り消したら何が起きるか | **「表が定義ごと消える」ことは明示的には返らない** |
| `preview_redo` | やり直したら何が戻るか | |
| `dry_run_diff` | 差分を当てたら何が起きるか | 本体は何も変わらない。**書き込み先の列の型は見ていない**。**【2026-08-25 追記】読む道具だが、`apply_diff` と同じ権限が要る**(役割の規則の `target: "app"` / `can: ["write"]`)。**規則を1本も書いていないアプリでは持ち主でも断られる。****返るのはそのアプリ自身の表だけで、ログイン・セッション・役割の付与を保管する予約テーブルは載らない** |
| `generate_requirements_doc` | 今の定義から要件ドキュメントを組み立てる | AI 生成ではなく決まった型どおりの組み立て。**見た目は1文も載らない** |
| `report_drift` | 画面ごとの調整と見た目が同じ形で繰り返されている数を数え、まとめ方を提案する | **提案するだけで、何も書き込まず、何も止めない。** 逃げ道は1件も数えない |
| `read_report` | 集計表を1枚読む | |

### 書く道具

| 道具 | 用途 | 注意 |
|---|---|---|
| `create_app` | 中身が空のアプリを作る | 識別子を省くと名前から自動で採番される |
| `apply_diff` | 差分を当てる。**定義を変える唯一の手段** | 当てる前に自動でスナップショットを取る。**断ったときはアプリは何も変わっていない**(部分適用は無い) |
| `undo` | 直前の適用を取り消す | **戻るのは定義だけではなくデータベース全体である** |
| `redo` | 直前の取り消しをやり直す | **「やり直せるから取り消しは安全」ではない** |
| `insert_sample_data` | 表に複数行を入れる | 行ごとに独立に検証され、**一部の行だけ失敗しうる。** 成功した行は残る |
| `update_record` | 行1件の部分更新。**入れたデータの誤りを直す唯一の手段** | **変更履歴に載らず、取り消しでも戻らない。** 条件でまとめて直す手段は無い |
| `delete_record` | 行1件を完全に削除する | **取り消せない。変更履歴に載らず、取り消しでも戻らない** |
| `write_records` | 複数行の作成・更新を1つのトランザクションで行う | **全成功か全失敗。** 版を指定すると衝突を検出する |
| `delete_app` | アプリを器・変更履歴ごと完全に削除する | **取り消せない。** バックアップは削除に連動しない |
| `request_connection` | 外部送信の接続を申請する | **申請するだけ。** 発行は持ち主だけ |
| `request_ai_capability` | 機能の中の AI 呼び出しを申請する | **申請するだけ** |
| `request_inbound_endpoint` | 外部からの受信口を申請する | **申請するだけ** |
| `request_custom_css` | 逃げ道の資産を申請する | **申請するだけ。** 入力に CSS 本文の引数が無い |

### 使ううえでの注意

- **名乗り(`ST_MCP_ACTOR`)が空だと、道具はすべて断られる。** 起動そのものは成功するので、
  「繋がっているのに何もできない」という形で出る。書き換えたら繋ぎ直すこと。
- **名乗りに証明は要らない。** これは認可であって認証ではない。設定に持ち主の名前を書けば、
  その全権がそのまま渡る。
- **絞られないものが残っている。** 定義の取得とアプリの一覧は名乗りで絞られず、
  取り消しとアプリの削除は、名乗った人が読めない行まで消せる。
  **AI の操作は監査の記録に1行も残らない。**
- **サーバと AI 側の口は別のプロセスである。** データの置き場所の設定が食い違うと、状態を共有できない。

---

## HTTP の口

### 行の読み書き(ログインが要る。していなければ 401)

| メソッド | パス | 内容 |
|---|---|---|
| `GET` | `/api/apps/:app_id/tables/:table_id/records` | 一覧。並べ替え・絞り込み・件数・位置をクエリで受ける |
| `GET` | `/api/apps/:app_id/tables/:table_id/records/:record_id` | 1件 |
| `POST` | `/api/apps/:app_id/tables/:table_id/records` | 作成(201) |
| `PATCH` | `/api/apps/:app_id/tables/:table_id/records/:record_id` | **部分**更新(全体置換の口は無い) |
| `DELETE` | `/api/apps/:app_id/tables/:table_id/records/:record_id` | 削除(204) |
| `POST` | `/api/apps/:app_id/batch` | まとめ書き。**1つのトランザクションで全成功か全失敗** |
| `POST` | `/api/apps/:app_id/files` | ファイルのアップロード |
| `GET` | `/api/apps/:app_id/files/:file_id` | ファイルの取得。**参照されていない・非公開・不在はすべて 404**(存在そのものを隠す) |

`st_public` を持つ公開の表への読み取りだけは、ログインしていなくても公開行を返す。
同じ URL への書き込みは 401 のままである。

### 定義まわり

| メソッド | パス | 内容 | ログイン |
|---|---|---|---|
| `GET` | `/api/apps` | アプリの一覧 | 要らない |
| `GET` | `/api/apps/:app_id/manifest` | 今の定義 | **要る**(未ログインは 401) |
| `GET` | `/api/apps/:app_id/public` | アプリ名・ログイン前に見せる画面・配色だけ | 要らない |
| `POST` | `/api/apps/:app_id/diffs` | 差分を当てる(201) | **要らない** |
| `POST` | `/api/apps/:app_id/undo` | 取り消し。**本文は読まない** | **要らない** |
| `GET` | `/api/apps/:app_id/undo/preview` | 取り消しの下見 | **要らない** |
| `GET` | `/api/apps/:app_id/changelog` | 変更履歴 | **要らない** |
| `GET` | `/api/apps/:app_id/requirements` | 要件ドキュメント | **要らない** |

**アプリの定義を書き換える口は、今も認証で守られていない。**
守っているのは `127.0.0.1` への待ち受けだけである。**ローカルホストの外に公開してはならない。**

### そのほか

- 認証は Passkey を優先し、パスワードも併用する。セッションは Cookie で持つ。
- 接続・AI 呼び出し・受信口の **発行は持ち主だけ**(それ以外は 403、未ログインは 401)。
- 外部からの受信は `/inbound/:endpoint_id`(`/api` の下ではない)。
- アプリの作成・削除・やり直しは HTTP からは行えない(AI 側の口だけである)。

### エラーの形

常に `{ "errors": [...] }` の配列で、1件でも配列である。要素は場所・文面・許可値・ヒントを持つ。

| コード | 意味 |
|---|---|
| `400` | 値が不正 / 読み取り専用の表への書き込み / クエリの解釈失敗 |
| `401` | 守られている口への未ログインのアクセス |
| `403` | 持ち主だけの操作への、それ以外からのアクセス |
| `404` | アプリ・表・行が無い。`/api` の下の未定義のパス。参照されていないファイル |
| `409` | 版の食い違い(同時更新の衝突) |
| `500` | 想定外の例外。本文に内部の情報は載せない |

---

## できないこと

**言い換えても実現できない。** 頼まれても正直に断り、書ける範囲での代わりの案を出すことになっている。
無駄足を避けるため、先に把握しておくこと。

**「誰にもできない」と「AI にはできない」は違う。** 逃げ道の CSS は持ち主にはできて AI にはできない。
表の中で書き分けている。

| できないこと | よくある言い方 | 代わりにできること |
|---|---|---|
| **任意の CSS と、選べる値の外のレイアウト**(**AI には** できない。**持ち主にはできる**) | 「CSS を当てて」「この項目をここに置いて」「3段組にして」 | AI が CSS を書き込める場所は無い。ピクセル座標・絶対配置・重ね順を指す語彙も無い。**画面ごとの見せ方は決まった軸の中からしか選べない。** 表示する項目と並び順なら画面の列と並べ替えで変えられる。**持ち主が資産として発行すれば、その画面には実際に当たる** |
| **画面ごとの配色 / 値ごとの見た目** | 「この画面だけ濃い色に」「未処理の行だけ赤く」 | **見た目はアプリ単位である。** 画面ごとに選べる中に配色は入っていない。条件付き書式も無い |
| **1ページの自由な件数・日付や数値の書式** | 「1ページ30件に」「日付を和暦で」 | 件数は `10` / `20` / `50` / `100` から選ぶ。**それ以外は書けない。** 罫線の見せ方・未設定の表し方・日付の表示形・真偽値の記号・数値の桁区切り・ボタンの体裁も語彙に無い |
| **OS 設定に追随する暗い配色** | 「ダークモードに」 | **語彙には無い。** 持ち主が逃げ道の CSS に書けば効く |
| **画面幅に応じた自由な組み替え** | 「スマホでも崩れないように」 | 外枠の余白などは2つの切り替え点で変わるが、**段組と列幅は画面幅に追随しない。** 切り替え点は固定で、幅の条件を書く場所が語彙に無い |
| **対応表に無い言い方での見た目の指定** | 「かっこよくして」「今風に」 | 決まった言い方だけを受け付ける。**表に無い言い方は断られる** |
| **行の中の計算列** | 「単価×数量の列を自動計算して」 | 語彙に計算列は無い。集計は集計表か、島で数えて別の表に書く |
| **好きな形のグラフとダッシュボード** | 「売上を棒グラフにして」「ダッシュボードを」 | グラフは集計表の中の棒と折れ線だけである。**集計表の外にグラフは出せない** |
| **多段の関連の展開** | 「関連の関連まで辿って表示して」 | 詳細画面の関連一覧は **1ホップだけ。** 孫・多段・参照の展開は無い。島に渡す入力も1ホップである |
| **突き合わせを伴う自由な集計** | 「区分Bの注文だけを商品ごとに集計して」 | 集計表の中でだけ表を突き合わせられるが、**絞り込みは起点の表にしか掛からない。** 結合の条件式も射影も書けない |
| **CSV の取り込み画面** | 「CSV をアップロードして取り込んで」 | 取り込み **画面** は無い。**製品は外部から何も取得しない**(ファイルを読む口も URL を叩く口も無い)。運べるのは会話に貼り付けた文字列だけで、**一度限りの手作業であり、同期にも定期取り込みにもならない。** 列の対応づけは AI が行うので、取り違えは静かに残る |
| **複数のテナントの分離** | 「1つのアプリで複数の店舗を分けて」 | 1アプリ=1つの持ち主が前提。テナントの境界を分ける仕組みは無い |
| **自動処理から真偽値・数値の列に書くこと** | 「既読フラグを立てて」 | **書けない。** 文字列の列に「済」と書く |
| **自動処理の手動での一斉実行・一時停止** | 「今すぐ全部流して」「しばらく止めて」 | 画面のボタンから1本動かすことはできる。止める語彙は無いので、定義を取り除いて後で書き直す |
| **自動処理の状態を見る画面** | 「動いているか見たい」 | 専用の画面は無い。**履歴の表や書き先の表に自分で一覧画面を作ると、初めて見える** |

### 権限が掛からない入口

役割の規則と行ごとの付与が効くのは、**ブラウザからログインして使う人が通る行の読み書きの経路** である。
受信口・登録と更新をきっかけに動く自動処理・計算の島・AI 側の口には、それぞれ判定が配線してある。

**判定を1つも通らない入口が1本だけ残っている** —— **決まった時刻に動く自動処理** である。
**同じ自動処理でも、登録で動けば判定が効き、時刻で動けば効かない。**
これを「すべての入口に権限が効く」と読まないこと。

そのほか、閉じていないところを名指ししておく。

- **利用者の表とグループの表の読み取りは絞っていない。** 書き込みは持ち主だけに絞ってあるが、
  AI 側の口・受信口・自動処理・島からは同じ表に書ける。
- **画面の側は付与を見ていない。** 先回りして隠す仕組みは無く、止めているのはサーバの応答である。
- **取り消しは付与を過去へ戻す。** 取り消した共有が復活し、そのことは何も告げられない。
- **登録画面で立場を自分で名乗れる。** 塞ぐと招待の口も消えるので、塞いでいない。

---

## 取り消しが効くもの・効かないもの

### 取り消しの正体

**取り消しは差分の逆適用ではない。** 「取り消す適用の直前に取ったスナップショットで、
**データベース全体を書き戻す**」操作である。定義の逆適用も、列の逆操作もしない。

**したがって、その適用より後に入れた行・直した行も一緒に消える。**

実際に観測された例として、自動処理を作った差分を取り消したところ、
自動処理の定義だけでなく **履歴の表(定義と行の両方)も書き先の表も消えた。**
数週間動いた履歴があっても同じことが起きる。

> **「自動化をやめたい」と「自動化が動く前に戻りたい」は違う要求である。**
> 取り消しは後者しか提供しない。前者なら定義を取り除くのが正しい。

### 効くもの・効かないもの

| 操作 | 取り消しで戻るか |
|---|---|
| 差分の適用(定義の変更) | **戻る**(ただしデータベース全体が巻き戻る) |
| 見た目の差し替え | **戻る** |
| 役割の差し替え | **戻る** |
| 画面の定義の削除 | **戻る**(そもそも行を失っていない) |
| 行の部分更新 | **戻らない。** 変更履歴にも載らない |
| 行の削除 | **戻らない。** 変更履歴にも載らない |
| アプリの削除 | **戻らない。** スナップショットごと消える。自動バックアップも連動しない |
| 逃げ道の資産の失効 | **戻らない。** 参照は戻るが実体が無いので遮断される |
| 変更履歴そのもの | **戻らない。** 中身は戻るが、履歴だけは一方向に伸びる |

- **やり直し(`redo`)はあるが、取り消しが安全になるわけではない。**
  やり直しは直前の取り消しが取ったスナップショットへ戻すだけで、
  **取り消しの巻き添えを1件も減らさない。**
- **消したあとに入力したデータを保ったまま、消した項目の値だけを戻す手段は無い。**

**「スナップショットがあるので戻せます」「やり直せるので安全です」という説明は嘘である。**

### バックアップ

日次で7世代のローカル保存だけである。遠隔地保管も、任意の時点への復旧も約束しない。
**アプリの削除には連動しない。**

---

## 事故が起きやすいところ

### 行の訂正と削除は履歴に残らない

行を直す・消す操作はスナップショットを取らないので、変更履歴に残らず、取り消しでも戻せない。
取り消しを使っても戻らない —— データベース全体が巻き戻るので、
消した行が戻る代わりに **そのスナップショットより後に入れた行が全部消える。**

### 真偽値と数値の列に自動処理から書けない

| 書き方 | 何が起きるか |
|---|---|
| `"unread": true`(真偽値そのもの) | **差分の時点で断られる** |
| `"unread": "true"`(文字列) | **差分は通る。** しかし動くたびに書き込みで弾かれる |
| `"unread": "$record.<真偽値の列>"` | **差分は通る**(下見は字面しか見ていない)。動いたときに失敗する |

**作った時点では成功して見えるのに永久に動かない** という壊れ方をする。
しかもその失敗は履歴の `error` 列にだけ残る。
代わりに、文字列の列に「済」と書くこと。数値の列も同じである。

### 未記入の真偽値は絞り込みから漏れる

真偽値の列に何も書かれていない行は空のままなので、「立っていない行」を条件にしても引っかからない。
**「買い物かごがいつも0件」の正体はこれである。** 島から `false` を書いておくか、
真偽値ではなく文字列の区分で持たせること。

### アプリの識別子は予測できない

作るときに識別子を省くと、アプリ名から生成される。規則は次のとおりである。

1. 名前を正規化して小文字にする
2. 小文字英数と `_` 以外をハイフンに置き換える(**日本語・記号・空白はここで落ちる**)
3. 連続する区切りをまとめ、先頭と末尾の区切りを落とす
4. 先頭が英小文字ならそのまま使う。空なら `app`。空ではないが英小文字始まりでないなら `app-` を付ける
5. 64文字に切り詰める
6. 既に使われていれば `-2`、`-3` と連番を足す

**4 と 6 が同じ形の文字列を生む。** `app-2` は「衝突の2番目」からも、
「名前から作った断片が `2` だった」からも出る。

| アプリ名 | 生成される識別子 | 理由 |
|---|---|---|
| `読書記録` | `app` | 4 の逃げ道 |
| `読書記録2` | `app-2` | 4 の分岐。**衝突の連番ではない** |
| `蔵書管理` | `app-3` | 真の衝突(前2つが使われている) |
| `Book Tracker` | `book-tracker` | 4 でそのまま |
| `Book Tracker`(2つ目) | `book-tracker-2` | 6 の衝突回避 |

**日本語名の識別子は、名前に含まれる英数字と、先にどのアプリが何を占有しているかの両方に依存する。**
手元で推測できない。**必ず作成の応答か一覧から読み取ること。** 画面の URL も、返された値をそのまま使うこと。

### 同時更新

版を指定して書けば、食い違ったときに衝突として断られる。
**指定しなければ、あとから書いた側が勝つ。** 自動処理の中の更新は版を見ない。

### 性能

保証の対象は1つの表あたり 1,000 行までである。10,000 行を超える表は対象外である。

### そのほか

- 実行履歴の書き込みが失敗しても、会話には届かない(サーバのログ止まりである)。
- 更新の宛先に実在しない行の識別子を書いても、差分は通る。
- ファイルは、使われなくなっても自動では消えない。

---

## 設計の原則

迷ったときに立ち返る原則である。

1. **土台は退屈に作る。** 手書きの普通のコードにする。自分自身を作り替える仕組みにしない
2. **語彙は狭く始める。** 精度で信頼を稼ぐ。足したくなったら、まず今ある言葉で表せないかを疑う
3. **コード生成ではなく解釈実行。** データが入ったまま育つことが値打ちである
4. **常に戻せる。** 当てる前に必ずスナップショットを取り、取り消しを一手で
5. **差分に意図を残す。** 変更履歴がそのまま要件の記録になる
6. **できないことは正直に言う。** 曖昧に誤魔化した瞬間に信頼が死ぬ
7. **機能以外はプラットフォームが背負う。** 使う人と AI は、欲しい機能だけに集中する
8. **完成の条件を先に決める。** 始める前に、何をもって終わりとするかを固定する
