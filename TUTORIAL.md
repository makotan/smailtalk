# はじめてのチュートリアル

このチュートリアルは、**SmAIltalk をまだ一度も触ったことがない人**が、手を止めずに最後まで辿れるように書いてあります。
上から順に、書いてあるコマンドをそのまま打ってください。

---

## このチュートリアルで、できるようになること

終わったとき、あなたの手元では次のことが起きています。

- ブラウザで開ける画面と、そこに実際のデータが入ったアプリが1本できている
- そのアプリを **AI との会話だけ**で作り、項目を1つ足し、それを取り消せている
- 定義を変えても**入れたデータが消えない**こと、そして**取り消しには代償がある**ことを、自分の目で確かめている

所要時間は、はじめての人でおよそ30〜45分です。ほとんどは待ち時間ではなく、読みながら打つ時間です。

このチュートリアルは**作り方**だけを扱います。書ける定義の全量や、細かい制約の一覧はマニュアルにあります。

---

## 前提(要るもの)

| 要るもの | 確かめ方 |
|---|---|
| [mise](https://mise.jdx.dev/) | `mise --version` が答えること |
| Claude Code(`claude` コマンド) | `claude --version` が答えること |
| ブラウザ | ふだん使っているもので構いません |

言語ランタイム(Bun と Node)は個別に入れる必要はありません。次の手順1で mise がまとめて入れます。

Node も要るのは、型検査・整形・検査系のいくつかが **node で動くプログラム**だからです。Bun だけを入れていると、それらを走らせたときに `No version is set for shim: node` で落ちます。

以降、シェルで mise を有効化(`mise activate`)していない場合は、`bun ...` を `mise exec -- bun ...` に読み替えてください。

---

## 手順1: 取ってきて動かす

### 1-1. 言語ランタイムを入れる

```bash
mise install
```

### 1-2. 依存パッケージを入れる

```bash
bun install
```

### 1-3. 画面をビルドする(**飛ばせません**)

```bash
bun run build:web
```

**この手順を飛ばすと、あとでプレビューURLを開いたときに 404 になります。** はじめての人が踏む失敗の中で、これがいちばん多いものです。

理由は、アプリの画面(`/apps/<アプリID>`)が**ブラウザ側で描かれる**からです。サーバはこのパスに対してビルド済みのファイルを返そうとしますが、そのファイルはビルドするまで存在しません。存在しなければ 404 になります。

紛らわしいのは、**データの口(`/api/...`)はビルドしていなくても 200 を返す**ことです。そのため「AI 側の操作は全部成功しているのに、ブラウザで開くと 404」という状態になります。

マニフェスト(アプリの画面定義)を変えたときに、ビルドし直す必要はありません。画面は表示のたびに定義を取りに行って解釈するので、**ブラウザのリロードだけ**で新しい画面になります。ビルドが要るのは初回と、画面のコード自体を変えたときだけです。

---

## 手順2: サーバを起動して画面を開く

### 2-1. 専用のターミナルで起動する

**別のターミナル**を開いて、起動したままにしておきます。

```bash
ST_DATA_ROOT=data ST_AUTH_EXPECTED_ORIGIN=http://localhost:3000 bun run server
```

こう出れば起動しています。

```
smailtalk server: http://127.0.0.1:3000 (dataRoot=data, timeZone=Asia/Tokyo)
```

### 2-2. 環境変数を2本だけ、先に理解しておく

ここで指定した2本は、**忘れると必ず後で詰まります。** 名前と意味だけ覚えてください。値は自分で決めます。

| 名前 | 何を決めるか | 忘れるとどうなるか |
|---|---|---|
| `ST_DATA_ROOT` | アプリとデータの置き場所(相対パスなので、**起動したときの現在地を基準に解けます**) | AI 側と食い違うと、アプリを作れているのに画面に何も出てきません。**どちらのプロセスもエラーを出しません** |
| `ST_AUTH_EXPECTED_ORIGIN` | **画面からの書き込みを受け付ける起点(オリジン)。** ブラウザで開くアドレスと一致させます | 既定値は開発用の別のアドレスなので、**ログインが 401、レコードの書き込みが 403 になります。**「リクエスト元 … は許可されていません」と出ます |

`ST_AUTH_EXPECTED_ORIGIN` の落とし穴は、**`curl` では絶対に露見しない**ことです。`curl` はこの起点を名乗らないので、コマンド行での確認は全部通ってしまいます。**ブラウザで触ったときにだけ壊れます。** 上のとおり、起動時に必ず指定してください。

ほかに知っておくと役に立つもの:

- ポートは既定で **3000**。変えるときは `PORT=3100` を頭に付けます。**変えたら `ST_AUTH_EXPECTED_ORIGIN` も同じポートに揃えてください。**
- `timeZone` は、決まった時刻に動く処理が「毎日 何時何分」を判定する基準です(`ST_TIMEZONE`)。

このサーバは**手元のマシンの外に出さないでください。** レコードの読み書きはログインで守られていますが、アプリの器を操作する管理系の口は今も認証を持たず、`127.0.0.1` に縛ることだけが防御です。

### 2-3. ブラウザで開く

<http://localhost:3000> を開きます。アプリの一覧が出れば成功です。まだアプリを1本も作っていないので、一覧は空です。

**ここが 404 になるなら、手順1-3(`bun run build:web`)に戻ってください。**

---

## 手順3: AI から使えるようにする(MCP の設定)

ここからは Claude Code との会話でアプリを作ります。SmAIltalk は**独自のチャット画面を持ちません。** 頭脳は Claude Code に任せ、こちらは「カーネルを操作する道具箱」を MCP サーバとして差し出します。

### 3-1. 名乗る名前を先に決める

**ここが最初の関門です。** この口には、**「どの利用者として操作するか」の名乗り**が要ります。

- 名乗りは環境変数 `ST_MCP_ACTOR` で、**起動時に一度だけ**決まります。会話の途中で切り替えることはできません。
- **空のままだと、道具は1本も動きません。** 起動そのものは成功するので、繋がっているように見えて全部失敗します。
- 名乗りに証明は要りません。**これは認可であって認証ではありません** —— 起動する人は誰の名前でも書けます。

したがって、**これから自分がブラウザで登録するログイン名を、先に決めてください。** ここでは仮に `admin` とします。**あなたは好きな名前にして構いません。以降の `admin` を、決めた名前に読み替えてください。**

### 3-2. 設定ファイルを置く

このディレクトリ(このチュートリアルと同じ場所)に `.mcp.json` を置きます。

```json
{
  "mcpServers": {
    "smailtalk": {
      "command": "mise",
      "args": ["exec", "--", "bun", "run", "src/mcp/index.ts"],
      "env": {
        "ST_DATA_ROOT": "data",
        "ST_PREVIEW_BASE_URL": "http://localhost:3000",
        "ST_MCP_ACTOR": "admin"
      }
    }
  }
}
```

3つの値の意味:

| 名前 | 揃える相手 |
|---|---|
| `ST_DATA_ROOT` | **手順2で起動したサーバと同じ値**にします。相対パスなので、**両方を同じディレクトリで起動する**なら既定のままで揃います |
| `ST_PREVIEW_BASE_URL` | サーバが待ち受けているアドレス。ポートを変えたならここも変えます |
| `ST_MCP_ACTOR` | 3-1 で決めた、あなたのログイン名 |

`bun` を直接書かず **`mise exec -- bun`** を通しているのには理由があります。`bun` は mise の入口としてしか存在せず、その入口が PATH に載るのは mise を有効化した対話シェルの中だけです。Claude Code が MCP サーバを子プロセスとして起動するときの環境には載らないことがあり、その場合 `"command": "bun"` は `Executable not found in $PATH: "bun"` で落ちます。`mise` 自体は通常の PATH にあるので解決でき、`mise exec` が固定バージョンの Bun を起動してくれます。

`mise` すら解決できない環境では、**絶対パスに逃がしてください**(自分の環境のパスに読み替えます)。

```bash
command -v mise   # 例: /opt/homebrew/bin/mise
command -v bun
```

**出力がパスにならないことがあります。** mise を有効化したシェルでは `mise` が**シェル関数**になっていることがあり、
そのとき `which mise` は関数の中身を丸ごと表示し、`command -v mise` は `mise` とだけ返します(どちらもパスではありません)。
**実際にそうなることを確かめてあります。**

そのときは、次のどちらかでパスを取ってください。

```bash
whence -p mise    # zsh。関数を飛ばして実体のパスだけを返す
type -a mise      # bash。一覧の中に実体のパスが出る
```

`bun` のほうは、関数ではないので `command -v bun` で実体のパスが返ります。

### 3-3. Claude Code を起動して承認する

**さらに別のターミナル**で、このディレクトリに移動して起動します。

```bash
claude
```

初回に「このプロジェクトの MCP サーバを使いますか?」という**承認プロンプト**が出るので、承認してください。**承認するまで接続されません。**

### 3-4. 繋がったか確かめる

別のターミナルで:

```bash
claude mcp list
```

`smailtalk` の行に接続済みの印が付いていれば成功です。まだ承認していないと、こう出ます。

```
smailtalk: mise exec -- bun run src/mcp/index.ts - ⏸ Pending approval (run `claude` to approve)
```

一度「拒否」してしまってプロンプトが二度と出なくなったときは、選択をやり直せます。

```bash
claude mcp reset-project-choices
```

詳細を見るには `claude mcp get smailtalk` を使います。

---

## 手順4: アプリの器を作って、最初の利用者(持ち主)になる

**順序に理由があります。** アプリを作れるのは AI 側の口だけで、ブラウザからアプリを新規作成する画面はありません。一方、アプリの中身を作るには、**そのアプリに登録済みの利用者として名乗る**必要があります。したがって次の順になります。

### 4-1. 空の器を作る(会話)

Claude Code にこう頼みます。

```
読書記録アプリを作って。
```

AI は器だけを作り、アプリIDとプレビューURLを返します。**この時点のアプリは中身が空**(表も画面も0件)です。

**返ってきたアプリIDを控えてください。** アプリIDは名前から予測できません。英小文字・数字・ハイフンしか使えないので、**日本語だけの名前は変換結果が空になり、既定のIDが割り当てられます。** さらに、それ以前に作ったアプリが何を使い済みかでも変わります。

**このチュートリアルに出てくるIDを、自分の環境のURLに書き写さないでください。** 必ず、返ってきた値を使います。

### 4-2. ブラウザで最初の1人として登録する

<http://localhost:3000> を開き、いま作ったアプリを選びます。ログイン画面が出ます。

画面には登録の枠が2つあります。**下の「運営(管理者)の登録」を使ってください。**

- **「はじめての方の登録」**(上の枠)を押すと、一般利用者になります。自分に関わる記録しか見えず、運営の画面は開けません。
- **「運営(管理者)の登録」**(下の枠)の **最初の1人だけが、そのアプリの持ち主(オーナー)**になります。2人目以降は閲覧者です。

ユーザ名には、**手順3-1 で `ST_MCP_ACTOR` に書いたのと同じ名前**を入れてください。「Passkeyで新規登録」でも「パスワードで新規登録」でも構いません。パスワードにするなら、**自分で決めた値を控えておいてください**(このチュートリアルは実際の値を1つも載せません)。

> **名前を揃えるのが大事です。** 名乗りとブラウザの登録名が食い違うと、AI 側の操作は `名乗った利用者は、このアプリに登録されていません。` で全部断られます。そのときは `.mcp.json` を直し、**Claude Code のセッションを再起動**してください(名乗りは起動時にしか読まれません)。

### 4-3. 登録できたか確かめる

ログインした状態でアプリの画面に入れれば成功です。まだ画面を1枚も作っていないので、「使える画面がありません」と出ます。**これで正常です。**

---

## 手順5: AI に頼んで小さなアプリを1つ作る

ここからは会話だけです。**道具を呼ぶのは全部 AI** なので、あなたがやるのは文章を打つことと、ブラウザで見ることだけです。以下はそのままコピーして構いません。

### 5-1. 表と一覧画面を作る

```
本を記録する表を作って。項目はタイトル(必須)と著者。
一覧画面も作って、タイトルと著者が見えるようにして。
```

AI は差分パッチを1本組み立てて適用します。差分には **「何がしたいのか(意図)」が必須**で、それがそのまま変更履歴に残ります。

### 5-2. データを入れる

```
夏目漱石の「吾輩は猫である」と「こころ」を入れてみて。
```

検査に引っかかった行だけが失敗として報告され、通った行はそのまま入ります。

### 5-3. 画面のURLをもらう

```
ブラウザで確認したいので、URLを教えて。
```

返ってきたURLを**そのまま**ブラウザで開いてください。本が2件並んでいれば成功です。

> **URLを手で組み立てないでください。** アプリIDも画面IDも予測できません。なお**IDの区切りはハイフン**です。

---

## 手順6: 作ったものを画面で確かめる(そして取り消す)

### 6-1. 項目を1つ足す

```
読み終わった日も記録したい。一覧にも表示して。
```

### 6-2. リロードするだけで反映される

**ブラウザをリロードするだけ**で「読了日」の列が現れます。ビルドし直す必要も、サーバを再起動する必要もありません。

**そして、先に入れた本2件は消えていません。** 追加された列は、既存の行では空になります。**これがこの製品の中心にある一点です** —— データが入ったまま定義を育てられます。

### 6-3. 取り消す

```
やっぱり今の変更を取り消して。
```

AI はまず「何が取り消されるか」「失われる行が何件あるか」を**何も変えずに**調べて提示し、同意を求めます。同意すると取り消しが走ります。

**ここで、代償を必ず理解してください。** 取り消しは差分の逆適用ではなく、**データベース全体を適用直前の状態へ巻き戻すもの**です。したがって、**その変更のあとに追加・編集した行も一緒に消えます。**

### 6-4. 戻ったことを確認する

**ブラウザをリロードすると**「読了日」の列が消え、本2件はそのまま残っています。

「取り消しの取り消し(やり直し)」はできますが、**それは直前の取り消しが取ったスナップショットへ戻すだけ**で、巻き添えを減らしてはくれません。戻しすぎたときは、差分を組み直して適用し直すのが確実です。

**ここまでで一巡です。** 作る → 見る → 育てる → 戻す、が全部できました。

---

## うまくいかないとき(躓きと対処)

### アプリを作れたのに、画面に何も出てこない(最も多い混乱)

**`ST_DATA_ROOT` の食い違い**を疑ってください。圧倒的にこれです。

AI 側の口とサーバは**別のプロセス**で、**この置き場を通じてだけ状態を共有しています。** 指す先が違えば、片方に書いて、もう片方を読むので、どちらも正常に動いているのに何も見えません。**エラーは1件も出ません。**

- 症状: AI にはアプリの一覧が見えるのに、ブラウザのアプリ一覧が空。
- 確認: サーバの起動ログ末尾の `(dataRoot=..., timeZone=...)` と、`.mcp.json` の `ST_DATA_ROOT` を見比べる。
- **注意**: 値が同じ `data` でも、**起動したときの現在地が違えば別の場所**を指します。両方を同じディレクトリで起動してください。
- 実体の確認: `ls data/apps/` に、期待するアプリIDのディレクトリがあるか。

### 画面からログインできない / 保存できない(401 や 403 になる)

**`ST_AUTH_EXPECTED_ORIGIN` を指定せずにサーバを起動しています。** 既定値は開発用の別のアドレスなので、`http://localhost:3000` の画面からの要求は「リクエスト元 … は許可されていません」で断られます。

手順2-1 のとおり、**ブラウザで開くアドレスと同じ値**を指定して起動し直してください。ポートを変えたなら、こちらも同じポートにします。

### AI 側の操作が全部断られる

2つのどちらかです。

- **`この MCP サーバは、どの利用者として動くのかを名乗っていません。`** → `.mcp.json` の `ST_MCP_ACTOR` が空です。
- **`名乗った利用者は、このアプリに登録されていません。`** → 名乗った名前が、そのアプリに登録されていません。手順4-2 で登録した名前と綴りを見比べてください。

**どちらも、直したら Claude Code のセッションを再起動してください。** 名乗りは起動時にしか読まれず、会話の途中では変えられません。

### プレビューURLが 404 になる

`bun run build:web` を実行していません。手順1-3 に戻ってください。

**逆は成り立ちません。** ビルド済みの状態では、**存在しないアプリのURLでも 200 が返ります**(画面側で「見つかりません」と描かれます)。存在の確認にはデータの口を使ってください。

```bash
curl -o /dev/null -w '%{http_code}\n' http://127.0.0.1:3000/api/apps        # 200 → サーバは生きている
curl -i http://127.0.0.1:3000/api/apps/<アプリID>/manifest                  # 404 なら、そのアプリは無い
```

### 画面が真っ白 / 「見つかりません」になる(404 ではない)

多くはアプリIDか画面IDの打ち間違いです。**IDの区切りはハイフンです。** URLを手で打たず、AI に聞き直して返り値をそのまま使ってください。

### `Executable not found in $PATH: "bun"`

`.mcp.json` の `command` が `bun` になっています。手順3-2 のとおり `mise exec -- bun` の形に直すか、絶対パスを指定してください。

### `No version is set for shim: node`

`mise install` を実行していません。手順1-1 に戻ってください。

### ポートが衝突する(`EADDRINUSE`)

3000番を既に何かが使っています。

```bash
lsof -i :3000
```

ポートを変えるなら、**サーバ・`ST_AUTH_EXPECTED_ORIGIN`・`.mcp.json` の `ST_PREVIEW_BASE_URL` の3つを揃えてください。** 1つでも取り残すと、返ってくるURLが繋がりません。

### 承認プロンプトが出ない / 一度拒否してしまった

承認・拒否の選択は記憶されます。やり直すには `claude mcp reset-project-choices` を打ってから `claude` を起動し直してください。

### `.mcp.json` を編集したのに反映されない

Claude Code は起動時にこのファイルを読みます。**編集したらセッションを再起動してください。** `claude mcp list` で表示が変わっているかを確認できます。

### AI 側の口が無言になる / 途中で切れる

この口は標準入出力をそのまま通信路として使います。したがって、そこへ通信以外の文字が1つでも混ざるとプロトコルが壊れます。壊れ方がはっきりしたエラーにならず、「無言で応答が来なくなる」形になります。手を入れたあとに急に繋がらなくなったら、まずこれを疑ってください。切り分けは、口を直接起動して標準出力を眺めることです。

```bash
bun run mcp    # 何も打たなければ標準出力は無言のはず(起動ログは標準エラーに出ます)
```

---

## ここまででできないこと(正直に書きます)

**この製品はできることの範囲を意図的に狭くしてあります。** 以下は、頼み方を変えても実現できません。無駄足を避けるために、先に読んでおいてください。AI もこれらは正直に断り、範囲内の代わりの案を出します。

### 作り方について

- **任意の CSS と、選べる範囲の外のレイアウトを AI が書くことはできません。** ピクセル座標も、絶対配置も、3段組も書けません。**ただし持ち主が「逃げ道」として CSS を発行すれば、その画面には実際に当たります** —— AI にできるのは申請までです。**「誰にもできない」とは読まないでください。**
- **配色・書体・余白はアプリ単位でだけ変えられます。** 画面ごとに色を変えることも、項目ごと・値ごとに塗り分けること(条件付き書式)もできません。**読みにくい配色は適用が拒否されます。**
- **決まった言い方でしか、日常語の調整を受け付けません。** 「もっと余白を広く」は通りますが、「かっこよくして」「今風に」は断られます。**配色を言葉から決めることはできません。**
- **1ページの件数は決まった選択肢から選ぶ形で、自由な数は書けません。** 日付の表示形・数値の桁区切り・罫線の見せ方も、書く場所がありません。
- **表を跨いだ表示は1ホップだけです。** 孫も、多段も、行ごとの計算列もありません。
- **CSV の取り込み画面はありません。** 会話に貼り付けた文字列を AI が行に起こすことはできますが、**製品が外部からファイルを取りに行くことは1度もありません。** 一度限りの手作業であって、同期にも定期取り込みにもなりません。**列の対応づけは AI が行うので、取り違えは静かに残ります。**
- **外部サービスへ自由に繋ぐことはできません。** 送信も受信も、持ち主が発行した接続の範囲でだけ動きます。**AI は申請しかできません。**

### 元に戻すことについて

- **削除・改名・型変更はできます。** できないのは、**その結果だけを選んで元に戻すこと**です。
- **取り消しは差分の逆適用ではなく、データベース全体の巻き戻しです。** その適用より後に入れたデータも一緒に消えます。
- **「スナップショットがあるので後から復元できます」は嘘です。** そう説明しないよう AI にも指示してあります。
- **レコードの訂正・1件削除は、変更履歴にも残らず、取り消しでも戻せません。**
- **アプリの完全削除は取り消せません。** スナップショットごと消えます。

### 守りについて

- **アプリの器を操作する管理系の口は、今も認証を持ちません。** `127.0.0.1` に縛ることだけが防御です。**手元のマシンの外に出す代物ではありません。**
- **AI 側の名乗りに証明はありません。** 起動する人は誰の名前でも書けます。持ち主の名前を書けば、その全権がそのまま渡ります。
- **AI 側の操作は監査記録に1行も残りません。** 断られた操作も残りません。
- **アプリの一覧と定義の取得は、名乗りで絞られません。** 名乗った人に見えない画面まで含めて、定義は全部返ります。
- **決まった時刻に動く処理には、権限の判定が掛かりません。** ここだけは今も素通りです。**「全部の入口に効く」とは読まないでください。**
- **複数の組織を1つのサーバで分けて運用する仕組みはありません。** 1アプリ = 1つの用途 = 自分で立てて使う前提です。

### 範囲の外だと決めているもの

実運用としての公開・TLS・過負荷対策・監視、**本物の決済とカード情報の取り扱い**、多通貨・多言語・複数拠点。これらは作っていません。

---

## 次に読むもの

- **マニュアル** —— 書ける定義と操作の全量、道具の一覧、注意すべき挙動。「これは書けるのか」を引くための本です。
- **入口の説明** —— この製品が何で、どういう考え方で作られているか。

ここで見せたかったのは機能の多さではありません。**データが入ったまま定義を育てられて、しかも取り消せる**という一点と、**できないことを正直に言う**という態度です。
