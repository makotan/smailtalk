[test]
# コンポーネントテスト用の DOM(happy-dom)を用意する。
# ネットワーク系グローバルは Bun 標準のまま残すので、カーネル/サーバのテストには影響しない。
#
# 【V9-M11-T01 / X-G24】このファイルは公開単位(apps/smailtalk)が自分で持つ検査設定である。
# **インストール側のセクションを1バイトも書かない**(限定2。綴りを書くと限定の式に自分で当たるので書かない)。
# 束ねのリンカ設定は正本のルートの bunfig.toml に在り、
# その撤去は X-G31(V9-M12-T01)の射程である。
# preload の要素は1本だけである(限定3)。
preload = ["./web/test/setup.ts"]
