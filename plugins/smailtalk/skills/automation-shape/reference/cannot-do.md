# できないこと(自動処理の形) —— 語彙の正からの逐語

**下の引用は `src/mcp/vocabulary.ts` の `CANNOT_DO` からそのまま引いたものである。**
**言い換えていない。要約もしていない。** 引用の外に、この説明書が独自に書き起こした
「できないこと」は1件も無い。

---

## 外部への送信・失敗したときの巻き戻し・条件に大小を書くこと

> 外部の宛先へ送るには call_external アクションと**接続(capability)**が要りますが、**接続を発行できるのは人間(owner)だけで、AI は request_connection で申請することしかできません。**接続が無い送信・接続のスコープ(許可ホスト)外の送信は、説明文ではなく**カーネルの実行層で遮断されます**。だから頼まれても、AI にできるのは申請までです)、「ワークフローが失敗しても登録だけは残して」「この自動化は失敗しても無視して」(**アクションが1つでも失敗すると、そのワークフローを発火させたレコード書込は成立しません** —— 巻き戻すかどうかをアプリごとに選ぶキーはマニフェストに1つもなく、**特定のアクションだけ「失敗してもよい」と宣言する手段もありません。**失敗は実行履歴に残り(発火元が巻き戻っても残ります)、呼び出し元にはエラーが返ります。**ただし決まった時刻の発火(schedule)は巻き戻らないので、そこでは途中まで書けた行が残ります**)、「金額が1000円以上の行だけ処理して」「残数が10を下回った行だけ印を付けて」(**数値や文字列の大小をワークフローの条件に書く手段は1つもありません** —— アクションの条件(when)は「この項目がこの値と等しいか」という等値1形だけで、比較(より大きい / より小さい)・かつ / または・else・入れ子・計算式は1つも書けません。

## 表を書かない `schedule` で書けないもの

**【`V8-M22` / 台帳 `J-G35`。2026-08-10 に引用の範囲を伸ばした】**
**旧版はこの引用を「画面ごと・項目ごとの表示権限を AI が設定することはできません。」で切っていた** ——
**その1文は今日は偽であり、`CANNOT_DO` の側では直後に訂正が続いている。**
**旧版の切り方だと、この説明書だけを読む相手には訂正が1文字も届かなかった。**
**引用の字面は1文字も書き換えていない。伸ばしただけである**(照合は `scripts/skill-vocabulary-inventory.txt` の `verbatim` 行と `pair` 行)。

> **trigger に表を書かない schedule では、$record.<フィールドID> も when も1つも書けません** —— トリガー元のレコードが無いためで、書いた差分は適用時に拒否されます)、「管理者だけ見られるようにして」(画面ごと・項目ごとの表示権限を AI が設定することはできません。**訂正します。直前の1文は今日は偽です** —— **役割の規則(app.roles[].rules)を差分操作 set_roles で書けば、画面ごと・項目ごとの表示権限をあなたが設定できます**(下の「今日は、画面を名指しして見せる相手を書けます」と「項目(フィールド)単位でも、見せる相手を書けます」の2段落が今日の正です)。**今日も書けないのは「管理者だけ」という言い回しをそのまま写すことだけです** —— 相手は役割の名前で書き、規則を1本も書いていない対象は今日どおり全員に見えます。

**【2026-08-12 訂正(`V8-M32`。`V8-M26` / `ADR-0317` / ユーザ決定 `D-V8-38` / `D-V8-42`)。上の引用の字面は1文字も書き換えていない。引用の外に足している】**
**上の引用の末尾「規則を1本も書いていない対象は今日どおり全員に見えます」は今日は偽である** ——
**規則が1本も無い対象は、既定で閉じる**(表 / 画面 / ボタン / `app` / `role` の5対象)。
**役割を1つも宣言していないアプリでも閉じる。**
**項目(`field`)だけは今日も管轄外で、全員に出る** —— **閉じたのは5対象であって6対象ではない。**
**ただし表・画面・ボタンを足すと、既定の3役割(持ち主・編集者・閲覧者)ぶんの規則が自動で入る。**
**`CANNOT_DO` の側は今日もこの訂正を持っていない** —— **直す先は `src/mcp/vocabulary.ts` であって、この説明書ではない。**

## AI 呼び出しの上限と、外部データの取り込み

> 上限を変更できるのも人間だけです。**capability が無い呼び出し・上限を超えた呼び出しは、説明文ではなく**カーネルの実行層で遮断され、記録されます**。だから頼まれても、AI にできるのは申請までです)、「CSVを取り込んで」「外部データを読み込んで」(任意の外部データ取り込みはできません —— call_external は依然送信専用で、応答本文をアプリ内テーブルへ取り込む経路はありません。

## 外部から入ってこられる唯一の経路(受信口)

> 外部からアプリへ入るのは、人間(owner)が発行した受信口(inbound capability)の下で、署名付き Webhook を書込先テーブル1つへ1行 create する限定経路だけです(外部サービスからの通知を受け取る口です。EC-G4 / ADR-0160)—— 受信口を発行できるのは人間(owner)だけで、AI は request_inbound_endpoint で申請することしかできません。**受けられる署名の形も決まっています**(ヘッダ名3つ・値の書き表し方3つ・アルゴリズムは HMAC-SHA256 の1種だけ)—— **時刻の印などを本文に連結してから署名する送り手からの通知は、今日1つも受け取れません。**署名検証を通ったものだけが書かれ、CSV の読み込みや任意の外部同期にはなりません。

## 集計とグラフ

> (4)**行に起こす時点で、あなたが列を対応づけて値を書き換えています**(取り違えは静かに残るので、書く前に対応づけを人間に見せて確認してください))、「売上をグラフにして」(グラフの描画は依然としてありません —— これは表示層の話で、この語彙には無い。関数の出力も数値であって、描画ではありません)、「差分操作だけで月ごとの集計を出して」(add_table などの差分操作(宣言)で書ける集計は一覧の合計(list_view の sum_field)1つだけで、group by / 平均 / 最小 / 最大 をカーネルに書くことはできません。その合計も、画面に出る「全 N 件」とまったく同じ集合の number 列1本を足すだけで、1画面で合計できる列は1本だけ、合計で絞り込むことも並べ替えることもできません。ただし合計・件数・平均などの集計値は、関数リソース(コードの島)を作り、その JavaScript コードの中で集計して、出力をテーブルに書き、既存のビューで見せる形でなら表現できます(ワークフローの run_function アクションで発火すれば、集計は自動で計算し直されて保存されます)。「何でも集計できる」わけではなく、集計ロジックは島のコードとして自分で書く必要があります。そして「グラフにして」は、この形でも実現できません(描画は表示層の機能で、この語彙に無い))。(2026-08-14 訂正)直前の括弧の中の2文は今日は正しくありません —— 集計表という4種目の画面が加わり、束ねる指定と件数が差分操作で書けるようになりました。それでも平均・最小・最大・中央値は1つも書けず、束ね方は日・週・月の3つ、束ねるキーと集計はそれぞれ1〜3本まで、集計した値で絞り込むことも並べ替えることも今日はできません。上の一覧の合計は今日も別の宣言として残っており、グラフの描画も今日は依然としてありません。この4種目の画面は、今日はまだブラウザに描かれません(宣言として書けるところまでです)。(2026-08-15 追記)集計表には上限が4つあります —— 結合できる表は起点を含めて5表まで、参照の鎖は3段まで(この2つは差分の適用時に拒否されます)、群にまとめる前に読む行は表ごとに 10,000 件まで、作る群は 10,000 件まで(この2つは集計を要求したときに 400 になります)。4つとも値ちょうどは通り、超えたときだけ拒否されます。行が増えていくと、ある日この上限に当たって集計表が開かなくなります —— そのときは絞り込み(filter)で対象を狭めるか、集計表を分けてください。

**【2026-08-16 訂正(`V8-M13-T04`。台帳 `Q-G30` / `Q-G33`)。上の引用ブロックを1バイトも消していない】**
**引用の中が「今日はまだ無い」と書いたものは、今日は在る** —— 引用は `CANNOT_DO` の逐語なので触らず、ここに今日の事実を並べる。

- **集計表はブラウザに描かれる**(`V8-M11`)。画面の上にグラフ、下に群の表が出る。
- **グラフは棒と折れ線の2種**だけで、`report` の `chart` に書く(`V8-M12`)。省略すると棒。円・散布図・積み上げ・ドーナツ・面は1つも書けず、どの集計を描くかも選べない。**集計表の外にはグラフは今日も1つも無い**(一覧・詳細・入力の画面にも、常設領域にも出せない)。
- **集計の母集団には、その画面を開いた人が読める行だけが入る**(`V8-M10`)—— したがって**見る人によって数が変わる**。**掛かるのはこの読取経路だけである** —— **島が `output_table` へ書いた表には1ミリも掛からず、受信口・ワークフロー・`schedule` は今日も素通りである。** **【禁止】これを「集計に権限が効くようになった」と書かない。**
- **MCP からは `read_report` で集計表を1枚読める**(`V8-M13-T02`)。`list_records` は今日も `report` を1つも見ない。

**【`Q-G29`。門Aの判定 = 限定採用】** **集計を書く道は2本ある** —— **宣言の道(`report_view`)と、島の道(`function` で数えて `output_table` に書き、一覧で見せる)である。** **正は宣言の道である。**
**島の道を選ぶ条件は3つだけである** —— **（あ）結果を他の画面から参照したいとき、（い）結果をワークフローの発火元にしたいとき、（う）見る人によらず同じ数を出したいとき。** **4つ目の条件を足すには改めて審査が要る。**
**2本の道は同じデータに違う数を返すのが正常であり、片方をもう片方に移せるとは書けない。**

---

## この参照ファイルに入っていない「できないこと」

**`CANNOT_DO` の全文は入っていない。** 入れなかったもののうち、主なものは:

- **人間が会話に貼り付けた表の中身を行に起こすときに添える4点**のうち、(1)〜(3)。
- **予約規約フィールド4本**(`st_owner` / `st_public` / `st_undeletable` /
  `st_no_direct_create`)が自動処理をどう素通りさせるかについてのもの。
  **【`V8-M20` で 5本 → 4本 になった】** **5本目だった `st_admin_readable` は撤去された** ——
  同じことは役割の規則(`app.roles[].rules`)の「対象 `table` × `read`」に置き直されている。
- **MCP から読むときに役割の規則(`app.roles[].rules`)が1つも効かないこと**についてのもの。
  **【`V8-M20` で置き直された】** 旧文はここを「`audience` の宣言」と書いていた ——
  **そのキーは撤去されたので、今日は名前ごと存在しない。**
  **旧文の逐語**: 「**ワークフロー・コードの島・受信口も、この規則を1つも通らない**(逐語は view-shape の参照ファイル)。」
  **【`V8-M22` / 台帳 `J-G35`。2026-08-10。旧文を1バイトも消していない】この1文は今日は偽である** ——
  **`V8-M21` が3経路に判定を配線した**(受信口 / 登録・更新をきっかけに動く自動処理 / コードの島)。
  **今日も規則を1つも通らないのは2本だけである** —— **MCP(あなた)と、決まった時刻に動く処理(`schedule`)。**
  **同じワークフローでも、登録で動けば規則が効き、時刻で動けば効かない。**
  **【2026-08-12 訂正(`V8-M32`。`V8-M31` / `ADR-0327` / `ADR-0328`)。この項目の見出しの1行と直前の2文を1バイトも消していない】**
  **この項目の見出し「MCP から読むときに役割の規則(`app.roles[].rules`)が1つも効かないこと」も、**
  **直前の「今日も規則を1つも通らないのは2本だけである」も今日は偽である** ——
  **あなた(MCP)は起動設定の `ST_MCP_ACTOR` で「この人として動く」を名乗り、名乗った人の権限で判定される**
  (名乗りが無いと23本のツールはすべて失敗する)。**読む操作も絞られる。**
  **今日も規則を1つも通らないのは `schedule` の1本だけで、0本ではない。**
  **【禁止】これを「全部の入口に効く」と書かない。**
  **【2026-08-16 訂正(`V8-M13-T04`。台帳 `Q-G30`)。直前の括弧を1バイトも消していない】**
  **括弧の中の本数は今日は偽である** —— **`V8-M13-T02` が `read_report` を足したので、今日は24本である。**
  **【2026-08-24 訂正。直前の1文を1バイトも消していない】**
  **直前の「今日は24本である」も今日は偽である** —— **`list_comments`(積まれたコメントを読む参照系)を1本足したので、今日は25本である。**
  **足したのは読む道具1本だけであり、書く道具は1本も増えていない。**
  **失敗の仕方も、名乗りの要不要も1ミリも変わっていない。**
  **【2026-08-25 訂正。直前の3文を1バイトも消していない】**
  **直前の「今日は25本である」も、直前の「書く道具は1本も増えていない」も今日は偽である** —— **`set_comment_visibility`(コメントの欄と読む場所を画面に出すかを倒す書く道具)を1本足したので、今日は26本であり、名乗りが無ければその26本すべてが今日も失敗する。**

**この3つの説明書は `CANNOT_DO` の代わりではない。**
正は `src/mcp/vocabulary.ts` の `CANNOT_DO` そのものである。

**【2026-08-16 訂正(`V8-M47`)。直前の2文を1バイトも消していない】**
**直前の「この3つの説明書は `CANNOT_DO` の代わりではない。」の「この3つ」は今日は偽である** —— **説明書は今日4本になった**(4本目は `app-build`。アプリを組み立てる順序と骨格)。
**趣旨そのものは今日も変わらない** —— **4本になっても、説明書は語彙の正の代わりにはならない。**
