# できないこと(差分の形) —— 語彙の正からの逐語

**下の引用は `src/mcp/vocabulary.ts` の `CANNOT_DO` からそのまま引いたものである。**
**言い換えていない。要約もしていない。** 引用の外に、この説明書が独自に書き起こした
「できないこと」は1件も無い。

---

## アプリを丸ごと消すこと

> 「アプリごと消して」はできます(専用の delete_app があります)。ただし**取り消せません** —— スナップショットごと消えるので undo でも戻りません。バックアップ(data/backups/)は日次の別系統で、この削除とは連動しません(最大7世代・約7日残りうるので「消したのに残っている」ように見える場合があります)。実行する前に、消える中身(アプリ名・テーブル・レコード件数・ビュー・ワークフロー・変更履歴・スナップショット件数など)を get_manifest / list_records / get_changelog で人間に見せ、同意を得てから delete_app を呼んでください。

---

## この参照ファイルに入っていない「できないこと」

**`CANNOT_DO` の全文は入っていない。** 入れなかったものは、主題ごとに別の説明書にある:

- **見た目・画面ごとの権限**についてのもの → view-shape の説明書の参照ファイル
- **外部への送信・受信・AI 呼び出し・集計とグラフ**についてのもの → automation-shape の説明書の参照ファイル
- **どの説明書にも入れなかったもの**がある。**この3つの説明書は `CANNOT_DO` の代わりではない。**
  正は `src/kernel/types.ts` の3配列と `schemas/*.json`、および `CANNOT_DO` そのものである。

**【2026-08-16 訂正(`V8-M47`)。直前の1文を1バイトも消していない】**
**直前の「この3つの説明書は `CANNOT_DO` の代わりではない。」の「3つ」は今日は偽である** —— **説明書は今日4本である**(4本目は `app-build`。アプリを組み立てる順序と骨格)。
**「`CANNOT_DO` の代わりではない」という趣旨そのものは今日も変わらない** —— **4本になっても、説明書は語彙の正の代わりにはならない。**
