---
name: view-shape
description: >-
  SmAIltalk(MCP サーバ smailtalk)の画面定義(form / list_view / detail_view)の形と、書けるキー・書けないキーの全量を確かめるための説明書。add_view / update_view を含む差分を書く直前に開く。「この画面の列を変えて」「一覧に絞り込みを付けて」「入力フォームを2段組にして」「この画面は管理者だけに見せて」と頼まれたときにも開く。画面まわりで「未知のプロパティ "…" は指定できません。」「v0の語彙にないプロパティです。」が返ったとき、書き直す前に開く。
when_to_use: >-
  「列を足して」「並べ替えを変えて」「絞り込みを付けて」「この画面は特定の人にだけ見せて」など、画面の中身と見せる相手を変えるときに開く。差分の外側と操作の必須キーは diff-shape、ワークフローと関数は automation-shape、アプリを最初から作る順序は app-build にある。
allowed-tools: mcp__smailtalk__dry_run_diff mcp__smailtalk__get_manifest mcp__smailtalk__get_preview_url
---

# 画面の形(SmAIltalk)

## この説明書が扱うこと(かつて説明文に書いていた目次)

**【2026-08-16。`V8-M47`】この節は、かつて front matter の `description` に書いていた文である。**
**公式の作法に従い `description` を「何をするか・いつ開くか」だけにしたので、外した文をここへ逐語で移した。1文字も変えていない。**

扱うのは (1) 画面定義の必須キー、(2) form / list_view / detail_view それぞれに書けないキーの一覧、(3) update_view の changes に書ける24キー(id / type / table / related は入っていない)、(4) 列・並べ替え・絞り込み・子一覧の値の形、(5) preset_ で始まる9軸24選択肢の値域、(6) 画面・項目・ボタンを「見せる相手」を決める役割の規則(set_roles の roles[].rules と条件 when。かつての audience / writable_by は撤去された)、(7) 見た目と表示権限についてのできないこと。差分の外側と操作の必須キーは diff-shape に、ワークフローと関数は automation-shape に分けてある。

---

## この説明書が狙っている躓き

実測(この製品の試行証跡 62本・機械分類できたエラー 116件)の上位3類型のうち、
**この説明書が主に狙うのは「語彙に無いキーを書いた」(23件)である。**

| 実測で多かった躓き | 件数 | この説明書での扱い |
|---|---:|---|
| 語彙に無いキーを書いた | 23 | **主目的**。下の「型ごとに書けないキー」と「changes の24キー」 |
| 型・形の取り違え | 20 | **主目的**。下の「値の形」 |
| 必須プロパティの書き忘れ | 39 | 一部(画面定義の分だけ)。差分全体は diff-shape |

**狙っていないもの**: 実在しない ID を指した(10件)/ どの規則にも当たらなかったもの(24件)。

---

## 1. 画面定義の必須キー

```json
{ "op": "add_view",
  "view": { "id": "task_list", "type": "list_view", "table": "task", "columns": ["title", "due_on"] } }
```

- **どの種別でも必須**: `id` / `type` / `table`
- `type` は **`form` / `list_view` / `detail_view` / `report_view` の4つだけ**(5つ目は無い)
  **【2026-08-14 訂正。旧行の逐語は「- `type` は **`form` / `list_view` / `detail_view` の3つだけ**(4つ目は無い)」である。1バイトも消していない】**
  **4つ目 `report_view`(集計表)が加わった。** **集計表は行を1行ずつ並べず、宣言した束ね方で数えた結果だけを返す。**
  **【2026-08-15 追記(`V8-M10`)。上の2行を1バイトも書き換えていない】**
  **集計表には上限が4つある** —— **結合できる表は起点を含めて5表まで・参照の鎖は3段まで**
  (**差分を適用するときに拒否される**)、**群にまとめる前に読む行は表ごとに 10,000 件まで・作る群は 10,000 件まで**
  (**集計を要求したときに 400 になる**)。**4つとも、その値ちょうどは通り、超えたときだけ拒否される。**
  **全量と、当たったときの直し方は参照ファイル `reference/view-keys.md` に書いてある。**
  **【2026-08-16 追記(`V8-M13-T04`。台帳 `Q-G30`)。上の4行を1バイトも書き換えていない】**
  **集計表はブラウザに描かれる**(`V8-M11`。画面の上にグラフ、下に群の表)。
  **グラフは棒と折れ線の2種**だけで、`report.chart` に書く(`V8-M12`。省略すると棒。円・散布図・積み上げ・ドーナツ・面は書けない)。
  **集計の母集団には、その画面を開いた人が読める行だけが入る**(`V8-M10`)—— **見る人によって数が変わる**。
  **掛かるのはこの読取経路だけで、島が `output_table` へ書いた表には1ミリも掛からない**(**【禁止】「集計に権限が効くようになった」と書かない**)。
  **MCP からは `read_report` で集計表を1枚読める**(`V8-M13-T02`)。**2本の道の話は参照ファイル `reference/view-keys.md` にある。**
- **種別ごとに増える必須キー**:
  - `type: "list_view"` → **`columns` が必須**
  - `type: "form"` → **`fields` が必須**
  - `type: "detail_view"` → 追加の必須キーは無い
  - `modal: true` を書いたとき → **`menu_listed` が必須**

## 2. 型ごとに「書けない」キー(書くと差分全体が拒否される)

**画面定義に書けるキーは31個あるが、種別ごとに閉じている。**
下は**その種別では1つも書けない**キーである。

| 種別 | 書けないキー |
|---|---|
| `list_view` | `fields`, `related`, `preset_label_placement`, `preset_field_columns`, `field_groups`, `modal`, `after_save`, `reference_pickers`, `after_delete` |
| `form` | `columns`, `sort`, `filter`, `related`, `actions`, `preset_column_align`, `preset_column_width`, `preset_pager_position`, `preset_image_size`, `preset_text_preview`, `field_groups`, `preset_list_shape`, `search_fields`, `page_size`, `sum_field`, `after_delete` |
| `detail_view` | `columns`, `sort`, `filter`, `preset_column_align`, `preset_column_width`, `preset_pager_position`, `preset_list_shape`, `modal`, `search_fields`, `page_size`, `sum_field`, `reference_pickers` |

**言い換えると**: 子一覧(`related`)は **detail_view にだけ**書ける。

**`after_save`(行き先の画面)は `form` と `detail_view` の2種別に書ける。**
**一覧(`list_view`)と集計表(`report_view`)には書けない。**
**発火するきっかけは種別で違う** —— `form` では保存が成立したとき。
**`detail_view` に「保存」という出来事は無く、移るのは `actions` の `set` の形
(値の書換)の書込が成立したときだけである。** **`run` の形(自動処理の起動)の後は
1ミリも移らない。** **`detail_view` では画面に1本であり、ボタンごとの行き先は書けない。**

**保存した行が次の画面へ渡るのは、`form` の `after_save` に「同じテーブルの `detail_view`」を
書いたときだけである** —— **別テーブルの `detail_view`・`list_view`・`report_view`・`form` を
書いたときは1バイトも渡らず、別テーブルの `detail_view` は今日どおりエラー画面になる。**
**`detail_view` の `after_save` は行を1つも渡さない。**
**一覧の `actions` の `view` の形とは違う** —— **あちらはテーブルを問わず押した行を開く。**

**`after_delete`(削除が成立したあとの行き先)は `detail_view` にだけ書ける。**
**行き先にできるのは一覧画面(`list_view`)か集計表(`report_view`)だけである** ——
**詳細画面(`detail_view`)や入力画面(`form`)を指した差分は拒否される**
(消したばかりの行を開こうとしてエラー画面になるため)。
**書かなければ今日どおりで、同じテーブルの一覧があればそこへ、無ければ画面一覧へ戻る。**
**削除の確認のしかたもボタンの文言も、ここでは1つも指定できない。**
**テーブルに紐づかない案内ページ(「退会しました」等)は今日も作れないので、
その行き先には指定できない。**

**`flow`(一続きの流れの中の段)は `list_view` / `form` / `detail_view` の3種別に書ける。**
**集計表(`report_view`)には書けない。**
**書くのは3つだけである** —— 流れの名前(`id`)/ その流れの中の位置(`step`。1から始まる整数)/
段の種類(`kind`。`input` か `confirm`)。
**同じ名前を書いた画面どうしが1つの流れになる**(`id` は画面のIDではない)。
**同じ流れの中で位置が重なっている差分・位置が抜けている差分・1から始まっていない差分は拒否される。**
**確認の段(`kind: "confirm"`)を書けるのは `detail_view` だけである** ——
**`form` や `list_view` に書いた差分は拒否される。**
**段の名前・アイコン・進み具合はここでは1つも指定できず、段のあいだで未確定の入力を
持ち回すこともできない。**

一覧の器の形(`preset_list_shape`)は **list_view にだけ**書ける。
項目のまとまり(`field_groups`)は **detail_view にだけ**書ける。

## 3. `update_view` の `changes` に書けるのは27キー

`name`, `columns`, `sort`, `filter`, `fields`, `actions`,
`preset_column_align`, `preset_column_width`, `preset_pager_position`,
`preset_label_placement`, `preset_field_columns`, `preset_image_size`,
`preset_text_preview`, `preset_list_shape`, `preset_density`,
`custom_css`, `menu_listed`, `field_groups`, `modal`,
`search_fields`, `page_size`, `after_save`, `sum_field`, `reference_pickers`,
`report`, `after_delete`, `flow`

**この27個に入っていない主なもの**(= `update_view` では変えられない):

- `id` / `type` / `table` —— 画面の同一性そのもの
- `related` —— 子一覧。後から変えるには `remove_view` してから `add_view` で作り直す
- `audience` —— **かつてはここに「見せる相手」があった。V8-M20 が画面定義ごと撤去した** ——
  **今日その名前を書くと差分全体が拒否される。**見せる相手は下の §3a に書く

**`update_view` は差し替えであって、既存のテーブル・フィールド・行を1件も消さない。**

## 3a. 「見せる相手」は画面ではなく役割の規則に書く(`set_roles`)

**画面・項目・ボタンを誰に見せるかは、画面定義にも項目定義にも書かない。**
**`app.roles[].rules` に書き、差分操作は `set_roles`(全置換)である。**

```json
{ "op": "set_roles",
  "roles": [
    { "id": "owner",  "name": "管理" },
    { "id": "editor", "name": "編集" },
    { "id": "viewer", "name": "閲覧",
      "rules": [
        { "target": "view",  "view": "task_list", "can": ["read"] },
        { "target": "field", "table": "task", "field": "cost", "can": ["read"] },
        { "target": "table", "table": "task", "can": ["read", "write"],
          "when": { "and": [ { "field": "state", "equals": "doing" },
                             { "field": "assignee", "equals_current_user": true } ] } }
      ] }
  ] }
```

| 対象(`target`) | 一緒に必須 | 書ける `can` | `when` |
|---|---|---|---|
| `table` | `table` | `read` / `write` / `delete` | 書ける |
| `field` | `table`, `field` | `read` / `write` | 書ける |
| `view` | `view` | `read` だけ | **書けない** |
| `action` | `view`, `action` | `read` だけ | **書けない** |

- **`target` と `can` はどの規則でも必須。** 対象4種・動詞3語の外は書けない。
  **【2026-08-12 訂正(`V8-M32`。`V8-M28` / `ADR-0322`)。直前の「対象4種」を1バイトも消していない】**
  **その字面は今日は偽で、対象は6種である** —— **5種目は `app`(アプリの設定 = 定義の変更)、**
  **6種目は `role`(人に役割を配ること)。****上の表には行が2つ足りない。**
  **この2つに書ける動詞は `write` 1語ちょうど**(`read` も `delete` も書けない)で、
  **名指しする先(表ID・項目ID・画面ID・ボタンID)も条件(`when`)も1つも書けない。**
  **【禁止】これを「権限管理ができるようになった」と書かない** —— 足したのは対象2つだけである。
- **`can` に書けるのは「できること」だけで、「できないこと」を書く値は1つも無い。**
- **どの規則からも名指しされていない対象は、今日どおり全員に開いている**(既定は「変えない」)。
  **1本でも書くと、その対象は「書いた役割だけ」に閉じる。**
  **【2026-08-12 訂正(`V8-M32`。`V8-M26` / `ADR-0317` / ユーザ決定 `D-V8-38` / `D-V8-42`)。直前の2文を1バイトも消していない】**
  **前の1文は今日は偽である** —— **規則が1本も無い対象は、既定で閉じる**
  (表 / 画面 / ボタン / `app` / `role` の5対象)。
  **項目(`field`)だけは今日も管轄外で、全員に開いている** —— **書き忘れた項目は「見えない」ではなく「見える」に落ちる。**
  **ただし表・画面・ボタンを足すと、既定の3役割(持ち主・編集者・閲覧者)ぶんの規則が自動で入る。**
  **【禁止】これを「6対象すべてで既定を閉じた」と書かない。**
  **【2026-09-05 訂正。直前の箇条(既定は閉じる / 項目だけ開く / 足すと3役割ぶん入る)を1バイトも消していない】**
  **上の3つは、ボタンについては今日は足りない** ——
  **行ごとの付与(表の定義の `access_control`)を宣言した表では、規則で開いていても、
  その行への付与が無ければボタンは出ない。**
  **見る対象は形ごとに違う** —— 値の書換(`set`)と自動処理(`run`)はその行の書込、
  別画面へ移るボタン(`view`)はその行の読取、付与表に行を作るボタン(`form`)はその行に付与を配れるか。
  **宣言していない表は今日どおり、役割の規則だけで決まる。**
  **出さないだけで、遮断は今日もサーバの 403 である** —— 画面から消えるので、
  **「そもそも無い」と「自分に権限が無い」を利用者は見分けられない。**
  **ボタンの出し分けは、あなた(この口)・受信口・自動処理・コードの島・URL の直叩きには1バイトも掛からない。**
  **【2026-09-05 追記(台帳 `RB-G7` / `RB-G8`)。直前の4形の列挙を1バイトも消していない】**
  **出し分けられるのは宣言由来の4形だけではない** ——
  **詳細画面に元から在る「編集」「削除」の2つのボタンも、同じように行ごとの判定で出し分けられる。**
  **見る対象はここでも動詞ごとに違う** —— **「編集」はその行の書込、「削除」はその行の削除である**
  (押した先でサーバが見る動詞に合わせてある)。
  **宣言していない表では、この2つのボタンは今日と1バイトも同じ見え方である。**
  **ここでも出さないだけで、遮断は今日どおりサーバの 403 である** ——
  **URL を直に叩けば入力画面は今日どおり開き、保存を押した時点で 403 になる。**
  **この出し分けは、あなた(この口)・受信口・自動処理・コードの島・URL の直叩き・保存ボタンには
  1バイトも掛からない。**
  **付与で「編集」が消えた相手に、「閲覧のみ」の注記は1文字も出ない。**
- **条件(`when`)は行ごとに判定される。**葉は「項目の値が定数と等しい」と
  「項目の値が要求している人と等しい(担当が自分)」の2形だけで、枝は `and` / `or`(2件以上)/
  `not`。**深さの上限は8段**で、比較(`gt` / `lt`)も集計値も書けない(全量は参照ファイル)。
  **【`V8-M26` / ユーザ決定 `D-V8-70`。2026-08-11。直前の「2形だけ」を1バイトも消していない】
  その字面は今日は偽である** —— **葉は3形で、3つ目は
  `{ "field": "<項目ID>", "is_empty": true }`(その項目が空)である。**
  **空とみなすのは `null` と、値が入っていないことと、長さ0の文字列の3つだけで、`0` と `false` は空ではない。**
  **`is_empty` に書けるのも `true` だけ**(「空でない」は `not` で書く)。
  **この形は要求している人を1バイトも見ないので、ログインしていない要求でも成り立つ。**
- **効くのはブラウザからログインして使う人が通る HTTP のレコード経路だけである。**
  **MCP(あなた)・受信口・ワークフロー・コードの島には1バイトも掛からない。**
  **【`V8-M22` / 台帳 `J-G35`。2026-08-10。直前の1文を1バイトも消していない】この1文は今日は偽である** ——
  **`V8-M21` が3経路に判定を配線した**(受信口 / 登録・更新をきっかけに動く自動処理 / コードの島)。
  **今日も規則を1つも通らないのは2本だけで、理由は別々である** ——
  **(1) MCP(あなた)は常に管理者として扱うと決まっている / (2) 決まった時刻に動く処理(`schedule`)は素通しのままにすると決まっている。**
  **【2026-08-12 訂正(`V8-M32`。`V8-M31` / `ADR-0327` / `ADR-0328`)。旧の2文を1バイトも消していない】**
  **(1) は今日は偽である** —— **MCP(あなた)は起動設定の `ST_MCP_ACTOR` で「この人として動く」を名乗り、**
  **名乗った人の権限で判定される**(名乗りが無いと23本のツールはすべて失敗する)。
  **今日も規則を1つも通らないのは (2) の1本だけで、0本ではない。**
  **同じワークフローでも、登録で動けば規則が効き、時刻で動けば効かない。**
  **【2026-08-16 訂正(`V8-M13-T04`。台帳 `Q-G30`)。直前の括弧を1バイトも消していない】**
  **括弧の中の本数は今日は偽である** —— **`V8-M13-T02` が `read_report` を足したので、今日は24本である。**
  **【2026-08-24 訂正。直前の1文を1バイトも消していない】**
  **直前の「今日は24本である」も今日は偽である** —— **`list_comments`(積まれたコメントを読む参照系)を1本足したので、今日は25本である。**
  **足したのは読む道具1本だけであり、書く道具は1本も増えていない。**
  **【2026-08-25 訂正。直前の2文を1バイトも消していない】**
  **直前の「今日は25本である」も、直前の「書く道具は1本も増えていない」も今日は偽である** —— **`set_comment_visibility`(コメントの欄と読む場所を画面に出すかを倒す書く道具)を1本足したので、今日は26本であり、名乗りが無ければその26本すべてが今日も失敗する。**
- **役割の規則(面)と、表に宣言する行ごとの付与(`access_control`。点)は、どちらか一方が許せば通る** ——
  **両方が宣言されている表でだけ重なる。** **規則で閉じた行でも、その行に付与があれば見える。**
  **閉じたいときは面と点の両方で閉じること。**
- **【2026-09-09 追記。直前の箇条を消していない】**
  **同じ行に複数の付与が付いているときは、強い側が勝つ** —— **読む・書く・消すのそれぞれについて、1件でも許す付与があれば通る。**
  **弱い付与を足して範囲を狭める手段は、今日1つも無い** —— **狭めたいときは、その付与そのものを消すこと。**
- **【2026-09-10 訂正。直前の2行を1バイトも消していない】直前の2行は今日は偽である** ——
  **弱い付与を足して範囲を狭める手段が、今日は1つある。**
  **権限名の宣言に `restrictive: true`(この権限名は上限である、の符号)を書くと、その権限名の付与は「できることを足す」と同時に「その行のその人にできることを、そこまでに絞る」ものになる。**
  **同じ行に強い付与があっても、上限の付与が1件でも効いていれば、答えはその上限までである。**
  **上限は、相手を直接名指しした付与だけでなく、グループ経由で解けた付与にも、引き継ぎ元(親)から降りてきた付与にも効く** ——
  **あとからその人をグループに入れたことで、その人にできることが減ることがある。**
  **上限だけを持つ人は、その上限が許すぶんをそのまま受け取る**(0 にはならない)。
  **ただし、上限で書けない人でも、同じ行について他の人に強い権限を渡す付与は作れる** ——
  **付与を作ってよいかの判定に、上限は1つも掛かっていない。**
  **符号を1つも書いていないアプリの答えは、今日と1ビットも変わらない。**
- **【2026-09-06 訂正。直前の箇条を1バイトも消していない】「どちらか一方が許せば通る」は、行を作るときには足りない** ——
  **表の `access_control` に `inherit_from` を書いた表では、面と点のどちらかが許していても、その参照が指す親の行に書けなければ `403` になる。**
  **この判定は面と点の合成(どちらか一方)の外側に在り、合成に入る前に断る。**
  **`creatable_by` も書いた表では、加えて、並べた権限名のいずれかを親の行について持つことが要る。**
  **効くのはブラウザ画面と HTTP のレコード作成の2経路(単件 / まとめ書き)だけで、あなた(この口)・受信口・自動処理・コードの島は今日も素通りする。**
  **読取・更新・削除は今日どおり「どちらか一方が許せば通る」である。**
- **【2026-09-07 訂正。直前の1文を1バイトも消していない】「あなた(この口)・受信口・自動処理・コードの島は今日も素通りする」は、行を**作る**側については今日は偽である** —— **作成の関門は、今日はあなた(この口)・受信口・自動処理・コードの島の4本の入口にも掛かる** —— **`write_records` / `insert_sample_data` でも、これから作る行が指す親の行に書けなければ `403` で断られる。** **双方向に書く** —— **素通りするのは、決まった時刻に動く処理(`schedule`)1本だけである。** **親の付け替え(既存の行の参照を別の親へ変える**更新**)が効くのは今日も画面と HTTP だけで、この4本の入口の更新は今日も素通りする** —— **親を指さずに作ってから、あとで親を書き入れる道は今日も開いている。** **受信口には逃げ道が今日1つも無い**(書込主体は固定で、「誰の権限で動かすか」を書く場所が今日どこにも無い)。 **【禁止】これを「塞いだ」「安全になった」と読まない。**
- **【2026-09-13 訂正。直前の1文を1バイトも消していない】直前の「親の付け替え(既存の行の参照を別の親へ変える**更新**)が効くのは今日も画面と HTTP だけで、この4本の入口の更新は今日も素通りする」は今日は偽である** —— **AI の口(この口)の**更新**にも、今日は親の関門が掛かる** —— **1件ずつ直す `update_record` と、まとめて書き換える `write_records` の `update` op の、どちらにも掛かる。** **掛かる口は、画面・HTTP・AI の口(この口)である。** **双方向に書く** —— **素通りする口は、受信口・自動処理・コードの島の3本である** —— **決まった時刻に動く処理(`schedule`)も今日どおり素通りする。** **あわせて、親を別の親へ付け替えるとき、**移す前の親**に要求する権限は「書ける」から「消せる」へ上がった**(移した先の親に要求するのは今日どおり書込である)。 **参照を**空にする**更新は今日も通る。** **【禁止】これを「塞いだ」「もう起きない」と読まない。**
- **【2026-09-09 追記。上のどの箇条も消していない】「どちらか一方が許せば通る」は**読むとき**の話である。**
  **その表を書き先とする名前つきのボタンが1本でもあると、そのボタンを読めない相手は、行ごとの付与で配られていても作成・更新が 403 になる。**
  **識別子(`id`)を書いていないボタンは名指しできないので、壁の材料にならない。**
  **消すこと(削除)と読むことには、この壁は1文字も掛からない**(書込の種類は作成と更新の2つだけである)。 **名前つきのボタンが1本も無い表では、この壁は立たない。**
  **双方向に書く** —— 読むときは点(付与)が漏れる(規則で閉じた行でも付与があれば見える)一方で、書くときは点が届かないことがある(そのボタンを読めなければ 403)。
  **【禁止】これを「塞いだ」と読まない** —— 面と点の重ね方も、その前に立つ親の関門も1ミリも動いていない。変わったのは説明だけである。
- **【2026-09-11 追記。上のどの箇条も1バイトも消していない】「どちらか一方が許せば通る」は、**読むとき**については今日は不完全である。**
  **行ごとの付与(`access_control`)を宣言している表では、役割の規則に「条件を1つも付けない読取」を1行書いても、それだけではその表の行を1件も読めない** —— **読むには、その行ごとの付与が要る。**
  **ただし運営者(役割 `owner`)は今日どおりその表の行を全部読める**(この例外はユーザの決定であり、**すり抜けが1本残るということである**)。
  **条件つきの規則の読取は今日どおり効く。** **行ごとの付与を1件も宣言していない表の答えは、今日と1バイトも変わらない。**
  **書込(作成・更新)と削除は1ビットも変えていない** —— **今日どおり「どちらか一方が許せば通る」であり、「この行は閲覧のみ」と印を付けた行でも、その表への書込を役割の規則として持つ人の書き換えは今日も止まらない。**
  **双方向に書く** —— **読むときは条件なしの規則1行で行ごとの付与を丸ごと無効にはできなくなった一方で、運営者と、書くこと・消すことについては今日と同じである。**
  **【禁止】これを「塞いだ」「すり抜けが1本も無くなった」と読まない** —— **変わったのは読むときの答えだけである。**
- **`{ "target": "table", ..., "can": ["read"] }` は `st_owner`(個人所有)の壁を越える** ——
  **その役割の人にはその表の全員分の行が見える。**書く前に人間に確認すること(参照ファイルの逐語)。

- **【2026-08-16 追記(`V8-M47`)。直前の2つの箇条書きを1バイトも書き換えていない】**
  **上の2文は今日も正しい。足りない1点だけを足す** —— **点(付与)は `st_owner` の壁を越えない。**
  重ね方の実装は **( `st_owner` が一致 **または** 面が表全体の読取を許している ) かつ ( 面 **または** 点 )**
  の1本で、**壁を越えるのは面(`target: "table"` の `read` の規則)だけである。**
  **点はその「かつ」の内側にいる。**
  - したがって **`st_owner` と `access_control` を併せ持つ表では、見える行は
    「自分の行 かつ 付与のある行」に潰れる。** 付与を足しても他人の行は1件も見えない。
  - **壁を越えるのは読取だけである** —— **`write` と `delete` は越えない。**
- **【2026-08-16 追記(`V8-M47`)。ブラウザで実測した】**
  **項目(`field`)の読取の規則を書いても、詳細画面の編集ボタンは消えない。**
  **サーバはその項目への書き込みを 403 で断る**(文面は
  「項目 "<項目ID>" に対する書き込みは、あなたの役割に許されていません(止めた層: role)。」で、
  `<項目ID>` のところに実際の項目IDが入る)——
  **それでも画面はボタンを1つも消さず、「閲覧のみ」の注記も出さない。**
  画面が見ているのは役割と `st_owner` の所有権だけで、
  **項目の読取の規則を見る呼び出しが画面の側に1つも無い**ためである。
  - **書ける項目が1本も無い相手にも「編集」が出る。** 押した先は入力欄が0本のかたちになり、
    **そこで保存を押すと、値を1つも変えない更新が 200 で通る。**
  - **入力欄は書込の規則で黙って落ちる。** **書けない理由は画面に1文字も出ない。**
  - **項目の書込の判定は、要求の本文に載っている項目だけを見る** ——
    **その項目を書かない更新は通る。**
  - **【2026-09-05 訂正(台帳 `RB-G7` / `RB-G8`)。上の「それでも画面はボタンを1つも消さず」から
    直前の箇条までを1バイトも消していない】**
    **「画面が見ているのは役割と `st_owner` の所有権だけ」は今日は偽である** ——
    **画面は行ごとの付与(`access_control`)も見る。**
    **宣言由来のボタン(`set` / `run` / `view` / `form`)に続いて、
    詳細画面に元から在る「編集」「削除」の2つのボタンも、その行の判定で出し分けられるようになった。**
    **「書ける項目が1本も無い相手にも「編集」が出る」は、項目の規則については今日も真である** ——
    **項目に書いた読取・書込の規則だけで「編集」が消えることは、今日も1度も無い。**
    **偽になるのは行ごとの付与を宣言した表だけで、そこではその行を書き換えられない相手に「編集」が出ず、
    その行を消せない相手に「削除」も出ない。**
    **「項目の読取の規則を見る呼び出しが画面の側に1つも無い」は今日も真である**(直っていない)。
    **付与で「編集」が消えた相手にも、「閲覧のみ」の注記は1文字も出ない**(引き受けた代償。ユーザ決定 `D-V14-1`)。
- **【2026-08-16 追記(`V8-M47`)】自分で名乗って登録することを止めるかどうかは、
  役割の4キー目 `signup` に書く。**
  `app.roles[]` に書けるのは `id` / `name` / `rules` / `signup` の4キーで、`signup` はその4つ目である。
  **`signup` に書ける値は `open` と `invite` の2つちょうどで、3つ目は無い。**
  **書かなければ `open` として扱われる。**
  - **【誇張しない】`invite` にしても、最初の1人は常に通る** ——
    関門が外れるのは**アプリ全体の登録者が0人のとき**であって、役割ごとではない。
  - **【誇張しない】予約3語(`owner` / `editor` / `viewer`)だけを `invite` にしても、
    それ以外の立場を名乗る自己登録は今日どおり通る。**
  - **【禁止】これを「自称を止められる」「勝手に名乗れなくなる」と書かない。**
- **【2026-08-25 追記】画面を足す差分を「当てずに試す」のにも、
  `target: "app"` / `can: ["write"]` が要るようになった。**
  **`dry_run_diff` に `apply_diff` とまったく同じ判定が掛かる。**
  - **役割の規則を1本も書いていないアプリでは、持ち主でも断られる**
    (直し方は `set_roles` で `owner` にその1行を書き戻すこと)。
  - **画面の「見せる相手」(`target: "view"`)をいくら書いても、この壁は開かない** ——
    **見るのは `target: "app"` の規則だけである。**

## 4. 値の形(取り違えが多いところ)

| キー | 形 |
|---|---|
| `columns` / `fields` / `search_fields` | **フィールドID(文字列)の配列**。1件以上。定義オブジェクトの配列ではない。`search_fields` は最大8件 |
| `sort` | `{ "field": "due_on", "order": "asc" }` **または**その配列。`field` と `order` の**両方が必須**。`order` は `asc` / `desc` |
| `filter` | **木**。葉は `{ "field": "...", "equals": ... }` の形で、`equals` / `contains` / `gte` / `lte` / `in` の**どれか1つだけ**を持つ。枝は `{ "and": [...] }` / `{ "or": [...] }` / `{ "not": {...} }` |
| `related` | 配列。各要素は `table` / `via` / `columns` が**必須**で、`sort` と `name` と `id` が任意。`id` は名札で、**画面には1文字も出ない**(画面に出るのは `name` のほう)。**同じ画面に同じ `id` を2度書くと差分全体が拒否される** |
| `actions` | 配列。各要素は `form`+`prefill` / `set` / `view` / `run` の**どれか1組**を持つ |
| `custom_css` | `{ "asset": "...", "digest": "<sha256 の16進64文字>" }`。**両方必須** |
| `menu_listed` / `modal` | 真偽値 |
| `page_size` | **`10` / `20` / `50` / `100` の4つだけ**(整数。自由な数は書けない) |
| `after_save` / `sum_field` | ID(文字列)1つ |

**ID の形は `^[a-z][a-z0-9_-]*$` の 1〜64文字。** 大文字・日本語・空白は使えない。

### `actions` の1件に書けるキーと、出す条件(`visible_when`)

**【2026-08-16 追記(`V8-M47`)。上の表の `actions` の行を1バイトも書き換えていない】**
**1つのボタンに書けるキーは8つで、9つ目は書けない。**

| キー | 何を書くか |
|---|---|
| `id` | ボタンのID。**役割の規則(`target` が `action`)で名指しするにはこれが要る** |
| `name` | 画面に出す名前 |
| `form` | 開く入力画面のID(`prefill` と対で書く) |
| `prefill` | 開いた入力画面に先に入れる値 |
| `set` | 今開いている行の項目を書き換える |
| `view` | 開く画面のID |
| `run` | 動かすワークフローのID |
| `visible_when` | **このボタンを出す条件**(下) |

**`form`+`prefill` / `set` / `view` / `run` の4形は排他で、どれか1組ちょうどを書く。**

**`visible_when` の値は絞り込みの葉(`filter_leaf`)そのものである。**
**葉に書けるキーは6つ**(`field` / `equals` / `contains` / `gte` / `lte` / `in`)で、
**`field` が必須、残りの5つのうちちょうど1つを書く。** **枝(`and` / `or` / `not`)は1つも書けない。**
実物で見つかった形は `{ "field": "done", "equals": false }` と
`{ "field": "status", "equals": "active" }` の2つで、
**`equals` 以外の演算子を使った実物は、探した範囲では見つからなかった。**

**【混ぜないこと】ワークフローの `when` と同じ形ではない。**
**あちらは別の定義で、書けるキーは `field` と `equals` の2つだけ・両方が必須である。**
**「ワークフローの `when` と同じ」は今日は偽である。**
**同じなのは、どちらも `and` / `or` / `not` を1つも書けないという点だけである。**

**ボタンを隠すことは、書き込みを止めることではない。**

## 4a. 一覧画面の「新規作成」(宣言するキーは無い)

**一覧画面(`list_view`)には「新規作成」のボタンが自動で出る。** **このボタンを表すキーは1つも無く、書いて出すことも、書いて消すこともできない。**

出るのは、次の4つが**全部**そろったときだけである。

- 同じテーブルの `form` が在る(2本以上あるときは**定義順の先頭**の1本)
- その `form` がその人に見える(役割の規則で、その画面の読取が許されている)
- その人がそのテーブルへ書ける
- そのテーブルが `st_no_direct_create` を宣言していない

**押すと、その `form` の新規作成の画面が開くだけである** —— **一覧の絞り込みの値も、検索語も、1つも引き継がない。** **「この一覧では出さない」と画面ごとに書き分けることもできない**(消したいときは、テーブルの側に `st_no_direct_create` を宣言する)。

**行ごとの操作起点(`actions`)の「新規作成へ」とは別のボタンである** —— あちらは押した行の値を次の画面に渡すが、こちらは1つも渡さない。

## 4b. 一続きの流れ(`flow`)—— 段を宣言し、次の段へ進める

**一覧 → 入力 → 確認 → 完了 のように、画面をいくつか続けて通す道を宣言できる。**
**宣言するキーは `flow` の1本だけで、流れそのものを表す入れ物(「flow の一覧」のような
別の定義)は語彙のどこにも無い。**

### 値の形は3つだけ

```json
"flow": { "id": "apply", "step": 2, "kind": "input" }
```

| 鍵 | 何を書くか | 値域 |
|---|---|---|
| `id` | **その流れの名前**(画面のIDではない) | 文字列 |
| `step` | **その流れの中の位置** | **1から始まる整数**(0も負も小数も書けない) |
| `kind` | **段の種類** | **`input`(入力の段)/ `confirm`(確認の段)の2つだけ** |

**3つとも必須で、4つ目の鍵は無い。** 段の見出し・アイコン・色・所要時間・分岐の条件などを
書く場所は1つも無く、書いた差分は拒否される。

### 流れは「同じ名前の画面を位置の順に並べたもの」である

- **同じ `id` を書いた画面どうしが1つの流れになる。** **複数の画面が同じ `id` を持つのが
  正常であり、`id` の重複は拒否されない。**
- **並び順を書く場所は無い。** 並びは `step` から導かれる。
- **同じ `id` の中で `step` が重なっている差分は拒否される。**
- **同じ `id` の中で `step` が飛んでいる差分(1,2,4 のような欠番)も拒否される。**
- **`step` が 1 から始まっていない差分も拒否される。**
- **1段だけの流れは書ける**(その画面には次の段が無いので、次へは進まない)。

### 書ける種別

- **書けるのは一覧画面(`list_view`)・入力画面(`form`)・詳細画面(`detail_view`)の3種だけ。**
- **集計表(`report_view`)には書けない。** 書いた差分は拒否される。
- **確認の段(`kind: "confirm"`)にできるのは詳細画面だけである。**
  **入力画面や一覧画面に `confirm` を書いた差分は拒否される。**

### 宣言があれば段、無ければ今日どおり

- **段を宣言した画面では、次の行き先を決めるのは段の並びである。**
- **`after_save` と `flow` の両方を書いた画面では、段のほうが勝つ**
  (`after_save` は、段が解けなかったときにだけ効く)。
- **段を1つも宣言していない画面は、今日どおりの決まりで動く。**
  **`flow` を足しても、書かなかった画面の行き先は1バイトも変わらない。**
  - **【2026-08-21 訂正。上の2行は1バイトも消していない】**
    **1行目(段を宣言していない画面そのものの振る舞い)は今日も真である。**
    **偽になったのは2行目のほうである。** **`flow` を足すと、その画面を行き先にしていた
    別の画面の行き先が変わる** —— **一覧の行を押したとき・参照の項目のセルのリンク・
    子一覧の行の3つは、段を宣言した詳細画面を行き先の候補から外す。**
    **外した結果1枚も残らなければ、その行は押せない**(別の画面へは倒さない)。
    **段を1つも宣言していない画面の飛び先も、この形で変わる** ——
    同じ表に段になった詳細画面が在れば、そこへはもう飛ばない。
    **保存が成立したあとの行き先だけは今日も変わっていない。**

### 次の段へ進むきっかけ(**種別で違う**)

| その画面 | 次の段へ進むきっかけ |
|---|---|
| 入力画面(`form`) | **保存が成立したとき。**「次へ」のボタンは出ない(送信ボタンは今日も1つである) |
| 一覧画面(`list_view`) | **「次へ」のボタンを押したとき** |
| 詳細画面で `kind` が `input` | **「次へ」のボタンを押したとき** |
| 詳細画面で `kind` が `confirm` | **「次へ」は出ない。** その画面に書いた**値の書換のボタン**(`actions` の `set` の形)の書込が成立したとき |

**「次へ」のボタンが出るのは、次の全部がそろったときだけである。**

- その画面が段を宣言している
- その画面が入力画面(`form`)ではない
- その段の `kind` が `confirm` ではない
- **同じ流れの `step + 1` を宣言した画面が、そのアプリに実在する**(最後の段では出ない)
- **その画面が、見ている人に見える**(役割の規則で読取が許されている)

**1つでも欠ければボタンごと出ない。** **押せないボタンは出さない。**

**自動処理を起こすボタン(`actions` の `run` の形)では、段を宣言していても次の段へ
1ミリも進まない。** **進めるのは `set` の形だけである**(起動が受理されても処理が
失敗することがあり、進む時点で成否が確定していないため)。

**【隠さずに書く】`kind` が `input` の詳細画面に `set` の形のボタンを書くと、その書込でも
次の段へ進む。** そのとき同じ画面に「次へ」も出るので、**進み口が2つ並ぶ。**
**確認の段として使う画面には `kind: "confirm"` を書くこと。**

### 次の段へ運ばれるもの(**行1つだけ。しかも条件つき**)

- **運ばれるのは、次の段が「同じテーブルの詳細画面」のときの、いま扱っている行1件だけである。**
- **それ以外では何も運ばれない** —— 次の段が一覧画面・入力画面・別のテーブルの詳細画面の
  ときは、行も値も1つも渡らない。
- **前の段で入力した値は1つも運ばれない。** **確定していない入力を段のあいだで
  持ち回す仕組みは1つも無い**(途中の値を残したいなら、段ごとに行として保存する)。
- **一覧画面から進むときは、行を1件も指していないので何も運ばない。**

### 段を宣言すると自動で出るもの(**宣言する口は1つも無い**)

**段を宣言した画面には、こちらが1バイトも書かなくても2つのものが出る。**
**どちらも出し方・文言・位置を宣言する口は1つも無く、出すか出さないかも選べない。**

#### (1) いま何段目 / 全部で何段(**3種別すべてに出る**)

- **段を宣言した画面には `3段目 / 全5段` の形の表示が1つ出る。**
- **出るのは2つの数だけである。** 段の名前・種類(`input` / `confirm`)・進み具合の割合・
  残り時間は1文字も出ない。
- **押せない。** その表示から他の段へ飛ぶことはできない(飛べる目次ではない)。
- **段を宣言していない画面には1ピクセルも出ない。**
- **重ねて出す画面(`modal: true`)にも出る。** —— **そこには「戻る」は出ないので、
  重ねた画面では「いま何段目か」だけが出ることになる**(閉じる操作が既に履歴を1つ戻すためである)。
- **画面の名前(`name`)に段番号を手で書かないこと** —— **同じ数が2箇所に並び、
  あとで `step` を入れ替えたときに名前のほうだけが古い数のまま残る。**
  **この食い違いで赤くなる検査は1本も無いので、誰も気づかないまま残る。**

**いま何段目かは、同じ流れの画面を `step` の順に並べたときの順位である。**
**`step` の値そのものではない** —— 欠番のある古い定義が残っている場合、`step` の値と
表示される順位はずれる(新しく書く差分は欠番が拒否されるので、ふつうは一致する)。

**全部で何段かは、見る人によって変わることがある**(**隠さずに書く**):

| 見る人 | 総数に入る段 |
|---|---|
| ログインしている人 | **その人には開けない段も総数に入る。** 数は流れの宣言の全量と同じになる |
| ログインしていない人 | **見せると決めた段だけが総数に入る。** 同じ流れが小さい数に見える |

**どちらの数え方にするかを宣言する口は無い。**

#### (2) 「戻る」(**入力画面にだけ出る**)

**【2026-08-21 訂正。上の見出しと下の行は1バイトも消していない】**
**上の見出しの「入力画面にだけ出る」は、今日は偽である。**
**「戻る」は、段を宣言した画面には種別を問わず出る** —— **一覧画面にも、詳細画面にも、
入力画面にも、それぞれ1つ出る。** **今日の正はこの訂正のほうである。**
**出ないのは「段を宣言していない画面」と「重ねて出す画面」の2つだけである。**

- **段を宣言した入力画面(`form`)には、「戻る」のボタンが1つ出る。**
  - **【2026-08-21 追記】** **入力画面だけではない** —— **段を宣言した一覧画面と
    詳細画面にも、同じ「戻る」が1つ出る。** **確認の段(`kind` が `confirm`)にも出る。**
- **押すと、ブラウザの履歴を1つ戻る。**
- **戻り先を宣言する口は1つも無い。** 段の並びを1つ遡るのではなく、**履歴を戻るだけ**である。
- **URL を直接開いた場合は、履歴にその前の項目が無い。** そのときは押しても何も起きない
  (ボタンは出たままである)。
- **重ねて出す画面(`modal: true`)には出ない。** その器は閉じる操作が既に同じことをしている。
- **段1(`step` が 1)の画面にも出る。** そのときの行き先は流れの外である。
- **一覧画面(`list_view`)と詳細画面(`detail_view`)には出ない。** 出るのは入力画面だけである。
  - **【2026-08-21 訂正。上の1行は1バイトも消していない】** **その字面は今日は偽である。**
    **段を宣言した一覧画面と詳細画面にも「戻る」は1つ出る。** **段の位置でも段の種類でも
    分岐していないので、最初の段にも最後の段にも出る。**
    **出し方・文言・位置・出す出さないを宣言する口は、今日も1つも無い。**

**「次へ」とは向きが逆なだけの対ではない** —— 「次へ」は段の並びから次の画面を解いて進むが、
「戻る」は段の並びを1バイトも見ていない。

### 今日は書けないもの(**言い換えても書けない**)

- **ボタンの文言。** 「次へ」も、入力画面の送信ボタンも、文言は宣言できない。
- **流れの外へ出る行き先。** **流れは1つのアプリの中に閉じており、外部の URL も、
  別のアプリの画面も書けない。**
- **段の分岐**(条件で次の段を変える)。次は `step + 1` の1つだけである。
- **段のあいだで未確定の入力を持ち回すこと。**
- **段の見出し・アイコン・所要時間。**
- **位置と総数の表示の出し方・文言・位置。** 出すか出さないかも選べない。
- **「戻る」の文言と戻り先。** 履歴を1つ戻る以外の行き先は書けない。

## 5. `preset_` で始まる9軸 —— 値は決まった選択肢だけ

| キー | 値 |
|---|---|
| `preset_column_align` | 列ID → `left` / `center` / `right` |
| `preset_column_width` | 列ID → `narrow` / `standard` / `wide` |
| `preset_pager_position` | `top` / `bottom` / `both` |
| `preset_label_placement` | `inline` / `stacked` |
| `preset_field_columns` | `1` / `2`(整数) |
| `preset_image_size` | `thumbnail` / `medium` / `original` |
| `preset_text_preview` | `short` / `standard` / `long` / `full` |
| `preset_list_shape` | `table` / `card` |
| `preset_density` | `comfortable` / `compact` |

**9軸・合計24の選択肢で全部である。** 数値・割合・CSS 文字列・ピクセル座標は1つも書けない。

## 6. できないこと(語彙の正からの逐語)

> 【できないこと・具体例】次のような要求は、言い換えても実現できません。

> 「CSSを当てて」「この項目をここに置いて」「この画面だけ色を変えて」(**あなた(AI)が書ける任意の CSS と、用意された選択肢の外の見せ方はありません** —— CSS を書き込める場所は1つもなく、ピクセル座標・絶対配置・重ね順を指す語彙もありません。

> **ただし見た目のうち配色・書体・余白などは、実際に変えられます** —— **アプリ単位のテーマ(set_theme)**で、あらかじめ決まった25個のスロットに実値を埋める形です。スロットを増やすことはできません。

**見せ方の逃げ道・画面ごとの配色・ダークモード・見せる相手(役割の規則)についての
「できないこと」は [`reference/cannot-do.md`](reference/cannot-do.md) に置いてある。**
**28キーの全量と、役割ID の値域・規則の条件(`when`)の全量は
[`reference/view-keys.md`](reference/view-keys.md) にある。**

## 7. この説明書が保証しないこと

- **画面に規則を書いても、テーブルの URL そのものは閉じない。** 閉じたいときは対象 `table` の
  規則を別に書く。詳しくは参照ファイルの逐語を読むこと。
- **規則を書いても `get_manifest` は未ログインに全画面・全項目の定義を返す。** 隠れるのは値であって定義ではない。
- **語彙の正はこのファイルではない。** 正は `schemas/manifest.schema.json` の `$defs/view` である。
  食い違いを見つけたら、スキーマのほうを信じること。
- **`allowed-tools` の3本は、MCP サーバの登録名が `smailtalk` のときの名前である。**
- **この説明書の版がサーバの実装とずれていても、実行は止まらない。**
  版は plugin の `version` 1本だけで、**版が合わないときに実行を止める仕組みは持っていない。**
  古い説明書を読んだまま差分を書くことがありうる。**食い違いを見つけたら、スキーマのほうを信じること。**
- **この説明書を開いたことで躓きが減るかどうかは、まだ測られていない。**
