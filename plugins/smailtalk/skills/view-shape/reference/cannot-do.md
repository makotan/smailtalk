# できないこと(画面の形) —— 語彙の正からの逐語

**下の引用は `src/mcp/vocabulary.ts` の `CANNOT_DO` からそのまま引いたものである。**
**言い換えていない。要約もしていない。** 引用の外に、この説明書が独自に書き起こした
「できないこと」は1件も無い。

---

## 見せ方 —— 逃げ道・9軸24選択肢・項目のまとまり・画面ごとの配色

> **ただし「逃げ道」だけは別で、これも1文に丸めてはいけません** —— (1)**あなたは CSS の本文を1バイトも書けません**(書けるのは request_custom_css での申請と、owner が既に発行した資産への参照だけです)。(2)**マニフェストの語彙に CSS の本文の置き場はありません**(custom_css に書けるのは資産名と内容ダイジェストの2要素だけです)。(3)**人間の owner が CSS を書いて資産として発行すれば、その画面には実際に効きます**(当ててよい画面の集合は owner が発行時に宣言し、宣言外の画面と未発行の資産は配信層が拒否します)。**ただし画面ごとの「見せ方」は、9つの軸に限って選べます** —— 列の寄せ / 列の幅 / 件数表示とページャの位置 / 項目名と値の向き / 項目の段組数 / image の表示サイズ / long_text の切り詰め長 / 一覧の器の形(表かカードか) / 画面の詰まり具合(ゆったりか密か) の9軸で、値はあらかじめ決まった選択肢(全部で24)から選ぶ形です(add_view / update_view の preset_ で始まるキーに書きます)。**選択肢の外は書けません** —— 「レイアウトを2カラムにして」は詳細画面の項目の段組なら選べますが、3段組も、任意の位置への配置もできません。**8軸のほかに、詳細画面(detail_view)だけは「項目のまとまり(field_groups)」を書けます** —— 関係のある項目どうしをまとめ、見出しつきの区切りで並べます。**書けるのは「どの項目がどのまとまりに属するか」だけで、まとまりの出し方(タブか区切りか)も、まとまりの並び順を指定するキーも、入れ子もありません**(順序は書いた順です)。**ただし、まとまりの値は2形あります** —— (1) 項目IDの配列か、(2) { "fields": [項目IDの配列] } の形かのどちらかです。**(2) の形にだけ名札(id)を書き足せます** —— 名札は省略でき、**画面には1文字も出ません**(画面に出るのは今日どおりまとまりの名前です)。**どちらの形で書いても、画面に出るものは1ピクセルも変わりません。****同じ画面の中で、まとまりの名札を2度書くことはできません**(別の画面なら同じ名札を書けます)。**画面ごとの配色は今もできません**(テーマはアプリ単位で、画面ごとの配色を書くキーはマニフェストに1つもありません。

## 選べない見せ方と、1ページの件数・未設定の表し方

> **入力フォームの見せ方のうち残る5軸**・罫線の見せ方・未設定の表し方・日付の表示形・boolean の記号・数値の桁区切り・ボタンの体裁なども選べません(**1ページの件数は 10 / 20 / 50 / 100 の4つからだけ選べます** —— **自由な数は今日も書けません。**今何ページ目かも書けません。**未設定の表し方については、フィールドに hide_when_empty を書けば 『詳細画面でその行ごと出さない』の1択だけは選べます** —— **「未設定」という文言そのものも、代わりに出す文字列も、1バイトも選べません。

## ダークモードとレスポンシブ

> **ダークモード**(OS 設定や時刻で切り替わる配色)も、画面幅に応じたレスポンシブも**この語彙には1つもありません**(**あなたが差分で指定する手段はありません。マニフェストの語彙にもありません。ただし owner が逃げ道の CSS に @media を書けば、その画面では効きます** —— この3つを「ダークモードは書けない」の1文に丸めないでください)。

## 画面単位の「見せる相手」(役割の規則 `app.roles[].rules`)

**【`V8-M20` で引き直された。旧版はここに `add_view` の `audience` についての逐語を置いていた】**
**そのキーは撤去されたので、今日の `CANNOT_DO` に `audience` という語は1度も現れない。**
**同じ主題を述べる今日の逐語を、そのまま置き直す。**

> **今日は、画面を名指しして見せる相手を書けます** —— 役割の規則(app.roles[].rules)に対象 view と画面のIDを書き、can に read と書くと、**その画面は規則で許した役割以外には一覧に並ばず、その画面を名乗ったレコード API の呼び出しはサーバが 403 で拒否します**。**未ログインの人は anonymous という役割として判定されます** —— **anonymous に read を書いた画面は、ログインしていない人にも描かれ、ログイン画面に導線が出ます。****anonymous はロールではありません**(「ロールを持たない人」を指す値です)—— **anonymous にだけ read を書いた画面は、ログイン済みの誰にも見えません。**

> **ただし守らない経路があります** —— (a)**どの役割の規則からも名指しされていない画面は、今日どおり全員に見えます**(既定は「変えない」)。**注意: ここは以前と向きが逆です** —— **名指しされていない画面は、未ログインの人にも並びます**(以前は「書いていない画面は未ログインには見せない」でした)。**未ログインから隠したい画面は、1画面ずつ規則で名指ししてください。**(b)**画面を名乗らずにテーブルの URL を直接叩いた要求は、今日どおり通ります** —— 画面の規則が遮断できるのは「その画面を名乗った要求」であって、テーブルそのものではありません(テーブルそのものを閉じるには、対象 table の規則を別に書きます)。(c)**get_manifest は未ログインでも全画面の定義を返します** —— どんな画面が存在し、どの役割に見せると書いてあるかは、規則を書いても隠れません。**規則で閉じた画面の定義も、未ログインでそのまま読めます** —— **「規則で隠す」は情報の遮断ではありません。**(d)**規則は set_roles で丸ごと差し替えます** —— 1本だけ足す / 消す手段は無く、書き直すときは全役割の全規則を書き直します(画面の定義そのものは1バイトも変わりません)。(e)**anonymous に read を書いても、その画面に行が出るとは限りません** —— 未ログインに返る行は今日どおり st_public が true の行だけなので、**公開でないテーブルの画面を anonymous に開くと、画面は開けても中身は空か 401 になります。**(f)**未ログインには書き込みの導線を1つも出しません** —— **止めているのは画面ではなくサーバです**(未ログインの作成 / 更新 / 削除は 401)。**訂正します。上の (f) は今日は一部が偽です** —— **画面へのコメントだけは、ログインしていない人でも書けます**(未ログインで書ける口は今日この1本ちょうどで、レコードの作成 / 更新 / 削除は今日どおり 401 です)。**未ログインの書き込みには、送ってくる相手を特定する手がかりも、回数の制限も1つもありません** —— **何度でも書けますし、誰が書いたのかを後から知る手立ても1つもありません**(読み取りは1ミリも開いていないので、書いた本人も自分のコメントを1件も読み返せません)。**訂正します。直前の「画面へのコメントだけは、ログインしていない人でも書けます」は、画面については今日は条件つきです** —— **書く欄が画面に出るのは、そのアプリで書く欄を出す設定にしたときだけで、既定は出しません**(配る版では、設定を出す側に倒しても書く欄は1つも出ません)。**それでも書き込みの口そのものは今日も1バイトも閉じていません** —— **欄が画面に出ていなくても、書き込みは今日どおり通ります**(設定を切っても書けなくなるわけではありません。止まるのは画面の表示だけです)。**訂正します。直前の「読み取りは1ミリも開いていない」は、両方の半分とも今日は偽です** —— **読み取りの口は今日2本あります**(GET /api/apps/:app_id/comments と、この MCP の道具 list_comments)。**「書いた本人も読み返せない」も、役割の規則を1本も書いていないアプリでは偽です** —— **そのアプリでは未ログインのまま、コメントが全件読めます。**(この偽は本審査が作ったものではありません —— 元の文を書いた後に、読む口を足した別のタスクが偽にしました。)**訂正します。直前の「書く欄が画面に出るのは、そのアプリで書く欄を出す設定にしたときだけ」は、未ログインの人には当てはまりません** —— **ログインしていない人には、設定を出す側に倒しても書く欄は1要素も出ません**(画面の側で未ログインを閉じているためで、設定はそのうちの1枚でしかありません)。**したがって画面から書けるのは、ログインしている人のうち、そのアプリで書く欄を出す設定にしたときだけです。****それでも書き込みの口は今日も1バイトも閉じていないので、画面を通さない書き込みは未ログインでも今日どおり通ります。**

**【2026-08-12 訂正(`V8-M32`。`V8-M26` / `ADR-0317` / ユーザ決定 `D-V8-38` / `D-V8-42`)。上の引用の字面は1文字も書き換えていない。引用の外に足している】**
**上の (a)「どの役割の規則からも名指しされていない画面は、今日どおり全員に見えます(既定は「変えない」)」は今日は偽である** ——
**規則が1本も無い画面は、既定で閉じる**(表・ボタンも同じ)。**役割を1つも宣言していないアプリでも閉じる。**
**したがって (a) に続く「名指しされていない画面は、未ログインの人にも並びます」も今日は成り立たない。**
**項目(`field`)だけは今日も管轄外で、全員に出る** —— **閉じたのは3対象であって4対象ではない**
(下の「項目単位の『見せる相手』」の (a) は今日も逐語のまま真である)。
**ただし表・画面・ボタンを足すと、既定の3役割(持ち主・編集者・閲覧者)ぶんの規則が自動で入る。**
**`CANNOT_DO` の側は今日もこの訂正を持っていない** —— **直す先は `src/mcp/vocabulary.ts` であって、この説明書ではない。**

**【2026-09-12 訂正(この節では2本目)。上の引用も、直前の訂正の塊も1バイトも消していない】**
**上の (b)「画面を名乗らずにテーブルの URL を直接叩いた要求は、今日どおり通ります」は、画面の規則の側から見ても今日は偽である** ——
**画面名を名乗らない読取にも、画面の規則の壁が立つ。**
**見るのは、その表を指す画面のうち、要求の形に合うものである** ——
**一覧を読む口は、その表を指す一覧画面と集計画面を見る。1行だけを読む口は、その表を指す詳細画面と入力フォームを見る。**
**そのうち1本でも読める画面があれば今日どおり通り、1本も読めなければ止まる。**
**止まり方は口によって違う** —— **一覧は応答から落ちて0件になり、1行だけを読む口は「その行はありません」と同じ答えになる。**
**その表を指す、その形の画面が1本も無いときは今日どおり通る**(そのときこの壁は1度も立たない)。
**画面を宣言していて「誰に見せるか」を1行も書いていない場合は止まる**(規則が1本も無い画面は既定で閉じるためで、直前の訂正と同じ向きである)。
**変わったのは読取だけである** —— **作成・更新・削除の答えは1ビットも変わっていない。**
**まとめ書きの口・集計表の口・「その行が誰に配られているか」を返す口・到達不能の行を返す口も、答えが1ビットも変わっていない。**
**あなた(MCP)が読むときにも同じ壁が立つ** —— **あなたには画面名を渡す手段が1つも無いので、つねに「名乗らない一覧」として判定される。**
**どこまで届くかをはっきり書く** —— **画面の定義そのものは1ミリも隠れていない。**
**あなたは今日どおり設計図を丸ごと読めるし、「見せない」と決めた画面の定義もそのまま返る** —— **返らなくなったのは行であって、画面の定義ではない。**
**【禁止】これを「安全になった」と読まない。** **【禁止】これを「画面の定義を隠した」と読まない。**

> **規約の反映で減るのは顧客のときだけです**(閲覧 / 編集 / 管理では1画面も減りません)が、**役割の規則が名指しした画面は、その規則で許されていない相手からは誰であっても減ります**(閲覧でも編集でも管理でも減ります)。

## 項目単位の「見せる相手」

> **項目(フィールド)単位でも、見せる相手を書けます** —— 役割の規則(app.roles[].rules)に対象 field と表のID・項目のIDを書き、can に read と書くと、**その規則で許されていない役割と未ログインの読み取り応答から、その項目が落ちます**。

> **ただしこちらにも守らない経路があります** —— (a)**どの規則からも名指しされていない項目は今日どおり全員に出ます。既定は「出す」です** —— **書き忘れは「見えない」ではなく「見える」に落ちる**ので、客に出したくない項目があるなら1つずつ数えて規則に書いてください。

## 項目単位の「書ける相手」(**読める相手とは別の動詞**)

**【`V8-M20` で引き直された。旧版はこの主題を1文も持たず、下の「入っていないもの」に `writable_by` として送っていた】**

> **項目(フィールド)単位で、書ける相手も書けます**(読める相手とは別の動詞です)—— 同じ対象 field の規則に can: write と書くと、**その規則で許されていない役割がブラウザからその項目に値を書こうとした要求は、サーバが 403 で全体を拒否します**(値を黙って捨てるのではなく、1バイトも書きません)。**読める相手と書ける相手は別に書きます** —— 「本人に見せるが本人には書かせない」(集計された残高・ランク)を表せるのはそのためです。

> **ただしこちらにも守らない経路があります** —— (a)**どの規則からも名指しされていない項目は今日どおり誰でも書けます。既定は「書ける」です。**(b)**効くのは作成 / 更新 / まとめ書きの3経路だけです** —— **削除(DELETE)は1ミリも変わりません**(「書けない」と「消せない」は別のことです)。(c)**ワークフロー・島(run_function)・受信 capability・あなたの write_records はこの規則に1ミリも守られません** —— それらは「誰が呼んだか」を持たない経路だからです。(d)**後からでも書き直せます** —— 規則は set_roles で丸ごと差し替えるので、あとから足すことも外すこともできます(1本だけ足す / 消す手段は無く、全役割の全規則を書き直します)。(e)**状態の順番(取り消し済みの行を「完了」にする等)は今日も1つも検査できません。**

**【2026-08-12 訂正(`V8-M32`。`V8-M31` / `ADR-0327`)。上の引用の字面は1文字も書き換えていない。引用の外に足している】**
**上の (c)「ワークフロー・島(run_function)・受信 capability・あなたの write_records はこの規則に1ミリも守られません」のうち、`write_records` の部分は今日は偽である** ——
**`write_records` は起動設定の `ST_MCP_ACTOR` で名乗った人の権限で判定され、項目の「書ける相手」の規則が当たる。**
**ワークフロー・島・受信口の3つについては、項目の規則は今日も当たらない**(当たるのは行の判定のほうである)。
**【禁止】これを「全部の入口に効く」と書かない。**

## 表そのものを役割に開くこと(**その表の全員分の行が見える**)

**【`V8-M20` で引き直された。旧版はこの主題を予約規約フィールド `st_admin_readable` として持っており、この説明書は1文も引いていなかった】**

> **ただし例外が1つだけあります** —— **役割の規則(app.roles[].rules)で「この役割はこの表を読める」と書くと、その役割の人には、その表の他人の行も一覧 GET と単件 GET で見えます**。**この例外は3方向に閉じています** —— (1)**開くのは読取の2経路だけで、書込は1ミリも開きません** —— 見えている他人の行に PATCH / DELETE やバッチの update を掛けても、存在しない扱い(404)で弾かれます(**見えても直せません。**「運営が購入者のデータを管理できる」ようになったわけではありません)。(2)**開くのは、その表に「読める」と書いた役割だけです** —— 規則を1本も書いていない表では、今日どおり管理(owner)でも他人の行は読めません。(3)**規則に条件(when)を書いた場合は、行ごとに判定されます** —— 条件を書かなければ、その役割にはその表の全行が見えます。**重要な代償です。読み飛ばさないでください —— 「この表を読める」と書いた役割は、運営かどうかに関わらず、誰であっても全員分の行が見えます** —— **書き方を間違えると、本来自分の分だけが見えるはずだった人に、他の人の行が全部見えます。**

**【2026-09-12 訂正。上の引用を1バイトも消していない】** **末尾の「「この表を読める」と書いた役割は、運営かどうかに関わらず、誰であっても全員分の行が見えます」は、今日は次の2つの場合に偽である。**

1. **行ごとの付与(`access_control`)を宣言している表** —— **条件を1つも付けない読取の規則を1行書いても、それだけではその表の行を1件も読めない。読むにはその行ごとの付与が要る**(**運営者(役割 `owner`)は今日どおり全部読める**)。
2. **その宣言が名簿(`members.table`)・班(`groups.table`)・付与(`grant.table`)として名指しした表** —— **返るのは、その人が読める親の行に関わる行と、自分自身の行・自分が所属する班の行だけである**(**運営者は今日どおり全部見える**)。

**2 の絞りが効くのは3本の口だけである** —— **画面から読むとき・HTTP から読むとき・AI(MCP)から読むとき。**
**一覧は返る行が減り、行を1件だけ読む口と「その行が誰に配られているか」を返す口は、見えない行については「その行は存在しません」と同じ答え(404)を返す。**
**決まった時刻に動く処理・外部からの受信口・自動処理・アプリに埋め込んだコードには1ビットも効かない。**
**`enabled` を `false` にした宣言が名指しした表と、どの宣言からも名指しされていない表にも1ビットも効かない。**

**それ以外の表では、上の引用は今日も逐語で正である** —— **個人所有(`st_owner`)の表も含め、「この表を読める」と書いた役割にはその表の全員分の行が見える。**
**【禁止】これを「塞いだ」と読まない。**

## 削除(`DELETE`)にどこまで掛かるか

> **削除(DELETE)にも「消す」の権限が効きます** —— **役割の規則の側でも、表に書いた「消せる(delete)」は DELETE に効きます** —— **ただし項目に書いた規則とボタンの壁は、今日も DELETE に1ミリも掛かりません**(項目の規則が持つのは「読める」「書ける」だけで、ボタンの壁が立つのは作成と更新だけです)。

**【2026-09-14 訂正。上の引用を1バイトも消していない】上の引用は今日も逐語で正だが、削除に掛かる関門は今日これだけではない。**
**その行を親として参照している行(ぶら下がっている行)が1件でもある行を消そうとすると、1回目は必ず断られます** ——
**断りには、いちばん下の段まで数えた合計の件数と、その行が居る表の名前が載ります。**
**同じ件数を印として添えて2回目を送ると、その行とぶら下がっている行が下の段までまとめて消えます(取り消せません)。**
**ぶら下がっている行の中に、その人が1件ずつ消せないものが1件でもあるときは、件数も表の名前も1文字も出さずに断ります**(どの行が原因かは、断られた人には分かりません)。
**関門が掛かる口は、画面・HTTP・AI(この口)の3本です。** **双方向に書く** ——
**素通りする口は、受信口・自動処理・コードの島・決まった時刻に動く処理の4本で、そちらは今日も親だけが消えてぶら下がっていた行が残ります。**
**まとめて消す処理は原子的ではありません** —— **途中で失敗すると、そこまでに消えた行は消えたままです。**
**【禁止】これを「一度断るようにしたから、行が残らなくなった」と読まない** —— **止めているのは、一度断って選ばせるところまでです。**

## MCP から読むときは規則が1つも効かない

> いずれにせよ、MCP / ローカル操作そのものに権限制御は無く、**あなたが list_records で読むときは役割の規則を1つも見ません**(規則が効くのはブラウザからの HTTP だけで、MCP からは今日どおり全部見えます)

**【2026-08-12 訂正(`V8-M32`。`V8-M31` / `ADR-0327` / `ADR-0328`)。上の引用の字面も、上の見出しも1文字も書き換えていない】**
**上の引用は今日は偽であり、見出し「MCP から読むときは規則が1つも効かない」も今日は偽である** ——
**あなた(MCP)は起動設定の `ST_MCP_ACTOR` で「この人として動く」を名乗り、名乗った人の権限で判定される**
(名乗りが無いと23本のツールはすべて失敗する)。
**`list_records` は面(表の関門)・行の判定・個人スコープ・項目の射影を順に通るので、名乗った人に見えない行と項目は返らない。**
**今日も規則を1つも通らないのは「決まった時刻に動く処理(`schedule`)」の1本だけで、0本ではない。**
**【禁止】これを「全部の入口に効く」と書かない。**
**【2026-08-16 訂正(`V8-M13-T04`。台帳 `Q-G30`)。直前の括弧を1バイトも消していない】**
**括弧の中の本数は今日は偽である** —— **`V8-M13-T02` が `read_report`(集計表を1枚読む参照系)を足したので、今日は24本である。**
**【2026-08-24 訂正。直前の1文を1バイトも消していない】**
**直前の「今日は24本である」も今日は偽である** —— **`list_comments`(積まれたコメントを読む参照系)を1本足したので、今日は25本である。**
**足したのは読む道具1本だけであり、書く道具は1本も増えていない。**
**【2026-09-12 訂正。直前のどの文も1バイトも消していない】**
**上の「`list_records` は面(表の関門)・行の判定・個人スコープ・項目の射影を順に通る」は、今日は関門が1つ足りない** ——
**画面名を名乗らない読取の壁も通る。**
**あなたには画面名を渡す手段が1つも無いので、つねに「名乗らない一覧」として判定され、その表を指す一覧画面と集計画面を1本も読めなければ、返る行は0件になる。**
**その表を指す、その形の画面が1本も無い表では、今日どおり全行が返る。**
**【2026-08-25 訂正。直前の2文を1バイトも消していない】**
**直前の「今日は25本である」も、直前の「書く道具は1本も増えていない」も今日は偽である** —— **`set_comment_visibility`(コメントの欄と読む場所を画面に出すかを倒す書く道具)を1本足したので、今日は26本であり、名乗りが無ければその26本すべてが今日も失敗する。**

---

## この参照ファイルに入っていない「できないこと」

**`CANNOT_DO` の全文は入っていない。** 入れなかったもののうち、主なものは:

- **値の意味に応じた強調(`emphasis`)**についてのもの —— この説明書は1文も引いていない。
- **テーマの当たる範囲**(外枠・読み込み中の表示・ボタンの色・チェックボックス)についてのもの。
- **予約規約フィールド4本**(`st_owner` / `st_public` / `st_undeletable` /
  `st_no_direct_create`)についてのもの。**5本目だった `st_admin_readable` は `V8-M20` が
  撤去した** —— 同じことは上の「表そのものを役割に開くこと」に置き直されている。
- **ボタン(役割の規則の `target: "action"`)の壁がどこに立つか**についてのもの ——
  上に引いた逐語は「`DELETE` には掛からない」ことしか述べていない。
  **壁そのものの説明は `CANNOT_DO` ではなく `VOCABULARY_SCOPE`**(`apply_diff` のツール説明)**にある。**
- **付与(`access_control`)**についてのもの。この説明書は1文も引いていない。

**この3つの説明書は `CANNOT_DO` の代わりではない。**
正は `src/mcp/vocabulary.ts` の `CANNOT_DO` そのものである。

**【2026-08-16 訂正(`V8-M47`)。直前の2文を1バイトも消していない】**
**直前の「この3つの説明書は `CANNOT_DO` の代わりではない。」は今日は偽である** —— **今日は3本ではなく4本である**(4本目は `app-build`。アプリを組み立てる順序と骨格)。
**ただし「`CANNOT_DO` の代わりではない」という趣旨は今日も変わらない** —— **4本になっても、説明書は語彙の正の代わりにはならない。**
