# 画面定義に書けるキーの全量(31キー)

**この表の出所は `schemas/manifest.schema.json` の `$defs/view` である。**
食い違いを見つけたら、スキーマのほうを信じること。

**【2026-08-25 追記】このキーをどう書いても、差分を試す口は開かない。**
**画面を足す・変える差分を「当てずに試す」`dry_run_diff` には、`apply_diff` とまったく同じ
判定(役割の規則の `target: "app"` / `can: ["write"]`)が掛かる。**
**見るのは `target: "app"` の規則だけであり、`target: "view"` の規則は1つも見ない。**
**役割の規則を1本も書いていないアプリでは、持ち主でも断られる**
(直し方は `set_roles` で `owner` にその1行を書き戻すこと)。

**【`V8-M20` で 29 → 28 になった】** **画面の `audience`(見せる相手)が撤去された。**
**同じことは役割の規則(`app.roles[].rules`)に書く**(下の「画面を『見せる相手』」)。

**【2026-08-14。`V8-M8` で 28 → 29 になった】** **画面種別に4種目 `report_view`(集計表)が
加わり、その中身を書く `report` が29キー目として入った。** **`report` を書けるのは
`report_view` だけで、他の3種に書くと差分全体が拒否される。** **逆に `report_view` には
`columns` / `fields` / `related` / `actions` / `sort` / `filter` / `sum_field` /
`after_save` / `modal` / `reference_pickers` / `search_fields` / `page_size` /
`field_groups` と `preset_` で始まる9キーを1つも書けない**(書けるのは
`id` / `type` / `table` / `name` / `menu_listed` / `custom_css` / `report` の7つだけ)。

**【2026-08-20 に 29 → 30 になった。上の2つの訂正は書き換えていない】**
**削除が成立したあとの行き先を書く `after_delete` が30キー目として入った。**
**書けるのは `detail_view` だけである。**
**行き先にできるのは一覧画面(`list_view`)か集計表(`report_view`)だけで、
詳細画面 / 入力画面を指した差分は拒否される。**
**書かなければ今日どおりで、同じテーブルの一覧があればそこへ、無ければ画面一覧へ戻る。**

**【2026-08-20 に 30 → 31 になった。上の3つの訂正は書き換えていない】**
**一続きの流れの中の段を書く `flow` が31キー目として入った。**
**書けるのは `list_view` / `form` / `detail_view` の3種別で、集計表(`report_view`)には書けない。**
**書くのは3つだけである** —— 流れの名前(`id`)/ その流れの中の位置(`step`。1から始まる整数)/
段の種類(`kind`。`input` か `confirm`)。
**同じ名前を書いた画面どうしが1つの流れになる**(`id` は画面のIDではないので、
複数の画面が同じ名前を持つのが普通である)。
**同じ流れの中で位置が重なっている差分・位置が抜けている差分・1から始まっていない差分は拒否される。**
**確認の段(`kind: "confirm"`)を書けるのは `detail_view` だけである。**

## 31キーの一覧

| キー | 形 | どの種別に書けるか |
|---|---|---|
| `id` | ID | 3種すべて(**必須**) |
| `type` | `form` / `list_view` / `detail_view` / `report_view` | 4種すべて(**必須**) |
| `table` | ID | 3種すべて(**必須**) |
| `name` | 文字列 | 3種すべて |
| `columns` | ID の配列(1件以上) | list_view(**必須**) |
| `fields` | ID の配列(1件以上) | form(**必須**)/ detail_view |
| `sort` | 並べ替え1件、またはその配列 | list_view |
| `filter` | 絞り込みの木 | list_view |
| `related` | 子一覧の配列 | **detail_view だけ** |
| `actions` | 操作の配列 | list_view / detail_view |
| `menu_listed` | 真偽値 | 3種すべて |
| `modal` | 真偽値 | **form だけ**。書くなら `menu_listed` も必須 |
| `field_groups` | 項目のまとまり | **detail_view だけ** |
| `search_fields` | ID の配列(1〜8件) | list_view |
| `page_size` | `10` / `20` / `50` / `100` | list_view |
| `after_save` | ID | form / detail_view |
| `sum_field` | ID | list_view |
| `custom_css` | `{ asset, digest }` | 3種すべて |
| `preset_column_align` | 列ID → `left` / `center` / `right` | list_view |
| `preset_column_width` | 列ID → `narrow` / `standard` / `wide` | list_view |
| `preset_pager_position` | `top` / `bottom` / `both` | list_view |
| `preset_label_placement` | `inline` / `stacked` | form / detail_view |
| `preset_field_columns` | `1` / `2` | form / detail_view |
| `preset_image_size` | `thumbnail` / `medium` / `original` | list_view / detail_view |
| `preset_text_preview` | `short` / `standard` / `long` / `full` | list_view / detail_view |
| `preset_list_shape` | `table` / `card` | **list_view だけ** |
| `preset_density` | `comfortable` / `compact` | 3種すべて |
| `reference_pickers` | 参照項目ID → `list` / `type_filter` / `search` | **form だけ** |
| `report` | `{ group_by, aggregates, filter? }` | **report_view だけ**(**必須**) |
| `after_delete` | ID(行き先は list_view / report_view だけ) | **detail_view だけ** |
| `flow` | `{ id, step, kind }`(`kind` は `input` / `confirm`) | list_view / form / detail_view(**`confirm` は detail_view だけ**) |

**32個目のキーは無い。** 一覧に無い名前を書くと差分全体が拒否される。

**【2026-08-14。`V8-M9` による訂正。上の表の行を1バイトも書き換えていない】**
**`report` の形は今日は `{ join?, group_by, aggregates, filter? }` である**(**画面のキーの数は
29 のままで、増えたのは `report` の**中**である)。**`join` には結合を4本まで書け、
1本は `{ table, via }` の2キーちょうどである**(`via` は `table` の表に実在する
`reference` 型の項目)。**結合できるのは起点を含めて5表まで・参照の鎖は3段まで・
一対多(親 → 子)の向きは1本まで。** **`group_by` と `aggregates` の要素には
`table`(その項目を持っている表)を書け、省略すると起点の表を指す。**
**【誇張しない】結合できるのは集計表の中だけである** —— **子一覧(`related`)・関数の入力・
`update_record` の `target` は今日も1ホップのままで、1バイトも変わっていない。**
**結合の条件式・射影(`select`)・計算式は今日も1つも書けない。**
**結合先の列で絞り込むこともできない**(`filter` は起点の表にだけ掛かる)。

**【2026-08-15。`V8-M9` による訂正。上の行を1バイトも書き換えていない】**
**`group_by` の `field` には、予約された行の番号 `_id` を書ける** ——
**その表の**行そのもの**で束ねる(1行が1群)。** **「1件ずつ並べて数える」形を、その表に
`select` や `reference` の項目が1つも無くても書けるのはこれである**
(**結合先に相手が1件も無い行も、0 として並ぶ**)。
**書けるのは `group_by` の `field` だけである** —— **`aggregates` の `field` には書けず、
`granularity` とも同時に書けない。** **`_id` 以外の予約名(`_created_at` / `_updated_at` /
`st_` で始まる予約規約の項目)は1つも書けない。** **結合先の表の `_id` も書ける**
(`{ "table": "...", "field": "_id" }`)。
**【誇張しない】群の見出しに出るのは行の番号(UUID)そのものであって、その行の
代表の項目の値ではない。** **群の数は起点の表の行数と等しくなるので、行が多い表では
群の上限に当たる。**

**【2026-08-15。`V8-M10` による追記。上の行を1バイトも書き換えていない】**
**上の2箇所が「上限」と書いているものの実数は、次の4つである。**

| # | 上限 | 実数 | いつ止まるか |
|---|---|---:|---|
| 1 | 結合できる表(起点を含む) | **5** | **差分を適用するとき**(拒否される) |
| 2 | 参照の鎖の段数 | **3** | **差分を適用するとき**(拒否される) |
| 3 | 群にまとめる前に読む行(**表ごと**) | **10,000** | **集計を要求したとき**(400) |
| 4 | 作る群の数 | **10,000** | **集計を要求したとき**(400) |

**4つとも、その値ちょうどは通り、超えたときだけ拒否される**(inclusive)。
**上限に当たった応答には群も合計も1つも入らない**(部分的な結果を返さない)。
**本当に0件のときは 200 で「群0・合計0・件数0」が返るので、上限と0件は取り違えられない。**
**合計の上限は無い** —— **5表 × 10,000 = 50,000 行がメモリに乗りうる。**
**上限は宣言から変えられない** —— **マニフェストにも `schemas/` にも実数は1つも出てこない。**
**【誇張しない】3 と 4 は差分を適用するときには1つも検査されない** ——
**差分は通り、行が増えた日にその集計表が開かなくなる。**
**そのときの直し方は、絞り込み(`report.filter`)で対象を狭めるか、集計表を分けることである。**

**【2026-08-16 追記(`V8-M13-T04`。台帳 `Q-G30` / `Q-G29`)。上の行を1バイトも書き換えていない】**
**上の記述が書かれた時点で無かったものが、今日は在る。**

- **集計表はブラウザに描かれる**(`V8-M11`)。画面の上にグラフ、下に群の表が出る。
- **グラフは棒と折れ線の2種**だけで、`report.chart` に `bar` / `line` のどちらか1つを書く(`V8-M12`)。**省略できる**(省略すると棒)。**円・散布図・積み上げ・ドーナツ・面は1つも書けず、どの集計を描くかも選べない**(描かれるのは `aggregates` に宣言した集計そのもの)。**1つの集計表に置けるグラフは1つで、`list_view` / `form` / `detail_view` には書けない。****集計表の外にはグラフは今日も1つも無い**(常設領域にも出せない)。
- **集計の母集団には、その画面を開いた人が読める行だけが入る**(`V8-M10`)—— したがって**見る人によって数が変わる**。**掛かるのはこの読取経路だけである** —— **島が `output_table` へ書いた表には1ミリも掛からず、受信口・ワークフロー・`schedule` は今日も素通りである。** **【禁止】これを「集計に権限が効くようになった」と書かない。**
- **MCP からは `read_report` で集計表を1枚読める**(`V8-M13-T02`)。**`list_records` は今日も `report` を1つも見ない。**

**【`Q-G29`。門Aの判定 = 限定採用】** **集計を書く道は2本ある** —— **宣言の道(`report_view`)と、島の道(`function` で数えて `output_table` に書き、`list_view` で見せる)である。** **正は宣言の道である。**
**島の道を選ぶ条件は3つだけである** —— **（あ）結果を他の画面から参照したいとき、（い）結果をワークフローの発火元にしたいとき、（う）見る人によらず同じ数を出したいとき。** **4つ目の条件を足すには改めて審査が要る。**
**2本の道は同じデータに違う数を返すのが正常であり、片方をもう片方に移せるとは書けない。**

## 一覧に横スクロールせずに入る列の数(ブラウザで実測した)

**【2026-08-16 追記(`V8-M47`)。上の `preset_column_width` の行を1バイトも書き換えていない。**
**あの行は選択肢の名前しか書いていないので、実数と、それが何列に化けるかをここに足す】**

- **ルートの文字の大きさは 16px、シェルの最大幅は `60rem`(= 960px)である。**
- **列幅の選択肢の実数は `narrow` = `6rem`(96px)/ `standard` = `12rem`(192px)/
  `wide` = `24rem`(算術では 384px。**この1つだけは実測していない**)。**
- **一覧の器が実際に使える幅は 960px ではなく 720px である** —— 960px から、
  シェルの左右の余白と、画面一覧の脇の帯(`12rem`)と、その隙間(`1rem`)が引かれる。
- **ビューポートの幅が 1280px でも 1440px でも(ルートが 16px のとき)、`standard` の列は
  3列までが横スクロールせずに入る。4列目から溢れる。**
- **1024px 以上では、ビューポートをいくら広げても器は 720px のままで、答えは3列で変わらない。**
  **効くのは 1024px より狭いときだけである**(900px → 3列 / 768px → 2列 / 640px → 2列 / 375px → 1列)。
- **3列以下では `standard` は効かない** —— 表は幅を固定して描くので、
  指定した幅の合計が器に満たないと、余りが等分される(3列のとき1列 239.94px になった)。

**測り方**: 一覧の器(横スクロールする要素)の `clientWidth` を取り、
各列の見出しの右端がその器の右端を超えないものを数えた。
**境目は `scrollWidth > clientWidth` が3列で偽・4列で真に変わるところである。**

## `reference_pickers` の意味(名前だけでは取れないので書く)

**入力画面(`form`)ごとに、参照項目の選び方を上書きする。**

```json
"reference_pickers": { "partner": "search", "category": "type_filter" }
```

- **キーは参照項目のフィールドID、値は `list` / `type_filter` / `search` の3つだけ**
  (`list` = 一覧から選ぶ。既定 / `type_filter` = 打った文字で絞る / `search` = 別の面を開いて探す)。
  **4つ目を書くと差分全体が拒否される。**
- **`form` にだけ書ける。** `list_view` / `detail_view` に書くと差分全体が拒否される ——
  一覧と詳細は値を表示するだけで、選ぶ操作が構造的に無いためである。
- **優先順位は「画面 > 項目 > 既定」の1本だけ。** ここに書いた項目は、項目の側の
  `reference_picker` より優先される。書かなかった項目は項目の側に、それも無ければ既定に倒れる。
- **全置換である。** 項目ごとに1本ずつ足す / 消す手段は1つも無い。**前進では外せない。**
- **書かなかった画面は1ピクセルも変わらない。**
- **どの器で描くかは書けない。** 器の割り当てはプラットフォームが持つ。
- **何を打つと当たるかは、この画面の側では決められない** —— それは参照される側のテーブル、
  または参照する項目の `reference_search_fields` である(→ diff-shape の説明書)。

## 並べ替え・絞り込み・子一覧・操作の形

```json
"sort": { "field": "due_on", "order": "asc" }
"sort": [ { "field": "state", "order": "asc" }, { "field": "due_on", "order": "desc" } ]
```

`field` と `order` は**両方必須**。`order` の値は `asc` / `desc` の2つだけ。

```json
"filter": { "and": [ { "field": "state", "equals": "open" },
                     { "field": "due_on", "lte": "2026-12-31" } ] }
```

葉は `field` が必須で、そのうえで `equals` / `contains` / `gte` / `lte` / `in` の
**どれか1つだけ**を持つ。枝は `and` / `or`(1件以上の配列)と `not`(1件)。

```json
"related": [ { "table": "comment", "via": "task", "columns": ["body", "created_on"] } ]
```

`table` / `via` / `columns` が**必須**。`via` は子テーブル側の `reference` 型フィールドで、
**親を辿る1ホップだけ**である。

```json
"actions": [ { "form": "task_form", "prefill": { "state": "open" } } ]
```

各要素は `form` + `prefill` / `set` / `view` / `run` の**どれか1組**を持つ。

**【2026-08-16 追記(`V8-M47`)。直前の1行を1バイトも書き換えていない】**
**1つの操作に書けるキーは8つで、9つ目は書けない。**

| キー | 何を書くか |
|---|---|
| `id` | 操作のID。**役割の規則(`target` が `action`)で名指しするにはこれが要る** |
| `name` | 画面に出す名前 |
| `form` | 開く入力画面のID(`prefill` と対で書く) |
| `prefill` | 開いた入力画面に先に入れる値 |
| `set` | 今開いている行の項目を書き換える |
| `view` | 開く画面のID |
| `run` | 動かすワークフローのID |
| `visible_when` | この操作の起点を出す条件 |

**`form`+`prefill` / `set` / `view` / `run` の4形は排他である**(どれか1組ちょうど)。

**`visible_when` の値は絞り込みの葉(`filter_leaf`)そのものである** ——
**`field` が必須で、`equals` / `contains` / `gte` / `lte` / `in` のうちちょうど1つを持つ**
(**葉に書けるキーは全部で6つ**)。**枝(`and` / `or` / `not`)は1つも書けない。**
**判定に使えるのは、今開いている行の値だけである。**
実物で見つかった形は `{ "field": "done", "equals": false }` と
`{ "field": "status", "equals": "active" }` の2つで、
**`equals` 以外の演算子を使った実物は、探した範囲では見つからなかった。**

**【混ぜないこと】ワークフローの `when` は別の定義である** ——
**あちらに書けるキーは `field` と `equals` の2つだけで、両方が必須である。**
**「ワークフローの `when` と同じ」は今日は偽である。**

## 一続きの流れ(`flow`)の値と、段の並び

```json
{ "op": "add_view",
  "view": { "id": "entry_list", "type": "list_view", "table": "entry",
            "columns": ["title", "state"],
            "flow": { "id": "apply", "step": 1, "kind": "input" } } }
```

### 3つの鍵だけで、4つ目は無い

| 鍵 | 必須 | 値 | よくある取り違え |
|---|---|---|---|
| `id` | **必須** | 流れの名前(文字列) | **画面のIDではない。** 同じ流れに属する画面は、全部この同じ名前を書く |
| `step` | **必須** | **1から始まる整数** | 0・負の数・小数・文字列は書けない |
| `kind` | **必須** | `input` / `confirm` の2つだけ | 他の語(`review` / `done` など)は書けない |

**この3つ以外の鍵を1つでも書くと、差分全体が拒否される。**
**段の見出し・アイコン・色・所要時間・次の段の条件を書く場所は、語彙のどこにも無い。**

### どの種別に書けるか

| 種別 | `flow` を書けるか | `kind` に書ける値 |
|---|---|---|
| `list_view` | 書ける | **`input` だけ** |
| `form` | 書ける | **`input` だけ** |
| `detail_view` | 書ける | `input` / `confirm` |
| `report_view` | **書けない**(差分全体が拒否される) | ― |

### 差分を適用するときに拒否される形(実際のメッセージの要点)

| 書いた形 | どうなるか |
|---|---|
| 同じ `id` の中で同じ `step` を2つの画面が持つ | **拒否。**「流れ …… の中で、位置(step)…… が重複しています」 |
| 同じ `id` の中で `step` が飛んでいる(1,2,4) | **拒否。**「位置(step)が連番になっていません。…… が抜けています」 |
| `step` が 1 から始まっていない(2,3) | **拒否**(上と同じ理由。1 が抜けている扱い) |
| `report_view` に `flow` を書いた | **拒否**(その種別では書けないキー) |
| `form` / `list_view` に `kind: "confirm"` を書いた | **拒否** |
| `flow` に4つ目の鍵を書いた | **拒否** |

**別の流れであれば、同じ位置を書いてよい**(位置が衝突するのは同じ `id` の中だけである)。
**`id` そのものの重複は拒否されない** —— **同じ名前を複数の画面が持つのが、流れの作り方である。**

### 段の並びは宣言から導かれる

- **並び順を保存する場所は無い。** マニフェストのどこにも「流れの一覧」は無い。
- **「次」は、同じ `id` を持ち `step` がいまの位置 + 1 の画面1つである。**
- **その画面がそのアプリに居なければ、そこが最後の段として扱われる**(次へは進まない)。

### 後から変えられるか

**`update_view` の `changes` に `flow` を書けるので、後から差し替えられる。**
**ただしキーを消す手段は無い**(別の値に差し替えるか、`undo` で戻す)。
**差し替えた結果、位置が重複したり飛んだりする形になれば、その差分は拒否される。**

### 次の段へ進むきっかけと、運ばれるもの

**きっかけは画面の種別で違う**(表と条件は view-shape の説明書の「一続きの流れ」の節にある)。
要点だけをここにも置く:

- **入力画面は保存が成立したら次の段へ進む。**「次へ」のボタンは出ない。
- **一覧画面と、`kind` が `input` の詳細画面には「次へ」のボタンが出る。**
- **`kind` が `confirm` の詳細画面には「次へ」は出ない** ——
  **その画面に書いた値の書換のボタン(`actions` の `set` の形)の書込が成立したときに進む。**
- **自動処理を起こすボタン(`actions` の `run` の形)では、段を宣言していても進まない。**
- **「次へ」は、次の段の画面がその人に見えないときには出ない**(ボタンごと出ない)。
- **運ばれるのは、次の段が同じテーブルの詳細画面のときの、いま扱っている行1件だけである。**
  **それ以外では何も運ばれず、前の段で入力した値は1つも運ばれない。**
- **流れは1つのアプリの中に閉じる** —— **外部の URL も、別のアプリの画面も書けない。**
- **ボタンの文言は今日も宣言できない。**

### 段を宣言すると自動で出る2つ(**宣言する口は1つも無い**)

**`flow` を書いた画面には、書かなくても次の2つが出る。** **どちらも出し方・文言・位置を
書く鍵は無く、出す / 出さないの選択もできない。**

| 出るもの | 出る画面 | 形 | 押せるか |
|---|---|---|---|
| いま何段目 / 全部で何段 | **`flow` を書いた `list_view` / `form` / `detail_view` の全部** | `3段目 / 全5段` | **押せない**(他の段へ飛べない) |
| 「戻る」 | **`flow` を書いた `form` だけ**(`modal: true` を除く) | ボタン1つ | 押すと**履歴を1つ戻る** |

**【2026-08-21 訂正。上の表は1行も1セルも書き換えていない】**
**表の「戻る」の行にある「`flow` を書いた `form` だけ」は、今日は偽である。**
**「戻る」は、段を宣言した画面には種別を問わず1つ出る** —— **`list_view` にも
`detail_view` にも `form` にも出る。** **出ないのは、段を宣言していない画面と、
重ねて出す画面(`modal: true`)の2つだけである。**

**`flow` を書いていない画面には、どちらも1ピクセルも出ない。**

位置と総数について:

- **出るのは2つの数だけである。** 段の名前・`kind`・進み具合の割合は1文字も出ない。
- **`name` に段番号を手で書かないこと** —— 同じ数が2箇所に並び、`step` を入れ替えたときに
  `name` のほうだけがずれる。**そのずれで赤くなる検査は1本も無い。**
- **いま何段目かは、同じ `id` の画面を `step` の順に並べたときの順位である。**
  **`step` の値そのものではない**(欠番の残る古い定義ではずれる)。
- **全部で何段かは見る人によって変わる** —— **ログインしている人には、その人に開けない段も
  総数に入る。** **ログインしていない人には、見せると決めた段だけが総数に入る**ので、
  同じ流れが小さい数に見える。**どちらの数え方かを宣言する鍵は無い。**

「戻る」について:

- **戻り先を書く鍵は1つも無い。** 段の並びを1つ遡るのではなく、**履歴を戻るだけ**である。
- **URL を直接開いた場合は履歴に前の項目が無く、押しても何も起きない。**
- **`step` が 1 の画面にも出る。** そのときの行き先は流れの外である。
- **`modal: true` の入力画面には出ない**(閉じる操作が既に同じことをしている)。
- **`list_view` と `detail_view` には出ない。**
- **【2026-08-21 訂正。上の1行は1バイトも消していない】** **その字面は今日は偽である。**
  **段を宣言した `list_view` と `detail_view` にも「戻る」は1つ出る。**
  **段の位置でも段の種類でも分岐していないので、最初の段にも最後の段にも出る。**
  **出し方・文言・位置・出す出さないを宣言する口は、今日も1つも無い。**

## 画面を「見せる相手」—— 画面定義には書かない

**【`V8-M20` で置き直された】** **かつてはこの節に `audience`(見せる相手)という
画面定義のキーがあり、`add_view` のときだけ書けた。****今日そのキーは1つも無い。**
**同じことは役割の規則(`app.roles[].rules`)に書き、差分操作は `set_roles` である。**

```json
{ "op": "set_roles",
  "roles": [
    { "id": "owner",  "name": "管理" },
    { "id": "editor", "name": "編集" },
    { "id": "viewer", "name": "閲覧",
      "rules": [ { "target": "view", "view": "task_list", "can": ["read"] } ] }
  ] }
```

- **1本の規則は `target`(何に対して)と `can`(何ができるか)が必須。**
  `target` は `table` / `field` / `view` / `action` の**4つだけ**。
  - **【2026-08-12 訂正(`V8-M32`。`V8-M28` / `ADR-0322`)。直前の1行を1バイトも消していない】**
    **その字面は今日は偽で、`target` は6つある** —— **5つ目は `app`(アプリの設定 = 定義の変更)、**
    **6つ目は `role`(人に役割を配ること)。**
    **どちらも動詞は `write` 1語ちょうど**(`read` も `delete` も書けない)で、
    **名指しする先も条件(`when`)も1つも書けない**(アプリ全体 / 役割の配布そのものが対象だからである)。
- **画面を名指しするときは `view`(画面ID)を書き、`can` は `["read"]` の1語だけ**
  (画面に `write` / `delete` は書けない)。
- **ボタンを名指しするときは `view` と `action` の両方を書く** ——
  `action` は画面の `actions[].id` である。**`id` を書いていないボタンは名指しできない。**
- **どの規則からも名指しされていない画面は、今日どおり全員に見える**(既定は「変えない」)。
  - **【2026-08-12 訂正(`V8-M32`。`V8-M26` / `ADR-0317` / ユーザ決定 `D-V8-38` / `D-V8-42`)。直前の1行を1バイトも消していない】**
    **その字面は今日は偽である** —— **規則が1本も無い画面は、既定で閉じる**(表・ボタンも同じ)。
    **役割を1つも宣言していないアプリでも閉じる。**
    **項目(`field`)だけは今日も管轄外で、全員に出る** —— **閉じたのは3対象であって4対象ではない。**
    **ただし画面を足すと、既定の3役割ぶんの規則が自動で入る。**
  - **【2026-09-05 訂正。直前の訂正の塊を1バイトも消していない】**
    **ボタンについては、上の「既定で閉じる / 足すと3役割ぶん入る」だけでは足りない** ——
    **行ごとの付与(表の定義の `access_control`)を宣言した表では、規則で開いていても、
    その行への付与が無ければそのボタンは出ない**(宣言していない表は今日どおり役割の規則だけで決まる)。
    **見る対象は形ごとに違う** —— `set` と `run` はその行の書込、`view` はその行の読取、
    付与表に行を作る `form` はその行に付与を配れるか。
    **出さないだけで、遮断は今日もサーバの 403 である。**
- **全置換である。** 1本だけ足す / 消す手段は無く、書き直すときは全役割の全規則を書き直す。

## 役割ID(`roles[].id`。規則の主体)に書ける値

- **運営の予約ロール3つ**: `owner`(管理)/ `editor`(編集)/ `viewer`(閲覧)。
  **この3つは必ず宣言に含める**(全置換で消すと適用時の検査で拒否される)
- **`anonymous`**(未ログイン)。**これはロールではなく「ロールを持たない人」を指す値である**
- **アプリが宣言した非運営の種類**(`set_user_kinds`)。宣言していないアプリでは `customer`(顧客)の1種だけ
  - **【2026-08-11 訂正(`V8-M29`。台帳 `T-G9a` / `T-G11`)。旧行を1バイトも消していない】**
    **`set_user_kinds` と `app.user_kinds` は廃止された。** **非運営の種類も `set_roles`(`app.roles`)で宣言する。**
    **宣言していないアプリで `customer` の1種だけであることは今日も変わらない。**

値の形は**小文字英数と `_`・1〜32文字**。**役割は1件以上12件以下**で、
**同じ `id` を2回書くと適用時の参照整合の検査で拒否される。**
**役割1つに書ける規則は最大64本。**

**`anonymous` の規則だけは狭い** —— **`target: "field"` を書けず、`can` は `read` だけである。**

**【正直に書く】`roles[].id` が `user_kinds` に実在するかは、今日1件も検査されない** ——
値の形(パターン)だけが見られる。**宣言していない種類名を書いた差分は今日は通る。**
**【2026-08-11 訂正。旧の2文を1バイトも消していない】** **`user_kinds` は廃止されたので、**
**「実在するか」を照らす先そのものが無い。** **値の形だけが見られることは今日も変わらない。**

## 規則の条件(`when`)—— 行ごとに判定する

**表(`table`)と項目(`field`)を対象にした規則にだけ、`when` を1つ書ける。**
**画面(`view`)とボタン(`action`)には書けない**(スキーマが `when: false` で塞いでいる)。

```json
{ "target": "table", "table": "task", "can": ["read", "write"],
  "when": { "and": [ { "field": "state", "equals": "doing" },
                     { "field": "assignee", "equals_current_user": true } ] } }
```

- **葉は2形だけ**: `{ "field": ..., "equals": ... }`(値が定数と等しい。`equals` は文字列 /
  数値 / 真偽値)と `{ "field": ..., "equals_current_user": true }`(値が要求している人と等しい)。
  **`equals_current_user` に書けるのは `true` だけ**で、`false` は書けない。
  - **【`V8-M26` / ユーザ決定 `D-V8-70`。2026-08-11。直前の「葉は2形だけ」を1バイトも
    消していない】その字面は今日は偽で、葉は3形ある。** 3つ目は
    `{ "field": ..., "is_empty": true }`(**その項目が空**)。
    **空とみなすのは `null` と、値が入っていないことと、長さ0の文字列の3つだけで、
    `0` と `false` は空ではない。**
    **`is_empty` に書けるのも `true` だけ**で、`false`(空でない)は書けない —— 否定は `not` で書く。
    **この形は要求している人を1バイトも見ない**ので、**ログインしていない要求でも成り立つ**
    (`equals_current_user` はログインしていなければ必ず偽で、向きが違う)。
    「自分の行、または持ち主が空の行(みんなのもの)」は次のように書く:

    ```json
    { "or": [ { "field": "st_owner", "equals_current_user": true },
              { "field": "st_owner", "is_empty": true } ] }
    ```

    **`st_owner` を持つ表を作ると、この形の規則が既定の3役割に自動で入る。**
- **枝は3形だけ**: `{ "and": [...] }` / `{ "or": [...] }`(**どちらも2件以上**)/ `{ "not": {...} }`。
- **入れ子の深さの上限は8段**(絞り込み `filter` と同じ上限)。超えると差分全体が拒否される。
- **`field` はその規則が名指しした表の項目でなければならない** —— 実在しない項目を書くと
  差分全体が拒否される。**参照を1ホップも辿らない**ので、別の表の項目は書けない。
- **比較(`gt` / `lt` / `gte` / `lte`)・集計値・件数は1つも書けない。**

## この参照ファイルが持っていないもの

- **テーマの25スロットの名前**。1つも列挙していない。
- **`field_groups` の中身の形**。
- **`custom_css` の資産をどう発行するか**(発行するのは人間の owner であって、AI ではない)。
